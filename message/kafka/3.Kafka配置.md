# Kafka配置

配置文件：properties文件。

配置方式：配置文件或编程式配置。

## 3.1 代理配置（Broker）

基本属性名称：

```
broker.id
log.dirs
zookeeper.connect
```

### advertised.listeners

```
属性名称：advertised.listeners
属性描述：与listeners配置不同时，发布到ZooKeeper，给客户端使用的监听器。
         在IaaS环境中，这可能需要与代理绑定的接口不同。
         如果没有设置，将使用listeners的值。
         与listeners不同，公开0.0.0.0元地址是无效的。
         也与listeners不同，这个属性可以有重复端口，因此，一个监听器可以配置为公开另一个监听器的地址。
         在使用外部负载均衡器的某些情况下，这可能很有用。
数据类型：string
默认的值：null
有效的值：
重要等级：高
更新模式：每个代理
```

### auto.create.topics.enable

```
属性名称：auto.create.topics.enable
属性描述：是否启用在服务器上自动创建主题
数据类型：boolean
默认的值：true
有效的值：
重要等级：高
更新模式：只读
```


```
属性名称：auto.leader.rebalance.enable
属性描述：Enables auto leader balancing.
A background thread checks the distribution of partition leaders at regular intervals,
configurable by `leader.imbalance.check.interval.seconds`.
If the leader imbalance exceeds `leader.imbalance.per.broker.percentage`,
leader rebalance to the preferred leader for partitions is triggered.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：background.threads
属性描述：The number of threads to use for various background processing tasks</p>
数据类型：int
默认的值：10
有效的值：[1,...]
重要等级：高
更新模式：cluster-wide
```

```
属性名称：broker.id
属性描述：The broker id for this server.
If unset,
a unique broker id will be generated.To avoid conflicts between zookeeper generated broker id's and user configured broker id's,
generated broker ids start from reserved.broker.max.id + 1.</p>
数据类型：int
默认的值：-1
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：compression.type
属性描述：Specify the final compression type for a given topic.
This configuration accepts the standard compression codecs ('gzip',
'snappy',
'lz4',
'zstd').
It additionally accepts 'uncompressed' which is equivalent to no compression; and 'producer' which means retain the original compression codec set by the producer.</p>
数据类型：string
默认的值：producer
有效的值：
重要等级：高
更新模式：cluster-wide
```

```
属性名称：control.plane.listener.name
属性描述：Name of listener used for communication between controller and brokers.
Broker will use the control.plane.listener.name to locate the endpoint in listeners list,
to listen for connections from the controller.
For example,
if a broker's config is :<br>listeners = INTERNAL://192.1.1.8:9092,
EXTERNAL://10.1.1.5:9093,
CONTROLLER://192.1.1.8:9094<br>listener.security.protocol.map = INTERNAL:PLAINTEXT,
EXTERNAL:SSL,
CONTROLLER:SSL<br>control.plane.listener.name = CONTROLLER<br>On startup,
the broker will start listening on "192.1.1.8:9094" with security protocol "SSL".<br>On controller side,
when it discovers a broker's published endpoints through zookeeper,
it will use the control.plane.listener.name to find the endpoint,
which it will use to establish connection to the broker.<br>For example,
if the broker's published endpoints on zookeeper are :<br>"endpoints" : ["INTERNAL://broker1.example.com:9092","EXTERNAL://broker1.example.com:9093","CONTROLLER://broker1.example.com:9094"]<br> and the controller's config is :<br>listener.security.protocol.map = INTERNAL:PLAINTEXT,
EXTERNAL:SSL,
CONTROLLER:SSL<br>control.plane.listener.name = CONTROLLER<br>then controller will use "broker1.example.com:9094" with security protocol "SSL" to connect to the broker.<br>If not explicitly configured,
the default value will be null and there will be no dedicated endpoints for controller connections.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：controller.listener.names
属性描述：A comma-separated list of the names of the listeners used by the controller.
This is required if running in KRaft mode.
When communicating with the controller quorum,
the broker will always use the first listener in this list.<br> Note: The ZK-based controller should not set this configuration.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：controller.quorum.election.backoff.max.ms
属性描述：Maximum time in milliseconds before starting new elections.
This is used in the binary exponential backoff mechanism that helps prevent gridlocked elections</p>
数据类型：int
默认的值：1000 (1 second)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：controller.quorum.election.timeout.ms
属性描述：Maximum time in milliseconds to wait without being able to fetch from the leader before triggering a new election</p>
数据类型：int
默认的值：1000 (1 second)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：controller.quorum.fetch.timeout.ms
属性描述：Maximum time without a successful fetch from the current leader before becoming a candidate and triggering a election for voters; Maximum time without receiving fetch from a majority of the quorum before asking around to see if there's a new epoch for leader</p>
数据类型：int
默认的值：2000 (2 seconds)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：controller.quorum.voters
属性描述：Map of id/endpoint information for the set of voters in a comma-separated list of `{id}@{host}:{port}` entries.
For example: `1@localhost:9092,2@localhost:9093,3@localhost:9094`</p>
数据类型：list
默认的值：""
有效的值：non-empty list
重要等级：高
更新模式：只读
```

```
属性名称：delete.topic.enable
属性描述：Enables delete topic.
Delete topic through the admin tool will have no effect if this config is turned off</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：leader.imbalance.check.interval.seconds
属性描述：The frequency with which the partition rebalance check is triggered by the controller</p>
数据类型：long
默认的值：300
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：leader.imbalance.per.broker.percentage
属性描述：The ratio of leader imbalance allowed per broker.
The controller would trigger a leader balance if it goes above this value per broker.
The value is specified in percentage.</p>
数据类型：int
默认的值：10
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：listeners
属性描述：Listener List - Comma-separated list of URIs we will listen on and the listener names.
If the listener name is not a security protocol,
<code>listener.security.protocol.map</code> must also be set.<br> Listener names and port numbers must be unique.<br> Specify hostname as 0.0.0.0 to bind to all interfaces.<br> Leave hostname empty to bind to default interface.<br> Examples of legal listener lists:<br> PLAINTEXT://myhost:9092,SSL://:9091<br> CLIENT://0.0.0.0:9092,REPLICATION://localhost:9093<br></p>
数据类型：string
默认的值：PLAINTEXT://:9092
有效的值：
重要等级：高
更新模式：每个代理
```

```
属性名称：log.dir
属性描述：The directory in which the log data is kept (supplemental for log.dirs property)</p>
数据类型：string
默认的值：/tmp/kafka-logs
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：log.dirs
属性描述：The directories in which the log data is kept.
If not set,
the value in log.dir is used</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：log.flush.interval.messages
属性描述：The number of messages accumulated on a log partition before messages are flushed to disk </p>
数据类型：long
默认的值：9223372036854775807
有效的值：[1,...]
重要等级：高
更新模式：cluster-wide
```

```
属性名称：log.flush.interval.ms
属性描述：The maximum time in ms that a message in any topic is kept in memory before flushed to disk.
If not set,
the value in log.flush.scheduler.interval.ms is used</p>
数据类型：long
默认的值：null
有效的值：
重要等级：高
更新模式：cluster-wide
```

```
属性名称：log.flush.offset.checkpoint.interval.ms
属性描述：The frequency with which we update the persistent record of the last flush which acts as the log recovery point</p>
数据类型：int
默认的值：60000 (1 minute)
有效的值：[0,...]
重要等级：高
更新模式：只读
```

```
属性名称：log.flush.scheduler.interval.ms
属性描述：The frequency in ms that the log flusher checks whether any log needs to be flushed to disk</p>
数据类型：long
默认的值：9223372036854775807
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：log.flush.start.offset.checkpoint.interval.ms
属性描述：The frequency with which we update the persistent record of log start offset</p>
数据类型：int
默认的值：60000 (1 minute)
有效的值：[0,...]
重要等级：高
更新模式：只读
```

```
属性名称：log.retention.bytes
属性描述：The maximum size of the log before deleting it</p>
数据类型：long
默认的值：-1
有效的值：
重要等级：高
更新模式：cluster-wide
```

```
属性名称：log.retention.hours
属性描述：The number of hours to keep a log file before deleting it (in hours),
tertiary to log.retention.ms property</p>
数据类型：int
默认的值：168
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：log.retention.minutes
属性描述：The number of minutes to keep a log file before deleting it (in minutes),
secondary to log.retention.ms property.
If not set,
the value in log.retention.hours is used</p>
数据类型：int
默认的值：null
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：log.retention.ms
属性描述：The number of milliseconds to keep a log file before deleting it (in milliseconds),
If not set,
the value in log.retention.minutes is used.
If set to -1,
no time limit is applied.</p>
数据类型：long
默认的值：null
有效的值：
重要等级：高
更新模式：cluster-wide
```

```
属性名称：log.roll.hours
属性描述：The maximum time before a new log segment is rolled out (in hours),
secondary to log.roll.ms property</p>
数据类型：int
默认的值：168
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：log.roll.jitter.hours
属性描述：The maximum jitter to subtract from logRollTimeMillis (in hours),
secondary to log.roll.jitter.ms property</p>
数据类型：int
默认的值：0
有效的值：[0,...]
重要等级：高
更新模式：只读
```

```
属性名称：log.roll.jitter.ms
属性描述：The maximum jitter to subtract from logRollTimeMillis (in milliseconds).
If not set,
the value in log.roll.jitter.hours is used</p>
数据类型：long
默认的值：null
有效的值：
重要等级：高
更新模式：cluster-wide
```

```
属性名称：log.roll.ms
属性描述：The maximum time before a new log segment is rolled out (in milliseconds).
If not set,
the value in log.roll.hours is used</p>
数据类型：long
默认的值：null
有效的值：
重要等级：高
更新模式：cluster-wide
```

```
属性名称：log.segment.bytes
属性描述：The maximum size of a single log file</p>
数据类型：int
默认的值：1073741824 (1 gibibyte)
有效的值：[14,...]
重要等级：高
更新模式：cluster-wide
```

```
属性名称：log.segment.delete.delay.ms
属性描述：The amount of time to wait before deleting a file from the filesystem</p>
数据类型：long
默认的值：60000 (1 minute)
有效的值：[0,...]
重要等级：高
更新模式：cluster-wide
```

```
属性名称：message.max.bytes
属性描述：The largest record batch size allowed by Kafka (after compression if compression is enabled).
If this is increased and there are consumers older than 0.10.2,
the consumers' fetch size must also be increased so that they can fetch record batches this large.
In the latest message format version,
records are always grouped into batches for efficiency.
In previous message format versions,
uncompressed records are not grouped into batches and this limit only applies to a single record in that case.This can be set per topic with the topic level <code>max.message.bytes</code> config.</p>
数据类型：int
默认的值：1048588
有效的值：[0,...]
重要等级：高
更新模式：cluster-wide
```

```
属性名称：metadata.log.dir
属性描述：This configuration determines where we put the metadata log for clusters in KRaft mode.
If it is not set,
the metadata log is placed in the first log directory from log.dirs.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：metadata.log.max.record.bytes.between.snapshots
属性描述：This is the maximum number of bytes in the log between the latest snapshot and the high-watermark needed before generating a new snapshot.</p>
数据类型：long
默认的值：20971520
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：metadata.log.segment.bytes
属性描述：The maximum size of a single metadata log file.</p>
数据类型：int
默认的值：1073741824 (1 gibibyte)
有效的值：[12,...]
重要等级：高
更新模式：只读
```

```
属性名称：metadata.log.segment.ms
属性描述：The maximum time before a new metadata log file is rolled out (in milliseconds).</p>
数据类型：long
默认的值：604800000 (7 days)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：metadata.max.retention.bytes
属性描述：The maximum combined size of the metadata log and snapshots before deleting old snapshots and log files.
Since at least one snapshot must exist before any logs can be deleted,
this is a soft limit.</p>
数据类型：long
默认的值：-1
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：metadata.max.retention.ms
属性描述：The number of milliseconds to keep a metadata log file or snapshot before deleting it.
Since at least one snapshot must exist before any logs can be deleted,
this is a soft limit.</p>
数据类型：long
默认的值：604800000 (7 days)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：min.insync.replicas
属性描述：When a producer sets acks to "all" (or "-1"),
min.insync.replicas specifies the minimum number of replicas that must acknowledge a write for the write to be considered successful.
If this minimum cannot be met,
then the producer will raise an exception (either NotEnoughReplicas or NotEnoughReplicasAfterAppend).<br>When used together,
min.insync.replicas and acks allow you to enforce greater durability guarantees.
A typical scenario would be to create a topic with a replication factor of 3,
set min.insync.replicas to 2,
and produce with acks of "all".
This will ensure that the producer raises an exception if a majority of replicas do not receive a write.</p>
数据类型：int
默认的值：1
有效的值：[1,...]
重要等级：高
更新模式：cluster-wide
```

```
属性名称：node.id
属性描述：The node ID associated with the roles this process is playing when `process.roles` is non-empty.
This is required configuration when running in KRaft mode.</p>
数据类型：int
默认的值：-1
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：num.io.threads
属性描述：The number of threads that the server uses for processing requests,
which may include disk I/O</p>
数据类型：int
默认的值：8
有效的值：[1,...]
重要等级：高
更新模式：cluster-wide
```

```
属性名称：num.network.threads
属性描述：The number of threads that the server uses for receiving requests from the network and sending responses to the network</p>
数据类型：int
默认的值：3
有效的值：[1,...]
重要等级：高
更新模式：cluster-wide
```

```
属性名称：num.recovery.threads.per.data.dir
属性描述：The number of threads per data directory to be used for log recovery at startup and flushing at shutdown</p>
数据类型：int
默认的值：1
有效的值：[1,...]
重要等级：高
更新模式：cluster-wide
```

```
属性名称：num.replica.alter.log.dirs.threads
属性描述：The number of threads that can move replicas between log directories,
which may include disk I/O</p>
数据类型：int
默认的值：null
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：num.replica.fetchers
属性描述：Number of fetcher threads used to replicate messages from a source broker.
Increasing this value can increase the degree of I/O parallelism in the follower broker.</p>
数据类型：int
默认的值：1
有效的值：
重要等级：高
更新模式：cluster-wide
```

```
属性名称：offset.metadata.max.bytes
属性描述：The maximum size for a metadata entry associated with an offset commit</p>
数据类型：int
默认的值：4096 (4 kibibytes)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：offsets.commit.required.acks
属性描述：The required acks before the commit can be accepted.
In general,
the default (-1) should not be overridden</p>
数据类型：short
默认的值：-1
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：offsets.commit.timeout.ms
属性描述：Offset commit will be delayed until all replicas for the offsets topic receive the commit or this timeout is reached.
This is similar to the producer request timeout.</p>
数据类型：int
默认的值：5000 (5 seconds)
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：offsets.load.buffer.size
属性描述：Batch size for reading from the offsets segments when loading offsets into the cache (soft-limit,
overridden if records are too large).</p>
数据类型：int
默认的值：5242880
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：offsets.retention.check.interval.ms
属性描述：Frequency at which to check for stale offsets</p>
数据类型：long
默认的值：600000 (10 minutes)
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：offsets.retention.minutes
属性描述：After a consumer group loses all its consumers (i.e.
becomes empty) its offsets will be kept for this retention period before getting discarded.
For standalone consumers (using manual assignment),
offsets will be expired after the time of last commit plus this retention period.</p>
数据类型：int
默认的值：10080
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：offsets.topic.compression.codec
属性描述：Compression codec for the offsets topic - compression may be used to achieve "atomic" commits</p>
数据类型：int
默认的值：0
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：offsets.topic.num.partitions
属性描述：The number of partitions for the offset commit topic (should not change after deployment)</p>
数据类型：int
默认的值：50
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：offsets.topic.replication.factor
属性描述：The replication factor for the offsets topic (set higher to ensure availability).
Internal topic creation will fail until the cluster size meets this replication factor requirement.</p>
数据类型：short
默认的值：3
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：offsets.topic.segment.bytes
属性描述：The offsets topic segment bytes should be kept relatively small in order to facilitate faster log compaction and cache loads</p>
数据类型：int
默认的值：104857600 (100 mebibytes)
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：process.roles
属性描述：The roles that this process plays: 'broker',
'controller',
or 'broker,controller' if it is both.
This configuration is only applicable for clusters in KRaft (Kafka Raft) mode (instead of ZooKeeper).
Leave this config undefined or empty for Zookeeper clusters.</p>
数据类型：list
默认的值：""
有效的值：[broker,
controller]
重要等级：高
更新模式：只读
```

```
属性名称：queued.max.requests
属性描述：The number of queued requests allowed for data-plane,
before blocking the network threads</p>
数据类型：int
默认的值：500
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：replica.fetch.min.bytes
属性描述：Minimum bytes expected for each fetch response.
If not enough bytes,
wait up to <code>replica.fetch.wait.max.ms</code> (broker config).</p>
数据类型：int
默认的值：1
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：replica.fetch.wait.max.ms
属性描述：The maximum wait time for each fetcher request issued by follower replicas.
This value should always be less than the replica.lag.time.max.ms at all times to prevent frequent shrinking of ISR for low throughput topics</p>
数据类型：int
默认的值：500
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：replica.high.watermark.checkpoint.interval.ms
属性描述：The frequency with which the high watermark is saved out to disk</p>
数据类型：long
默认的值：5000 (5 seconds)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：replica.lag.time.max.ms
属性描述：If a follower hasn't sent any fetch requests or hasn't consumed up to the leaders log end offset for at least this time,
the leader will remove the follower from isr</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：replica.socket.receive.buffer.bytes
属性描述：The socket receive buffer for network requests</p>
数据类型：int
默认的值：65536 (64 kibibytes)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：replica.socket.timeout.ms
属性描述：The socket timeout for network requests.
Its value should be at least replica.fetch.wait.max.ms</p>
数据类型：int
默认的值：30000 (30 seconds)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：request.timeout.ms
属性描述：The configuration controls the maximum amount of time the client will wait for the response of a request.
If the response is not received before the timeout elapses the client will resend the request if necessary or fail the request if retries are exhausted.</p>
数据类型：int
默认的值：30000 (30 seconds)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：sasl.mechanism.controller.protocol
属性描述：SASL mechanism used for communication with controllers.
Default is GSSAPI.</p>
数据类型：string
默认的值：GSSAPI
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：socket.receive.buffer.bytes
属性描述：The SO_RCVBUF buffer of the socket server sockets.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：102400 (100 kibibytes)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：socket.request.max.bytes
属性描述：The maximum number of bytes in a socket request</p>
数据类型：int
默认的值：104857600 (100 mebibytes)
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：socket.send.buffer.bytes
属性描述：The SO_SNDBUF buffer of the socket server sockets.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：102400 (100 kibibytes)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：transaction.max.timeout.ms
属性描述：The maximum allowed timeout for transactions.
If a client’s requested transaction time exceed this,
then the broker will return an error in InitProducerIdRequest.
This prevents a client from too large of a timeout,
which can stall consumers reading from topics included in the transaction.</p>
数据类型：int
默认的值：900000 (15 minutes)
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：transaction.state.log.load.buffer.size
属性描述：Batch size for reading from the transaction log segments when loading producer ids and transactions into the cache (soft-limit,
overridden if records are too large).</p>
数据类型：int
默认的值：5242880
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：transaction.state.log.min.isr
属性描述：Overridden min.insync.replicas config for the transaction topic.</p>
数据类型：int
默认的值：2
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：transaction.state.log.num.partitions
属性描述：The number of partitions for the transaction topic (should not change after deployment).</p>
数据类型：int
默认的值：50
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：transaction.state.log.replication.factor
属性描述：The replication factor for the transaction topic (set higher to ensure availability).
Internal topic creation will fail until the cluster size meets this replication factor requirement.</p>
数据类型：short
默认的值：3
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：transaction.state.log.segment.bytes
属性描述：The transaction topic segment bytes should be kept relatively small in order to facilitate faster log compaction and cache loads</p>
数据类型：int
默认的值：104857600 (100 mebibytes)
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：transactional.id.expiration.ms
属性描述：The time in ms that the transaction coordinator will wait without receiving any transaction status updates for the current transaction before expiring its transactional id.
This setting also influences producer id expiration - producer ids are expired once this time has elapsed after the last write with the given producer id.
Note that producer ids may expire sooner if the last write from the producer id is deleted due to the topic's retention settings.</p>
数据类型：int
默认的值：604800000 (7 days)
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：unclean.leader.election.enable
属性描述：Indicates whether to enable replicas not in the ISR set to be elected as leader as a last resort,
even though doing so may result in data loss</p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：高
更新模式：cluster-wide
```

```
属性名称：zookeeper.connect
属性描述：Specifies the ZooKeeper connection string in the form <code>hostname:port</code> where host and port are the host and port of a ZooKeeper server.
To allow connecting through other ZooKeeper nodes when that ZooKeeper machine is down you can also specify multiple hosts in the form <code>hostname1:port1,hostname2:port2,hostname3:port3</code>.<br>The server can also have a ZooKeeper chroot path as part of its ZooKeeper connection string which puts its data under some path in the global ZooKeeper namespace.
For example to give a chroot path of <code>/chroot/path</code> you would give the connection string as <code>hostname1:port1,hostname2:port2,hostname3:port3/chroot/path</code>.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：zookeeper.connection.timeout.ms
属性描述：The max time that the client waits to establish a connection to zookeeper.
If not set,
the value in zookeeper.session.timeout.ms is used</p>
数据类型：int
默认的值：null
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：zookeeper.max.in.flight.requests
属性描述：The maximum number of unacknowledged requests the client will send to Zookeeper before blocking.</p>
数据类型：int
默认的值：10
有效的值：[1,...]
重要等级：高
更新模式：只读
```

```
属性名称：zookeeper.session.timeout.ms
属性描述：Zookeeper session timeout</p>
数据类型：int
默认的值：18000 (18 seconds)
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：zookeeper.set.acl
属性描述：Set client to use secure ACLs</p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：高
更新模式：只读
```

```
属性名称：broker.heartbeat.interval.ms
属性描述：The length of time in milliseconds between broker heartbeats.
Used when running in KRaft mode.</p>
数据类型：int
默认的值：2000 (2 seconds)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：broker.id.generation.enable
属性描述：Enable automatic broker id generation on the server.
When enabled the value configured for reserved.broker.max.id should be reviewed.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：broker.rack
属性描述：Rack of the broker.
This will be used in rack aware replication assignment for fault tolerance.
Examples: `RACK1`,
`us-east-1d`</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：broker.session.timeout.ms
属性描述：The length of time in milliseconds that a broker lease lasts if no heartbeats are made.
Used when running in KRaft mode.</p>
数据类型：int
默认的值：9000 (9 seconds)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：connections.max.idle.ms
属性描述：Idle connections timeout: the server socket processor threads close the connections that idle more than this</p>
数据类型：long
默认的值：600000 (10 minutes)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：connections.max.reauth.ms
属性描述：When explicitly set to a positive number (the default is 0,
not a positive number),
a session lifetime that will not exceed the configured value will be communicated to v2.2.0 or later clients when they authenticate.
The broker will disconnect any such connection that is not re-authenticated within the session lifetime and that is then subsequently used for any purpose other than re-authentication.
Configuration names can optionally be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.oauthbearer.connections.max.reauth.ms=3600000</p>
数据类型：long
默认的值：0
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：controlled.shutdown.enable
属性描述：Enable controlled shutdown of the server</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：controlled.shutdown.max.retries
属性描述：Controlled shutdown can fail for multiple reasons.
This determines the number of retries when such failure happens</p>
数据类型：int
默认的值：3
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：controlled.shutdown.retry.backoff.ms
属性描述：Before each retry,
the system needs time to recover from the state that caused the previous failure (Controller fail over,
replica lag etc).
This config determines the amount of time to wait before retrying.</p>
数据类型：long
默认的值：5000 (5 seconds)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：controller.quorum.append.linger.ms
属性描述：The duration in milliseconds that the leader will wait for writes to accumulate before flushing them to disk.</p>
数据类型：int
默认的值：25
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：controller.quorum.request.timeout.ms
属性描述：The configuration controls the maximum amount of time the client will wait for the response of a request.
If the response is not received before the timeout elapses the client will resend the request if necessary or fail the request if retries are exhausted.</p>
数据类型：int
默认的值：2000 (2 seconds)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：controller.socket.timeout.ms
属性描述：The socket timeout for controller-to-broker channels</p>
数据类型：int
默认的值：30000 (30 seconds)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：default.replication.factor
属性描述：The default replication factors for automatically created topics</p>
数据类型：int
默认的值：1
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：delegation.token.expiry.time.ms
属性描述：The token validity time in miliseconds before the token needs to be renewed.
Default value 1 day.</p>
数据类型：long
默认的值：86400000 (1 day)
有效的值：[1,...]
重要等级：medium
更新模式：只读
```

```
属性名称：delegation.token.master.key
属性描述：DEPRECATED: An alias for delegation.token.secret.key,
which should be used instead of this config.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：delegation.token.max.lifetime.ms
属性描述：The token has a maximum lifetime beyond which it cannot be renewed anymore.
Default value 7 days.</p>
数据类型：long
默认的值：604800000 (7 days)
有效的值：[1,...]
重要等级：medium
更新模式：只读
```

```
属性名称：delegation.token.secret.key
属性描述：Secret key to generate and verify delegation tokens.
The same key must be configured across all the brokers.
 If the key is not set or set to empty string,
brokers will disable the delegation token support.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：delete.records.purgatory.purge.interval.requests
属性描述：The purge interval (in number of requests) of the delete records request purgatory</p>
数据类型：int
默认的值：1
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：fetch.max.bytes
属性描述：The maximum number of bytes we will return for a fetch request.
Must be at least 1024.</p>
数据类型：int
默认的值：57671680 (55 mebibytes)
有效的值：[1024,...]
重要等级：medium
更新模式：只读
```

```
属性名称：fetch.purgatory.purge.interval.requests
属性描述：The purge interval (in number of requests) of the fetch request purgatory</p>
数据类型：int
默认的值：1000
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：group.initial.rebalance.delay.ms
属性描述：The amount of time the group coordinator will wait for more consumers to join a new group before performing the first rebalance.
A longer delay means potentially fewer rebalances,
but increases the time until processing begins.</p>
数据类型：int
默认的值：3000 (3 seconds)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：group.max.session.timeout.ms
属性描述：The maximum allowed session timeout for registered consumers.
Longer timeouts give consumers more time to process messages in between heartbeats at the cost of a longer time to detect failures.</p>
数据类型：int
默认的值：1800000 (30 minutes)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：group.max.size
属性描述：The maximum number of consumers that a single consumer group can accommodate.</p>
数据类型：int
默认的值：2147483647
有效的值：[1,...]
重要等级：medium
更新模式：只读
```

```
属性名称：group.min.session.timeout.ms
属性描述：The minimum allowed session timeout for registered consumers.
Shorter timeouts result in quicker failure detection at the cost of more frequent consumer heartbeating,
which can overwhelm broker resources.</p>
数据类型：int
默认的值：6000 (6 seconds)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：initial.broker.registration.timeout.ms
属性描述：When initially registering with the controller quorum,
the number of milliseconds to wait before declaring failure and exiting the broker process.</p>
数据类型：int
默认的值：60000 (1 minute)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：inter.broker.listener.name
属性描述：Name of listener used for communication between brokers.
If this is unset,
the listener name is defined by security.inter.broker.protocol.
It is an error to set this and security.inter.broker.protocol properties at the same time.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：inter.broker.protocol.version
属性描述：Specify which version of the inter-broker protocol will be used.<br> This is typically bumped after all brokers were upgraded to a new version.<br> Example of some valid values are: 0.8.0,
0.8.1,
0.8.1.1,
0.8.2,
0.8.2.0,
0.8.2.1,
0.9.0.0,
0.9.0.1 Check ApiVersion for the full list.</p>
数据类型：string
默认的值：3.2-IV0
有效的值：[0.8.0,
0.8.1,
0.8.2,
0.9.0,
0.10.0-IV0,
0.10.0-IV1,
0.10.1-IV0,
0.10.1-IV1,
0.10.1-IV2,
0.10.2-IV0,
0.11.0-IV0,
0.11.0-IV1,
0.11.0-IV2,
1.0-IV0,
1.1-IV0,
2.0-IV0,
2.0-IV1,
2.1-IV0,
2.1-IV1,
2.1-IV2,
2.2-IV0,
2.2-IV1,
2.3-IV0,
2.3-IV1,
2.4-IV0,
2.4-IV1,
2.5-IV0,
2.6-IV0,
2.7-IV0,
2.7-IV1,
2.7-IV2,
2.8-IV0,
2.8-IV1,
3.0-IV0,
3.0-IV1,
3.1-IV0,
3.2-IV0]
重要等级：medium
更新模式：只读
```

```
属性名称：log.cleaner.backoff.ms
属性描述：The amount of time to sleep when there are no logs to clean</p>
数据类型：long
默认的值：15000 (15 seconds)
有效的值：[0,...]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleaner.dedupe.buffer.size
属性描述：The total memory used for log deduplication across all cleaner threads</p>
数据类型：long
默认的值：134217728
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleaner.delete.retention.ms
属性描述：The amount of time to retain delete tombstone markers for log compacted topics.
This setting also gives a bound on the time in which a consumer must complete a read if they begin from offset 0 to ensure that they get a valid snapshot of the final stage (otherwise delete tombstones may be collected before they complete their scan).</p>
数据类型：long
默认的值：86400000 (1 day)
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleaner.enable
属性描述：Enable the log cleaner process to run on the server.
Should be enabled if using any topics with a cleanup.policy=compact including the internal offsets topic.
If disabled those topics will not be compacted and continually grow in size.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：log.cleaner.io.buffer.load.factor
属性描述：Log cleaner dedupe buffer load factor.
The percentage full the dedupe buffer can become.
A higher value will allow more log to be cleaned at once but will lead to more hash collisions</p>
数据类型：double
默认的值：0.9
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleaner.io.buffer.size
属性描述：The total memory used for log cleaner I/O buffers across all cleaner threads</p>
数据类型：int
默认的值：524288
有效的值：[0,...]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleaner.io.max.bytes.per.second
属性描述：The log cleaner will be throttled so that the sum of its read and write i/o will be less than this value on average</p>
数据类型：double
默认的值：1.7976931348623157E308
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleaner.max.compaction.lag.ms
属性描述：The maximum time a message will remain ineligible for compaction in the log.
Only applicable for logs that are being compacted.</p>
数据类型：long
默认的值：9223372036854775807
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleaner.min.cleanable.ratio
属性描述：The minimum ratio of dirty log to total log for a log to eligible for cleaning.
If the log.cleaner.max.compaction.lag.ms or the log.cleaner.min.compaction.lag.ms configurations are also specified,
then the log compactor considers the log eligible for compaction as soon as either: (i) the dirty ratio threshold has been met and the log has had dirty (uncompacted) records for at least the log.cleaner.min.compaction.lag.ms duration,
or (ii) if the log has had dirty (uncompacted) records for at most the log.cleaner.max.compaction.lag.ms period.</p>
数据类型：double
默认的值：0.5
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleaner.min.compaction.lag.ms
属性描述：The minimum time a message will remain uncompacted in the log.
Only applicable for logs that are being compacted.</p>
数据类型：long
默认的值：0
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleaner.threads
属性描述：The number of background threads to use for log cleaning</p>
数据类型：int
默认的值：1
有效的值：[0,...]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.cleanup.policy
属性描述：The default cleanup policy for segments beyond the retention window.
A comma separated list of valid policies.
Valid policies are: "delete" and "compact"</p>
数据类型：list
默认的值：delete
有效的值：[compact,
delete]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.index.interval.bytes
属性描述：The interval with which we add an entry to the offset index</p>
数据类型：int
默认的值：4096 (4 kibibytes)
有效的值：[0,...]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.index.size.max.bytes
属性描述：The maximum size in bytes of the offset index</p>
数据类型：int
默认的值：10485760 (10 mebibytes)
有效的值：[4,...]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.message.format.version
属性描述：Specify the message format version the broker will use to append messages to the logs.
The value should be a valid ApiVersion.
Some examples are: 0.8.2,
0.9.0.0,
0.10.0,
check ApiVersion for more details.
By setting a particular message format version,
the user is certifying that all the existing messages on disk are smaller or equal than the specified version.
Setting this value incorrectly will cause consumers with older versions to break as they will receive messages with a format that they don't understand.</p>
数据类型：string
默认的值：3.0-IV1
有效的值：[0.8.0,
0.8.1,
0.8.2,
0.9.0,
0.10.0-IV0,
0.10.0-IV1,
0.10.1-IV0,
0.10.1-IV1,
0.10.1-IV2,
0.10.2-IV0,
0.11.0-IV0,
0.11.0-IV1,
0.11.0-IV2,
1.0-IV0,
1.1-IV0,
2.0-IV0,
2.0-IV1,
2.1-IV0,
2.1-IV1,
2.1-IV2,
2.2-IV0,
2.2-IV1,
2.3-IV0,
2.3-IV1,
2.4-IV0,
2.4-IV1,
2.5-IV0,
2.6-IV0,
2.7-IV0,
2.7-IV1,
2.7-IV2,
2.8-IV0,
2.8-IV1,
3.0-IV0,
3.0-IV1,
3.1-IV0,
3.2-IV0]
重要等级：medium
更新模式：只读
```

```
属性名称：log.message.timestamp.difference.max.ms
属性描述：The maximum difference allowed between the timestamp when a broker receives a message and the timestamp specified in the message.
If log.message.timestamp.type=CreateTime,
a message will be rejected if the difference in timestamp exceeds this threshold.
This configuration is ignored if log.message.timestamp.type=LogAppendTime.The maximum timestamp difference allowed should be no greater than log.retention.ms to avoid unnecessarily frequent log rolling.</p>
数据类型：long
默认的值：9223372036854775807
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.message.timestamp.type
属性描述：Define whether the timestamp in the message is message create time or log append time.
The value should be either `CreateTime` or `LogAppendTime`</p>
数据类型：string
默认的值：CreateTime
有效的值：[CreateTime,
LogAppendTime]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.preallocate
属性描述：Should pre allocate file when create new segment? If you are using Kafka on Windows,
you probably need to set it to true.</p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：log.retention.check.interval.ms
属性描述：The frequency in milliseconds that the log cleaner checks whether any log is eligible for deletion</p>
数据类型：long
默认的值：300000 (5 minutes)
有效的值：[1,...]
重要等级：medium
更新模式：只读
```

```
属性名称：max.connection.creation.rate
属性描述：The maximum connection creation rate we allow in the broker at any time.
Listener-level limits may also be configured by prefixing the config name with the listener prefix,
for example,
<code>listener.name.internal.max.connection.creation.rate</code>.Broker-wide connection rate limit should be configured based on broker capacity while listener limits should be configured based on application requirements.
New connections will be throttled if either the listener or the broker limit is reached,
with the exception of inter-broker listener.
Connections on the inter-broker listener will be throttled only when the listener-level rate limit is reached.</p>
数据类型：int
默认的值：2147483647
有效的值：[0,...]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：max.connections
属性描述：The maximum number of connections we allow in the broker at any time.
This limit is applied in addition to any per-ip limits configured using max.connections.per.ip.
Listener-level limits may also be configured by prefixing the config name with the listener prefix,
for example,
<code>listener.name.internal.max.connections</code>.
Broker-wide limit should be configured based on broker capacity while listener limits should be configured based on application requirements.
New connections are blocked if either the listener or broker limit is reached.
Connections on the inter-broker listener are permitted even if broker-wide limit is reached.
The least recently used connection on another listener will be closed in this case.</p>
数据类型：int
默认的值：2147483647
有效的值：[0,...]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：max.connections.per.ip
属性描述：The maximum number of connections we allow from each ip address.
This can be set to 0 if there are overrides configured using max.connections.per.ip.overrides property.
New connections from the ip address are dropped if the limit is reached.</p>
数据类型：int
默认的值：2147483647
有效的值：[0,...]
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：max.connections.per.ip.overrides
属性描述：A comma-separated list of per-ip or hostname overrides to the default maximum number of connections.
An example value is "hostName:100,127.0.0.1:200"</p>
数据类型：string
默认的值：""
有效的值：
重要等级：medium
更新模式：cluster-wide
```

```
属性名称：max.incremental.fetch.session.cache.slots
属性描述：The maximum number of incremental fetch sessions that we will maintain.</p>
数据类型：int
默认的值：1000
有效的值：[0,...]
重要等级：medium
更新模式：只读
```

```
属性名称：num.partitions
属性描述：The default number of log partitions per topic</p>
数据类型：int
默认的值：1
有效的值：[1,...]
重要等级：medium
更新模式：只读
```

```
属性名称：password.encoder.old.secret
属性描述：The old secret that was used for encoding dynamically configured passwords.
This is required only when the secret is updated.
If specified,
all dynamically encoded passwords are decoded using this old secret and re-encoded using password.encoder.secret when broker starts up.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：password.encoder.secret
属性描述：The secret used for encoding dynamically configured passwords for this broker.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：principal.builder.class
属性描述：The fully qualified name of a class that implements the KafkaPrincipalBuilder interface,
which is used to build the KafkaPrincipal object used during authorization.
If no principal builder is defined,
the default behavior depends on the security protocol in use.
For SSL authentication,
 the principal will be derived using the rules defined by <code>ssl.principal.mapping.rules</code> applied on the distinguished name from the client certificate if one is provided; otherwise,
if client authentication is not required,
the principal name will be ANONYMOUS.
For SASL authentication,
the principal will be derived using the rules defined by <code>sasl.kerberos.principal.to.local.rules</code> if GSSAPI is in use,
and the SASL authentication ID for other mechanisms.
For PLAINTEXT,
the principal will be ANONYMOUS.</p>
数据类型：class
默认的值：org.apache.kafka.common.security.authenticator.DefaultKafkaPrincipalBuilder
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：producer.purgatory.purge.interval.requests
属性描述：The purge interval (in number of requests) of the producer request purgatory</p>
数据类型：int
默认的值：1000
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：queued.max.request.bytes
属性描述：The number of queued bytes allowed before no more requests are read</p>
数据类型：long
默认的值：-1
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：replica.fetch.backoff.ms
属性描述：The amount of time to sleep when fetch partition error occurs.</p>
数据类型：int
默认的值：1000 (1 second)
有效的值：[0,...]
重要等级：medium
更新模式：只读
```

```
属性名称：replica.fetch.max.bytes
属性描述：The number of bytes of messages to attempt to fetch for each partition.
This is not an absolute maximum,
if the first record batch in the first non-empty partition of the fetch is larger than this value,
the record batch will still be returned to ensure that progress can be made.
The maximum record batch size accepted by the broker is defined via <code>message.max.bytes</code> (broker config) or <code>max.message.bytes</code> (topic config).</p>
数据类型：int
默认的值：1048576 (1 mebibyte)
有效的值：[0,...]
重要等级：medium
更新模式：只读
```

```
属性名称：replica.fetch.response.max.bytes
属性描述：Maximum bytes expected for the entire fetch response.
Records are fetched in batches,
and if the first record batch in the first non-empty partition of the fetch is larger than this value,
the record batch will still be returned to ensure that progress can be made.
As such,
this is not an absolute maximum.
The maximum record batch size accepted by the broker is defined via <code>message.max.bytes</code> (broker config) or <code>max.message.bytes</code> (topic config).</p>
数据类型：int
默认的值：10485760 (10 mebibytes)
有效的值：[0,...]
重要等级：medium
更新模式：只读
```

```
属性名称：replica.selector.class
属性描述：The fully qualified class name that implements ReplicaSelector.
This is used by the broker to find the preferred read replica.
By default,
we use an implementation that returns the leader.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：reserved.broker.max.id
属性描述：Max number that can be used for a broker.id</p>
数据类型：int
默认的值：1000
有效的值：[0,...]
重要等级：medium
更新模式：只读
```

```
属性名称：sasl.client.callback.handler.class
属性描述：The fully qualified name of a SASL client callback handler class that implements the AuthenticateCallbackHandler interface.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：sasl.enabled.mechanisms
属性描述：The list of SASL mechanisms enabled in the Kafka server.
The list may contain any mechanism for which a security provider is available.
Only GSSAPI is enabled by default.</p>
数据类型：list
默认的值：GSSAPI
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.jaas.config
属性描述：JAAS login context parameters for SASL connections in the format used by JAAS configuration files.
JAAS configuration file format is described <a href="http://docs.oracle.com/javase/8/docs/technotes/guides/security/jgss/tutorials/LoginConfigFile.html">here</a>.
The format for the value is: <code>loginModuleClass controlFlag (optionName=optionValue)*;</code>.
For brokers,
the config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.jaas.config=com.example.ScramLoginModule required;</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.kerberos.kinit.cmd
属性描述：Kerberos kinit command path.</p>
数据类型：string
默认的值：/usr/bin/kinit
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.kerberos.min.time.before.relogin
属性描述：Login thread sleep time between refresh attempts.</p>
数据类型：long
默认的值：60000
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.kerberos.principal.to.local.rules
属性描述：A list of rules for mapping from principal names to short names (typically operating system usernames).
The rules are evaluated in order and the first rule that matches a principal name is used to map it to a short name.
Any later rules in the list are ignored.
By default,
principal names of the form {username}/{hostname}@{REALM} are mapped to {username}.
For more details on the format please see <a href="#security_authz"> security authorization and acls</a>.
Note that this configuration is ignored if an extension of KafkaPrincipalBuilder is provided by the <code>principal.builder.class</code> configuration.</p>
数据类型：list
默认的值：DEFAULT
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.kerberos.service.name
属性描述：The Kerberos principal name that Kafka runs as.
This can be defined either in Kafka's JAAS config or in Kafka's config.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.kerberos.ticket.renew.jitter
属性描述：Percentage of random jitter added to the renewal time.</p>
数据类型：double
默认的值：0.05
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.kerberos.ticket.renew.window.factor
属性描述：Login thread will sleep until the specified window factor of time from last refresh to ticket's expiry has been reached,
at which time it will try to renew the ticket.</p>
数据类型：double
默认的值：0.8
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.login.callback.handler.class
属性描述：The fully qualified name of a SASL login callback handler class that implements the AuthenticateCallbackHandler interface.
For brokers,
login callback handler config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.callback.handler.class=com.example.CustomScramLoginCallbackHandler</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：sasl.login.class
属性描述：The fully qualified name of a class that implements the Login interface.
For brokers,
login config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.class=com.example.CustomScramLogin</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：sasl.login.refresh.buffer.seconds
属性描述：The amount of buffer time before credential expiration to maintain when refreshing a credential,
in seconds.
If a refresh would otherwise occur closer to expiration than the number of buffer seconds then the refresh will be moved up to maintain as much of the buffer time as possible.
Legal values are between 0 and 3600 (1 hour); a default value of  300 (5 minutes) is used if no value is specified.
This value and sasl.login.refresh.min.period.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：300
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.login.refresh.min.period.seconds
属性描述：The desired minimum time for the login refresh thread to wait before refreshing a credential,
in seconds.
Legal values are between 0 and 900 (15 minutes); a default value of 60 (1 minute) is used if no value is specified.
 This value and  sasl.login.refresh.buffer.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：60
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.login.refresh.window.factor
属性描述：Login refresh thread will sleep until the specified window factor relative to the credential's lifetime has been reached,
at which time it will try to refresh the credential.
Legal values are between 0.5 (50%) and 1.0 (100%) inclusive; a default value of 0.8 (80%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.8
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.login.refresh.window.jitter
属性描述：The maximum amount of random jitter relative to the credential's lifetime that is added to the login refresh thread's sleep time.
Legal values are between 0 and 0.25 (25%) inclusive; a default value of 0.05 (5%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.05
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.mechanism.inter.broker.protocol
属性描述：SASL mechanism used for inter-broker communication.
Default is GSSAPI.</p>
数据类型：string
默认的值：GSSAPI
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.url
属性描述：The OAuth/OIDC provider URL from which the provider's <a href="https://datatracker.ietf.org/doc/html/rfc7517#section-5">JWKS (JSON Web Key Set)</a> can be retrieved.
The URL can be HTTP(S)-based or file-based.
If the URL is HTTP(S)-based,
the JWKS data will be retrieved from the OAuth/OIDC provider via the configured URL on broker startup.
All then-current keys will be cached on the broker for incoming requests.
If an authentication request is received for a JWT that includes a "kid" header claim value that isn't yet in the cache,
the JWKS endpoint will be queried again on demand.
However,
the broker polls the URL every sasl.oauthbearer.jwks.endpoint.refresh.ms milliseconds to refresh the cache with any forthcoming keys before any JWT requests that include them are received.
If the URL is file-based,
the broker will load the JWKS file from a configured location on startup.
In the event that the JWT includes a "kid" header value that isn't in the JWKS file,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：sasl.oauthbearer.token.endpoint.url
属性描述：The URL for the OAuth/OIDC identity provider.
If the URL is HTTP(S)-based,
it is the issuer's token endpoint URL to which requests will be made to login based on the configuration in sasl.jaas.config.
If the URL is file-based,
it specifies a file containing an access token (in JWT serialized form) issued by the OAuth/OIDC identity provider to use for authorization.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：sasl.server.callback.handler.class
属性描述：The fully qualified name of a SASL server callback handler class that implements the AuthenticateCallbackHandler interface.
Server callback handlers must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.plain.sasl.server.callback.handler.class=com.example.CustomPlainCallbackHandler.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：security.inter.broker.protocol
属性描述：Security protocol used to communicate between brokers.
Valid values are: PLAINTEXT,
SSL,
SASL_PLAINTEXT,
SASL_SSL.
It is an error to set this and inter.broker.listener.name properties at the same time.</p>
数据类型：string
默认的值：PLAINTEXT
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：socket.connection.setup.timeout.max.ms
属性描述：The maximum amount of time the client will wait for the socket connection to be established.
The connection setup timeout will increase exponentially for each consecutive connection failure up to this maximum.
To avoid connection storms,
a randomization factor of 0.2 will be applied to the timeout resulting in a random range between 20% below and 20% above the computed value.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：socket.connection.setup.timeout.ms
属性描述：The amount of time the client will wait for the socket connection to be established.
If the connection is not built before the timeout elapses,
clients will close the socket channel.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：socket.listen.backlog.size
属性描述：The maximum number of pending connections on the socket.
In Linux,
you may also need to configure `somaxconn` and `tcp_max_syn_backlog` kernel parameters accordingly to make the configuration takes effect.</p>
数据类型：int
默认的值：50
有效的值：[1,...]
重要等级：medium
更新模式：只读
```

```
属性名称：ssl.cipher.suites
属性描述：A list of cipher suites.
This is a named combination of authentication,
encryption,
MAC and key exchange algorithm used to negotiate the security settings for a network connection using TLS or SSL network protocol.
By default all the available cipher suites are supported.</p>
数据类型：list
默认的值：""
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.client.auth
属性描述：Configures kafka broker to request client authentication.
The following settings are common:  </p><ul> <li><code>ssl.client.auth=required</code> If set to required client authentication is required.
</li><li><code>ssl.client.auth=requested</code> This means client authentication is optional.
unlike required,
if this option is set client can choose not to provide authentication information about itself </li><li><code>ssl.client.auth=none</code> This means client authentication is not needed.</li></ul><p></p>
数据类型：string
默认的值：none
有效的值：[required,
requested,
none]
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.enabled.protocols
属性描述：The list of protocols enabled for SSL connections.
The default is 'TLSv1.2,TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
With the default value for Java 11,
clients and servers will prefer TLSv1.3 if both support it and fallback to TLSv1.2 otherwise (assuming both support at least TLSv1.2).
This default should be fine for most cases.
Also see the config documentation for `ssl.protocol`.</p>
数据类型：list
默认的值：TLSv1.2
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.key.password
属性描述：The password of the private key in the key store file or the PEM key specified in `ssl.keystore.key'.
This is required for clients only if two-way authentication is configured.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.keymanager.algorithm
属性描述：The algorithm used by key manager factory for SSL connections.
Default value is the key manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：SunX509
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.keystore.certificate.chain
属性描述：Certificate chain in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with a list of X.509 certificates</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.keystore.key
属性描述：Private key in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with PKCS#8 keys.
If the key is encrypted,
key password must be specified using 'ssl.key.password'</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.keystore.location
属性描述：The location of the key store file.
This is optional for client and can be used for two-way authentication for client.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.keystore.password
属性描述：The store password for the key store file.
This is optional for client and only needed if 'ssl.keystore.location' is configured.
Key store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.keystore.type
属性描述：The file format of the key store file.
This is optional for client.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.protocol
属性描述：The SSL protocol used to generate the SSLContext.
The default is 'TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
This value should be fine for most use cases.
Allowed values in recent JVMs are 'TLSv1.2' and 'TLSv1.3'.
'TLS',
'TLSv1.1',
'SSL',
'SSLv2' and 'SSLv3' may be supported in older JVMs,
but their usage is discouraged due to known security vulnerabilities.
With the default value for this config and 'ssl.enabled.protocols',
clients will downgrade to 'TLSv1.2' if the server does not support 'TLSv1.3'.
If this config is set to 'TLSv1.2',
clients will not use 'TLSv1.3' even if it is one of the values in ssl.enabled.protocols and the server only supports 'TLSv1.3'.</p>
数据类型：string
默认的值：TLSv1.2
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.provider
属性描述：The name of the security provider used for SSL connections.
Default value is the default security provider of the JVM.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.trustmanager.algorithm
属性描述：The algorithm used by trust manager factory for SSL connections.
Default value is the trust manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：PKIX
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.truststore.certificates
属性描述：Trusted certificates in the format specified by 'ssl.truststore.type'.
Default SSL engine factory supports only PEM format with X.509 certificates.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.truststore.location
属性描述：The location of the trust store file.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.truststore.password
属性描述：The password for the trust store file.
If a password is not set,
trust store file configured will still be used,
but integrity checking is disabled.
Trust store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：ssl.truststore.type
属性描述：The file format of the trust store file.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
更新模式：每个代理
```

```
属性名称：zookeeper.clientCnxnSocket
属性描述：Typically set to <code>org.apache.zookeeper.ClientCnxnSocketNetty</code> when using TLS connectivity to ZooKeeper.
Overrides any explicit value set via the same-named <code>zookeeper.clientCnxnSocket</code> system property.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：zookeeper.ssl.client.enable
属性描述：Set client to use TLS when connecting to ZooKeeper.
An explicit value overrides any value set via the <code>zookeeper.client.secure</code> system property (note the different name).
Defaults to false if neither is set; when true,
<code>zookeeper.clientCnxnSocket</code> must be set (typically to <code>org.apache.zookeeper.ClientCnxnSocketNetty</code>); other values to set may include <code>zookeeper.ssl.cipher.suites</code>,
<code>zookeeper.ssl.crl.enable</code>,
<code>zookeeper.ssl.enabled.protocols</code>,
<code>zookeeper.ssl.endpoint.identification.algorithm</code>,
<code>zookeeper.ssl.keystore.location</code>,
<code>zookeeper.ssl.keystore.password</code>,
<code>zookeeper.ssl.keystore.type</code>,
<code>zookeeper.ssl.ocsp.enable</code>,
<code>zookeeper.ssl.protocol</code>,
<code>zookeeper.ssl.truststore.location</code>,
<code>zookeeper.ssl.truststore.password</code>,
<code>zookeeper.ssl.truststore.type</code></p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：zookeeper.ssl.keystore.location
属性描述：Keystore location when using a client-side certificate with TLS connectivity to ZooKeeper.
Overrides any explicit value set via the <code>zookeeper.ssl.keyStore.location</code> system property (note the camelCase).</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：zookeeper.ssl.keystore.password
属性描述：Keystore password when using a client-side certificate with TLS connectivity to ZooKeeper.
Overrides any explicit value set via the <code>zookeeper.ssl.keyStore.password</code> system property (note the camelCase).
Note that ZooKeeper does not support a key password different from the keystore password,
so be sure to set the key password in the keystore to be identical to the keystore password; otherwise the connection attempt to Zookeeper will fail.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：zookeeper.ssl.keystore.type
属性描述：Keystore type when using a client-side certificate with TLS connectivity to ZooKeeper.
Overrides any explicit value set via the <code>zookeeper.ssl.keyStore.type</code> system property (note the camelCase).
The default value of <code>null</code> means the type will be auto-detected based on the filename extension of the keystore.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：zookeeper.ssl.truststore.location
属性描述：Truststore location when using TLS connectivity to ZooKeeper.
Overrides any explicit value set via the <code>zookeeper.ssl.trustStore.location</code> system property (note the camelCase).</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：zookeeper.ssl.truststore.password
属性描述：Truststore password when using TLS connectivity to ZooKeeper.
Overrides any explicit value set via the <code>zookeeper.ssl.trustStore.password</code> system property (note the camelCase).</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：zookeeper.ssl.truststore.type
属性描述：Truststore type when using TLS connectivity to ZooKeeper.
Overrides any explicit value set via the <code>zookeeper.ssl.trustStore.type</code> system property (note the camelCase).
The default value of <code>null</code> means the type will be auto-detected based on the filename extension of the truststore.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
更新模式：只读
```

```
属性名称：alter.config.policy.class.name
属性描述：The alter configs policy class that should be used for validation.
The class should implement the <code>org.apache.kafka.server.policy.AlterConfigPolicy</code> interface.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：alter.log.dirs.replication.quota.window.num
属性描述：The number of samples to retain in memory for alter log dirs replication quotas</p>
数据类型：int
默认的值：11
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：alter.log.dirs.replication.quota.window.size.seconds
属性描述：The time span of each sample for alter log dirs replication quotas</p>
数据类型：int
默认的值：1
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：authorizer.class.name
属性描述：The fully qualified name of a class that implements <code>org.apache.kafka.server.authorizer.Authorizer</code> interface,
which is used by the broker for authorization.</p>
数据类型：string
默认的值：""
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：client.quota.callback.class
属性描述：The fully qualified name of a class that implements the ClientQuotaCallback interface,
which is used to determine quota limits applied to client requests.
By default,
&lt;user&gt;,
&lt;client-id&gt;,
&lt;user&gt; or &lt;client-id&gt; quotas stored in ZooKeeper are applied.
For any given request,
the most specific quota that matches the user principal of the session and the client-id of the request is applied.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：connection.failed.authentication.delay.ms
属性描述：Connection close delay on failed authentication: this is the time (in milliseconds) by which connection close will be delayed on authentication failure.
This must be configured to be less than connections.max.idle.ms to prevent connection timeout.</p>
数据类型：int
默认的值：100
有效的值：[0,...]
重要等级：low
更新模式：只读
```

```
属性名称：controller.quorum.retry.backoff.ms
属性描述：The amount of time to wait before attempting to retry a failed request to a given topic partition.
This avoids repeatedly sending requests in a tight loop under some failure scenarios.</p>
数据类型：int
默认的值：20
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：controller.quota.window.num
属性描述：The number of samples to retain in memory for controller mutation quotas</p>
数据类型：int
默认的值：11
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：controller.quota.window.size.seconds
属性描述：The time span of each sample for controller mutations quotas</p>
数据类型：int
默认的值：1
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：create.topic.policy.class.name
属性描述：The create topic policy class that should be used for validation.
The class should implement the <code>org.apache.kafka.server.policy.CreateTopicPolicy</code> interface.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：delegation.token.expiry.check.interval.ms
属性描述：Scan interval to remove expired delegation tokens.</p>
数据类型：long
默认的值：3600000 (1 hour)
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：kafka.metrics.polling.interval.secs
属性描述：The metrics polling interval (in seconds) which can be used in kafka.metrics.reporters implementations.</p>
数据类型：int
默认的值：10
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：kafka.metrics.reporters
属性描述：A list of classes to use as Yammer metrics custom reporters.
The reporters should implement <code>kafka.metrics.KafkaMetricsReporter</code> trait.
If a client wants to expose JMX operations on a custom reporter,
the custom reporter needs to additionally implement an MBean trait that extends <code>kafka.metrics.KafkaMetricsReporterMBean</code> trait so that the registered MBean is compliant with the standard MBean convention.</p>
数据类型：list
默认的值：""
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：listener.security.protocol.map
属性描述：Map between listener names and security protocols.
This must be defined for the same security protocol to be usable in more than one port or IP.
For example,
internal and external traffic can be separated even if SSL is required for both.
Concretely,
the user could define listeners with names INTERNAL and EXTERNAL and this property as: `INTERNAL:SSL,EXTERNAL:SSL`.
As shown,
key and value are separated by a colon and map entries are separated by commas.
Each listener name should only appear once in the map.
Different security (SSL and SASL) settings can be configured for each listener by adding a normalised prefix (the listener name is lowercased) to the config name.
For example,
to set a different keystore for the INTERNAL listener,
a config with name <code>listener.name.internal.ssl.keystore.location</code> would be set.
If the config for the listener name is not set,
the config will fallback to the generic config (i.e.
<code>ssl.keystore.location</code>).
Note that in KRaft a default mapping from the listener names defined by <code>controller.listener.names</code> to PLAINTEXT is assumed if no explicit mapping is provided and no other security protocol is in use.</p>
数据类型：string
默认的值：PLAINTEXT:PLAINTEXT,SSL:SSL,SASL_PLAINTEXT:SASL_PLAINTEXT,SASL_SSL:SASL_SSL
有效的值：
重要等级：low
更新模式：每个代理
```

```
属性名称：log.message.downconversion.enable
属性描述：This configuration controls whether down-conversion of message formats is enabled to satisfy consume requests.
When set to <code>false</code>,
broker will not perform down-conversion for consumers expecting an older message format.
The broker responds with <code>UNSUPPORTED_VERSION</code> error for consume requests from such older clients.
This configurationdoes not apply to any message format conversion that might be required for replication to followers.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：low
更新模式：cluster-wide
```

```
属性名称：metric.reporters
属性描述：A list of classes to use as metrics reporters.
Implementing the <code>org.apache.kafka.common.metrics.MetricsReporter</code> interface allows plugging in classes that will be notified of new metric creation.
The JmxReporter is always included to register JMX statistics.</p>
数据类型：list
默认的值：""
有效的值：
重要等级：low
更新模式：cluster-wide
```

```
属性名称：metrics.num.samples
属性描述：The number of samples maintained to compute metrics.</p>
数据类型：int
默认的值：2
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：metrics.recording.level
属性描述：The highest recording level for metrics.</p>
数据类型：string
默认的值：INFO
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：metrics.sample.window.ms
属性描述：The window of time a metrics sample is computed over.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：password.encoder.cipher.algorithm
属性描述：The Cipher algorithm used for encoding dynamically configured passwords.</p>
数据类型：string
默认的值：AES/CBC/PKCS5Padding
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：password.encoder.iterations
属性描述：The iteration count used for encoding dynamically configured passwords.</p>
数据类型：int
默认的值：4096
有效的值：[1024,...]
重要等级：low
更新模式：只读
```

```
属性名称：password.encoder.key.length
属性描述：The key length used for encoding dynamically configured passwords.</p>
数据类型：int
默认的值：128
有效的值：[8,...]
重要等级：low
更新模式：只读
```

```
属性名称：password.encoder.keyfactory.algorithm
属性描述：The SecretKeyFactory algorithm used for encoding dynamically configured passwords.
Default is PBKDF2WithHmacSHA512 if available and PBKDF2WithHmacSHA1 otherwise.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：quota.window.num
属性描述：The number of samples to retain in memory for client quotas</p>
数据类型：int
默认的值：11
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：quota.window.size.seconds
属性描述：The time span of each sample for client quotas</p>
数据类型：int
默认的值：1
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：replication.quota.window.num
属性描述：The number of samples to retain in memory for replication quotas</p>
数据类型：int
默认的值：11
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：replication.quota.window.size.seconds
属性描述：The time span of each sample for replication quotas</p>
数据类型：int
默认的值：1
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：sasl.login.connect.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider connection timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.login.read.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider read timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.login.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.login.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.oauthbearer.clock.skew.seconds
属性描述：The (optional) value in seconds to allow for differences between the time of the OAuth/OIDC identity provider and the broker.</p>
数据类型：int
默认的值：30
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.oauthbearer.expected.audience
属性描述：The (optional) comma-delimited setting for the broker to use to verify that the JWT was issued for one of the expected audiences.
The JWT will be inspected for the standard OAuth "aud" claim and if this value is set,
the broker will match the value from JWT's "aud" claim  to see if there is an exact match.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.oauthbearer.expected.issuer
属性描述：The (optional) setting for the broker to use to verify that the JWT was created by the expected issuer.
The JWT will be inspected for the standard OAuth "iss" claim and if this value is set,
the broker will match it exactly against what is in the JWT's "iss" claim.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.refresh.ms
属性描述：The (optional) value in milliseconds for the broker to wait between refreshing its JWKS (JSON Web Key Set) cache that contains the keys to verify the signature of the JWT.</p>
数据类型：long
默认的值：3600000 (1 hour)
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between attempts to retrieve the JWKS (JSON Web Key Set) from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between JWKS (JSON Web Key Set) retrieval attempts from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.oauthbearer.scope.claim.name
属性描述：The OAuth claim for the scope is often named "scope",
but this (optional) setting can provide a different name to use for the scope included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：scope
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：sasl.oauthbearer.sub.claim.name
属性描述：The OAuth claim for the subject is often named "sub",
but this (optional) setting can provide a different name to use for the subject included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：sub
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：security.providers
属性描述：A list of configurable creator classes each returning a provider implementing security algorithms.
These classes should implement the <code>org.apache.kafka.common.security.auth.SecurityProviderCreator</code> interface.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：ssl.endpoint.identification.algorithm
属性描述：The endpoint identification algorithm to validate server hostname using server certificate.
</p>
数据类型：string
默认的值：https
有效的值：
重要等级：low
更新模式：每个代理
```

```
属性名称：ssl.engine.factory.class
属性描述：The class of type org.apache.kafka.common.security.auth.SslEngineFactory to provide SSLEngine objects.
Default value is org.apache.kafka.common.security.ssl.DefaultSslEngineFactory</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
更新模式：每个代理
```

```
属性名称：ssl.principal.mapping.rules
属性描述：A list of rules for mapping from distinguished name from the client certificate to short name.
The rules are evaluated in order and the first rule that matches a principal name is used to map it to a short name.
Any later rules in the list are ignored.
By default,
distinguished name of the X.500 certificate will be the principal.
For more details on the format please see <a href="#security_authz"> security authorization and acls</a>.
Note that this configuration is ignored if an extension of KafkaPrincipalBuilder is provided by the <code>principal.builder.class</code> configuration.</p>
数据类型：string
默认的值：DEFAULT
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：ssl.secure.random.implementation
属性描述：The SecureRandom PRNG implementation to use for SSL cryptography operations.
</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
更新模式：每个代理
```

```
属性名称：transaction.abort.timed.out.transaction.cleanup.interval.ms
属性描述：The interval at which to rollback transactions that have timed out</p>
数据类型：int
默认的值：10000 (10 seconds)
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：transaction.remove.expired.transaction.cleanup.interval.ms
属性描述：The interval at which to remove transactions that have expired due to <code>transactional.id.expiration.ms</code> passing</p>
数据类型：int
默认的值：3600000 (1 hour)
有效的值：[1,...]
重要等级：low
更新模式：只读
```

```
属性名称：zookeeper.ssl.cipher.suites
属性描述：Specifies the enabled cipher suites to be used in ZooKeeper TLS negotiation (csv).
Overrides any explicit value set via the <code>zookeeper.ssl.ciphersuites</code> system property (note the single word "ciphersuites").
The default value of <code>null</code> means the list of enabled cipher suites is determined by the Java runtime being used.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：zookeeper.ssl.crl.enable
属性描述：Specifies whether to enable Certificate Revocation List in the ZooKeeper TLS protocols.
Overrides any explicit value set via the <code>zookeeper.ssl.crl</code> system property (note the shorter name).</p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：zookeeper.ssl.enabled.protocols
属性描述：Specifies the enabled protocol(s) in ZooKeeper TLS negotiation (csv).
Overrides any explicit value set via the <code>zookeeper.ssl.enabledProtocols</code> system property (note the camelCase).
The default value of <code>null</code> means the enabled protocol will be the value of the <code>zookeeper.ssl.protocol</code> configuration property.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：zookeeper.ssl.endpoint.identification.algorithm
属性描述：Specifies whether to enable hostname verification in the ZooKeeper TLS negotiation process,
with (case-insensitively) "https" meaning ZooKeeper hostname verification is enabled and an explicit blank value meaning it is disabled (disabling it is only recommended for testing purposes).
An explicit value overrides any "true" or "false" value set via the <code>zookeeper.ssl.hostnameVerification</code> system property (note the different name and values; true implies https and false implies blank).</p>
数据类型：string
默认的值：HTTPS
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：zookeeper.ssl.ocsp.enable
属性描述：Specifies whether to enable Online Certificate Status Protocol in the ZooKeeper TLS protocols.
Overrides any explicit value set via the <code>zookeeper.ssl.ocsp</code> system property (note the shorter name).</p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：low
更新模式：只读
```

```
属性名称：zookeeper.ssl.protocol
属性描述：Specifies the protocol to be used in ZooKeeper TLS negotiation.
An explicit value overrides any value set via the same-named <code>zookeeper.ssl.protocol</code> system property.</p>
数据类型：string
默认的值：TLSv1.2
有效的值：
重要等级：low
更新模式：只读
</ul>
  <p>More details about broker configuration can be found in the scala class <code>kafka.server.KafkaConfig</code>.</p>
```

### 3.1.1 更新代理配置

```
  From Kafka version 1.1 onwards,
some of the broker configs can be updated without restarting the broker.
See the
  <code>Dynamic Update Mode</code> column in <a href="#brokerconfigs">Broker Configs</a> for the update mode of each broker config.
  <ul>
    <li><code>read-only</code>: Requires a broker restart for update</li>
    <li><code>per-broker</code>: May be updated dynamically for each broker</li>
    <li><code>cluster-wide</code>: May be updated dynamically as a cluster-wide default.
May also be updated as a per-broker value for testing.</li>
  </ul>
  To alter the current broker configs for broker id 0 (for example,
the number of log cleaner threads):
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-configs.sh --bootstrap-server localhost:9092 --entity-type brokers --entity-name <span class="token number">0</span> --alter --add-config log.cleaner.threads<span class="token operator">=</span><span class="token number">2</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
  To describe the current dynamic broker configs for broker id 0:
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-configs.sh --bootstrap-server localhost:9092 --entity-type brokers --entity-name <span class="token number">0</span> --describe<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
  To delete a config override and revert to the statically configured or default value for broker id 0 (for example,
  the number of log cleaner threads):
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-configs.sh --bootstrap-server localhost:9092 --entity-type brokers --entity-name <span class="token number">0</span> --alter --delete-config log.cleaner.threads<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
  Some configs may be configured as a cluster-wide default to maintain consistent values across the whole cluster.
 All brokers
  in the cluster will process the cluster default update.
For example,
to update log cleaner threads on all brokers:
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-configs.sh --bootstrap-server localhost:9092 --entity-type brokers --entity-default --alter --add-config log.cleaner.threads<span class="token operator">=</span><span class="token number">2</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
  To describe the currently configured dynamic cluster-wide default configs:
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-configs.sh --bootstrap-server localhost:9092 --entity-type brokers --entity-default --describe<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
  All configs that are configurable at cluster level may also be configured at per-broker level (e.g.
for testing).
  If a config value is defined at different levels,
the following order of precedence is used:
  <ul>
  <li>Dynamic per-broker config stored in ZooKeeper</li>
  <li>Dynamic cluster-wide default config stored in ZooKeeper</li>
  <li>Static broker config from <code>server.properties</code></li>
  <li>Kafka default,
see <a href="#brokerconfigs">broker configs</a></li>
  </ul>
  <h5>Updating Password Configs Dynamically</h5>
  <p>Password config values that are dynamically updated are encrypted before storing in ZooKeeper.
The broker config
  <code>password.encoder.secret</code> must be configured in <code>server.properties</code> to enable dynamic update
  of password configs.
The secret may be different on different brokers.</p>
  <p>The secret used for password encoding may be rotated with a rolling restart of brokers.
The old secret used for encoding
  passwords currently in ZooKeeper must be provided in the static broker config <code>password.encoder.old.secret</code> and
  the new secret must be provided in <code>password.encoder.secret</code>.
All dynamic password configs stored in ZooKeeper
  will be re-encoded with the new secret when the broker starts up.</p>
  <p>In Kafka 1.1.x,
all dynamically updated password configs must be provided in every alter request when updating configs
  using <code>kafka-configs.sh</code> even if the password config is not being altered.
This constraint will be removed in
  a future release.</p>
  <h5>Updating Password Configs in ZooKeeper Before Starting Brokers</h5>
  From Kafka 2.0.0 onwards,
<code>kafka-configs.sh</code> enables dynamic broker configs to be updated using ZooKeeper before
  starting brokers for bootstrapping.
This enables all password configs to be stored in encrypted form,
avoiding the need for
  clear passwords in <code>server.properties</code>.
The broker config <code>password.encoder.secret</code> must also be specified
  if any password configs are included in the alter command.
Additional encryption parameters may also be specified.
Password
  encoder configs will not be persisted in ZooKeeper.
For example,
to store SSL key password for listener <code>INTERNAL</code>
  on broker 0:
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-configs.sh --zookeeper localhost:2182 --zk-tls-config-file zk_tls_config.properties --entity-type brokers --entity-name <span class="token number">0</span> --alter --add-config
    <span class="token string">'listener.name.internal.ssl.key.password=key-password,password.encoder.secret=secret,password.encoder.iterations=8192'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
  The configuration <code>listener.name.internal.ssl.key.password</code> will be persisted in ZooKeeper in encrypted
  form using the provided encoder configs.
The encoder secret and iterations are not persisted in ZooKeeper.
  <h5>Updating SSL Keystore of an Existing Listener</h5>
  Brokers may be configured with SSL keystores with short validity periods to reduce the risk of compromised certificates.
  Keystores may be updated dynamically without restarting the broker.
The config name must be prefixed with the listener prefix
  <code>listener.name.{listenerName}.</code> so that only the keystore config of a specific listener is updated.
  The following configs may be updated in a single alter request at per-broker level:
  <ul>
    <li><code>ssl.keystore.type</code></li>
    <li><code>ssl.keystore.location</code></li>
    <li><code>ssl.keystore.password</code></li>
    <li><code>ssl.key.password</code></li>
  </ul>
  If the listener is the inter-broker listener,
the update is allowed only if the new keystore is trusted by the truststore
  configured for that listener.
For other listeners,
no trust validation is performed on the keystore by the broker.
Certificates
  must be signed by the same certificate authority that signed the old certificate to avoid any client authentication failures.
  <h5>Updating SSL Truststore of an Existing Listener</h5>
  Broker truststores may be updated dynamically without restarting the broker to add or remove certificates.
  Updated truststore will be used to authenticate new client connections.
The config name must be prefixed with the
  listener prefix <code>listener.name.{listenerName}.</code> so that only the truststore config of a specific listener
  is updated.
The following configs may be updated in a single alter request at per-broker level:
  <ul>
    <li><code>ssl.truststore.type</code></li>
    <li><code>ssl.truststore.location</code></li>
    <li><code>ssl.truststore.password</code></li>
  </ul>
  If the listener is the inter-broker listener,
the update is allowed only if the existing keystore for that listener is trusted by
  the new truststore.
For other listeners,
no trust validation is performed by the broker before the update.
Removal of CA certificates
  used to sign client certificates from the new truststore can lead to client authentication failures.
  <h5>Updating Default Topic Configuration</h5>
  Default topic configuration options used by brokers may be updated without broker restart.
The configs are applied to topics
  without a topic config override for the equivalent per-topic config.
One or more of these configs may be overridden at
  cluster-default level used by all brokers.
  <ul>
    <li><code>log.segment.bytes</code></li>
    <li><code>log.roll.ms</code></li>
    <li><code>log.roll.hours</code></li>
    <li><code>log.roll.jitter.ms</code></li>
    <li><code>log.roll.jitter.hours</code></li>
    <li><code>log.index.size.max.bytes</code></li>
    <li><code>log.flush.interval.messages</code></li>
    <li><code>log.flush.interval.ms</code></li>
    <li><code>log.retention.bytes</code></li>
    <li><code>log.retention.ms</code></li>
    <li><code>log.retention.minutes</code></li>
    <li><code>log.retention.hours</code></li>
    <li><code>log.index.interval.bytes</code></li>
    <li><code>log.cleaner.delete.retention.ms</code></li>
    <li><code>log.cleaner.min.compaction.lag.ms</code></li>
    <li><code>log.cleaner.max.compaction.lag.ms</code></li>
    <li><code>log.cleaner.min.cleanable.ratio</code></li>
    <li><code>log.cleanup.policy</code></li>
    <li><code>log.segment.delete.delay.ms</code></li>
    <li><code>unclean.leader.election.enable</code></li>
    <li><code>min.insync.replicas</code></li>
    <li><code>max.message.bytes</code></li>
    <li><code>compression.type</code></li>
    <li><code>log.preallocate</code></li>
    <li><code>log.message.timestamp.type</code></li>
    <li><code>log.message.timestamp.difference.max.ms</code></li>
  </ul>
  From Kafka version 2.0.0 onwards,
unclean leader election is automatically enabled by the controller when the config
  <code>unclean.leader.election.enable</code> is dynamically updated.
  In Kafka version 1.1.x,
changes to <code>unclean.leader.election.enable</code> take effect only when a new controller is elected.
  Controller re-election may be forced by running:
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/zookeeper-shell.sh localhost
  rmr /controller<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
  <h5>Updating Log Cleaner Configs</h5>
  Log cleaner configs may be updated dynamically at cluster-default level used by all brokers.
The changes take effect
  on the next iteration of log cleaning.
One or more of these configs may be updated:
  <ul>
    <li><code>log.cleaner.threads</code></li>
    <li><code>log.cleaner.io.max.bytes.per.second</code></li>
    <li><code>log.cleaner.dedupe.buffer.size</code></li>
    <li><code>log.cleaner.io.buffer.size</code></li>
    <li><code>log.cleaner.io.buffer.load.factor</code></li>
    <li><code>log.cleaner.backoff.ms</code></li>
  </ul>
  <h5>Updating Thread Configs</h5>
  The size of various thread pools used by the broker may be updated dynamically at cluster-default level used by all brokers.
  Updates are restricted to the range <code>currentSize / 2</code> to <code>currentSize * 2</code> to ensure that config updates are
  handled gracefully.
  <ul>
    <li><code>num.network.threads</code></li>
    <li><code>num.io.threads</code></li>
    <li><code>num.replica.fetchers</code></li>
    <li><code>num.recovery.threads.per.data.dir</code></li>
    <li><code>log.cleaner.threads</code></li>
    <li><code>background.threads</code></li>
  </ul>
  <h5>Updating ConnectionQuota Configs</h5>
  The maximum number of connections allowed for a given IP/host by the broker may be updated dynamically at cluster-default level used by all brokers.
  The changes will apply for new connection creations and the existing connections count will be taken into account by the new limits.
  <ul>
    <li><code>max.connections.per.ip</code></li>
    <li><code>max.connections.per.ip.overrides</code></li>
  </ul>
  <h5>Adding and Removing Listeners</h5>
  <p>Listeners may be added or removed dynamically.
When a new listener is added,
security configs of the listener must be provided
  as listener configs with the listener prefix <code>listener.name.{listenerName}.</code>.
If the new listener uses SASL,
  the JAAS configuration of the listener must be provided using the JAAS configuration property <code>sasl.jaas.config</code>
  with the listener and mechanism prefix.
See <a href="#security_jaas_broker">JAAS configuration for Kafka brokers</a> for details.</p>
  <p>In Kafka version 1.1.x,
the listener used by the inter-broker listener may not be updated dynamically.
To update the inter-broker
  listener to a new listener,
the new listener may be added on all brokers without restarting the broker.
A rolling restart is then
  required to update <code>inter.broker.listener.name</code>.</p>
  In addition to all the security configs of new listeners,
the following configs may be updated dynamically at per-broker level:
  <ul>
    <li><code>listeners</code></li>
    <li><code>advertised.listeners</code></li>
    <li><code>listener.security.protocol.map</code></li>
  </ul>
  Inter-broker listener must be configured using the static broker configuration <code>inter.broker.listener.name</code>
  or <code>inter.broker.security.protocol</code>.
```

## 3.2 主题配置（Topic）

```
  Configurations pertinent to topics have both a server default as well an optional per-topic override.
If no per-topic configuration is given the server default is used.
The override can be set at topic creation time by giving one or more <code>--config</code> options.
This example creates a topic named <i>my-topic</i> with a custom max message size and flush rate:
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-topics.sh --bootstrap-server localhost:9092 --create --topic my-topic --partitions <span class="token number">1</span> <span class="token punctuation">\</span>
  --replication-factor <span class="token number">1</span> --config max.message.bytes<span class="token operator">=</span><span class="token number">64000</span> --config flush.messages<span class="token operator">=</span><span class="token number">1</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
  Overrides can also be changed or set later using the alter configs command.
This example updates the max message size for <i>my-topic</i>:
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-configs.sh --bootstrap-server localhost:9092 --entity-type topics --entity-name my-topic
  --alter --add-config max.message.bytes<span class="token operator">=</span><span class="token number">128000</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
  To check overrides set on the topic you can do
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-configs.sh --bootstrap-server localhost:9092 --entity-type topics --entity-name my-topic --describe<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
  To remove an override you can do
  <pre class="line-numbers  language-bash"><code class="  language-bash"><span class="token operator">&gt;</span> bin/kafka-configs.sh --bootstrap-server localhost:9092  --entity-type topics --entity-name my-topic
  --alter --delete-config max.message.bytes<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
  The following are the topic-level configurations.
The server's default configuration for this property is given under the Server Default Property heading.
A given server default config value only applies to a topic if it does not have an explicit topic config override.
  <ul class="config-list">
```

```
属性名称：cleanup.policy
属性描述：A string that is either "delete" or "compact" or both.
This string designates the retention policy to use on old log segments.
The default policy ("delete") will discard old segments when their retention time or size limit has been reached.
The "compact" setting will enable <a href="#compaction">log compaction</a> on the topic.</p>
数据类型：list
默认的值：delete
有效的值：[compact,
delete]
<tr><th>Server Default Property:</th><td>log.cleanup.policy
重要等级：medium
```

```
属性名称：compression.type
属性描述：Specify the final compression type for a given topic.
This configuration accepts the standard compression codecs ('gzip',
'snappy',
'lz4',
'zstd').
It additionally accepts 'uncompressed' which is equivalent to no compression; and 'producer' which means retain the original compression codec set by the producer.</p>
数据类型：string
默认的值：producer
有效的值：[uncompressed,
zstd,
lz4,
snappy,
gzip,
producer]
<tr><th>Server Default Property:</th><td>compression.type
重要等级：medium
```

```
属性名称：delete.retention.ms
属性描述：The amount of time to retain delete tombstone markers for <a href="#compaction">log compacted</a> topics.
This setting also gives a bound on the time in which a consumer must complete a read if they begin from offset 0 to ensure that they get a valid snapshot of the final stage (otherwise delete tombstones may be collected before they complete their scan).</p>
数据类型：long
默认的值：86400000 (1 day)
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>log.cleaner.delete.retention.ms
重要等级：medium
```

```
属性名称：file.delete.delay.ms
属性描述：The time to wait before deleting a file from the filesystem</p>
数据类型：long
默认的值：60000 (1 minute)
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>log.segment.delete.delay.ms
重要等级：medium
```

```
属性名称：flush.messages
属性描述：This setting allows specifying an interval at which we will force an fsync of data written to the log.
For example if this was set to 1 we would fsync after every message; if it were 5 we would fsync after every five messages.
In general we recommend you not set this and use replication for durability and allow the operating system's background flush capabilities as it is more efficient.
This setting can be overridden on a per-topic basis (see <a href="#topicconfigs">the per-topic configuration section</a>).</p>
数据类型：long
默认的值：9223372036854775807
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>log.flush.interval.messages
重要等级：medium
```

```
属性名称：flush.ms
属性描述：This setting allows specifying a time interval at which we will force an fsync of data written to the log.
For example if this was set to 1000 we would fsync after 1000 ms had passed.
In general we recommend you not set this and use replication for durability and allow the operating system's background flush capabilities as it is more efficient.</p>
数据类型：long
默认的值：9223372036854775807
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>log.flush.interval.ms
重要等级：medium
```

```
属性名称：follower.replication.throttled.replicas
属性描述：A list of replicas for which log replication should be throttled on the follower side.
The list should describe a set of replicas in the form [PartitionId]:[BrokerId],[PartitionId]:[BrokerId]:...
or alternatively the wildcard '*' can be used to throttle all replicas for this topic.</p>
数据类型：list
默认的值：""
有效的值：[partitionId]:[brokerId],[partitionId]:[brokerId],...
<tr><th>Server Default Property:</th><td>follower.replication.throttled.replicas
重要等级：medium
```

```
属性名称：index.interval.bytes
属性描述：This setting controls how frequently Kafka adds an index entry to its offset index.
The default setting ensures that we index a message roughly every 4096 bytes.
More indexing allows reads to jump closer to the exact position in the log but makes the index larger.
You probably don't need to change this.</p>
数据类型：int
默认的值：4096 (4 kibibytes)
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>log.index.interval.bytes
重要等级：medium
```

```
属性名称：leader.replication.throttled.replicas
属性描述：A list of replicas for which log replication should be throttled on the leader side.
The list should describe a set of replicas in the form [PartitionId]:[BrokerId],[PartitionId]:[BrokerId]:...
or alternatively the wildcard '*' can be used to throttle all replicas for this topic.</p>
数据类型：list
默认的值：""
有效的值：[partitionId]:[brokerId],[partitionId]:[brokerId],...
<tr><th>Server Default Property:</th><td>leader.replication.throttled.replicas
重要等级：medium
```

```
属性名称：max.compaction.lag.ms
属性描述：The maximum time a message will remain ineligible for compaction in the log.
Only applicable for logs that are being compacted.</p>
数据类型：long
默认的值：9223372036854775807
有效的值：[1,...]
<tr><th>Server Default Property:</th><td>log.cleaner.max.compaction.lag.ms
重要等级：medium
```

```
属性名称：max.message.bytes
属性描述：The largest record batch size allowed by Kafka (after compression if compression is enabled).
If this is increased and there are consumers older than 0.10.2,
the consumers' fetch size must also be increased so that they can fetch record batches this large.
In the latest message format version,
records are always grouped into batches for efficiency.
In previous message format versions,
uncompressed records are not grouped into batches and this limit only applies to a single record in that case.</p>
数据类型：int
默认的值：1048588
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>message.max.bytes
重要等级：medium
```

```
属性名称：message.format.version
属性描述：[DEPRECATED] Specify the message format version the broker will use to append messages to the logs.
The value of this config is always assumed to be `3.0` if `inter.broker.protocol.version` is 3.0 or higher (the actual config value is ignored).
Otherwise,
the value should be a valid ApiVersion.
Some examples are: 0.10.0,
1.1,
2.8,
3.0.
By setting a particular message format version,
the user is certifying that all the existing messages on disk are smaller or equal than the specified version.
Setting this value incorrectly will cause consumers with older versions to break as they will receive messages with a format that they don't understand.</p>
数据类型：string
默认的值：3.0-IV1
有效的值：[0.8.0,
0.8.1,
0.8.2,
0.9.0,
0.10.0-IV0,
0.10.0-IV1,
0.10.1-IV0,
0.10.1-IV1,
0.10.1-IV2,
0.10.2-IV0,
0.11.0-IV0,
0.11.0-IV1,
0.11.0-IV2,
1.0-IV0,
1.1-IV0,
2.0-IV0,
2.0-IV1,
2.1-IV0,
2.1-IV1,
2.1-IV2,
2.2-IV0,
2.2-IV1,
2.3-IV0,
2.3-IV1,
2.4-IV0,
2.4-IV1,
2.5-IV0,
2.6-IV0,
2.7-IV0,
2.7-IV1,
2.7-IV2,
2.8-IV0,
2.8-IV1,
3.0-IV0,
3.0-IV1,
3.1-IV0,
3.2-IV0]
<tr><th>Server Default Property:</th><td>log.message.format.version
重要等级：medium
```

```
属性名称：message.timestamp.difference.max.ms
属性描述：The maximum difference allowed between the timestamp when a broker receives a message and the timestamp specified in the message.
If message.timestamp.type=CreateTime,
a message will be rejected if the difference in timestamp exceeds this threshold.
This configuration is ignored if message.timestamp.type=LogAppendTime.</p>
数据类型：long
默认的值：9223372036854775807
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>log.message.timestamp.difference.max.ms
重要等级：medium
```

```
属性名称：message.timestamp.type
属性描述：Define whether the timestamp in the message is message create time or log append time.
The value should be either `CreateTime` or `LogAppendTime`</p>
数据类型：string
默认的值：CreateTime
有效的值：[CreateTime,
LogAppendTime]
<tr><th>Server Default Property:</th><td>log.message.timestamp.type
重要等级：medium
```

```
属性名称：min.cleanable.dirty.ratio
属性描述：This configuration controls how frequently the log compactor will attempt to clean the log (assuming <a href="#compaction">log compaction</a> is enabled).
By default we will avoid cleaning a log where more than 50% of the log has been compacted.
This ratio bounds the maximum space wasted in the log by duplicates (at 50% at most 50% of the log could be duplicates).
A higher ratio will mean fewer,
more efficient cleanings but will mean more wasted space in the log.
If the max.compaction.lag.ms or the min.compaction.lag.ms configurations are also specified,
then the log compactor considers the log to be eligible for compaction as soon as either: (i) the dirty ratio threshold has been met and the log has had dirty (uncompacted) records for at least the min.compaction.lag.ms duration,
or (ii) if the log has had dirty (uncompacted) records for at most the max.compaction.lag.ms period.</p>
数据类型：double
默认的值：0.5
有效的值：[0,...,1]
<tr><th>Server Default Property:</th><td>log.cleaner.min.cleanable.ratio
重要等级：medium
```

```
属性名称：min.compaction.lag.ms
属性描述：The minimum time a message will remain uncompacted in the log.
Only applicable for logs that are being compacted.</p>
数据类型：long
默认的值：0
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>log.cleaner.min.compaction.lag.ms
重要等级：medium
```

```
属性名称：min.insync.replicas
属性描述：When a producer sets acks to "all" (or "-1"),
this configuration specifies the minimum number of replicas that must acknowledge a write for the write to be considered successful.
If this minimum cannot be met,
then the producer will raise an exception (either NotEnoughReplicas or NotEnoughReplicasAfterAppend).<br>When used together,
<code>min.insync.replicas</code> and <code>acks</code> allow you to enforce greater durability guarantees.
A typical scenario would be to create a topic with a replication factor of 3,
set <code>min.insync.replicas</code> to 2,
and produce with <code>acks</code> of "all".
This will ensure that the producer raises an exception if a majority of replicas do not receive a write.</p>
数据类型：int
默认的值：1
有效的值：[1,...]
<tr><th>Server Default Property:</th><td>min.insync.replicas
重要等级：medium
```

```
属性名称：preallocate
属性描述：True if we should preallocate the file on disk when creating a new log segment.</p>
数据类型：boolean
默认的值：false
有效的值：
<tr><th>Server Default Property:</th><td>log.preallocate
重要等级：medium
```

```
属性名称：retention.bytes
属性描述：This configuration controls the maximum size a partition (which consists of log segments) can grow to before we will discard old log segments to free up space if we are using the "delete" retention policy.
By default there is no size limit only a time limit.
Since this limit is enforced at the partition level,
multiply it by the number of partitions to compute the topic retention in bytes.</p>
数据类型：long
默认的值：-1
有效的值：
<tr><th>Server Default Property:</th><td>log.retention.bytes
重要等级：medium
```

```
属性名称：retention.ms
属性描述：This configuration controls the maximum time we will retain a log before we will discard old log segments to free up space if we are using the "delete" retention policy.
This represents an SLA on how soon consumers must read their data.
If set to -1,
no time limit is applied.</p>
数据类型：long
默认的值：604800000 (7 days)
有效的值：[-1,...]
<tr><th>Server Default Property:</th><td>log.retention.ms
重要等级：medium
```

```
属性名称：segment.bytes
属性描述：This configuration controls the segment file size for the log.
Retention and cleaning is always done a file at a time so a larger segment size means fewer files but less granular control over retention.</p>
数据类型：int
默认的值：1073741824 (1 gibibyte)
有效的值：[14,...]
<tr><th>Server Default Property:</th><td>log.segment.bytes
重要等级：medium
```

```
属性名称：segment.index.bytes
属性描述：This configuration controls the size of the index that maps offsets to file positions.
We preallocate this index file and shrink it only after log rolls.
You generally should not need to change this setting.</p>
数据类型：int
默认的值：10485760 (10 mebibytes)
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>log.index.size.max.bytes
重要等级：medium
```

```
属性名称：segment.jitter.ms
属性描述：The maximum random jitter subtracted from the scheduled segment roll time to avoid thundering herds of segment rolling</p>
数据类型：long
默认的值：0
有效的值：[0,...]
<tr><th>Server Default Property:</th><td>log.roll.jitter.ms
重要等级：medium
```

```
属性名称：segment.ms
属性描述：This configuration controls the period of time after which Kafka will force the log to roll even if the segment file isn't full to ensure that retention can delete or compact old data.</p>
数据类型：long
默认的值：604800000 (7 days)
有效的值：[1,...]
<tr><th>Server Default Property:</th><td>log.roll.ms
重要等级：medium
```

```
属性名称：unclean.leader.election.enable
属性描述：Indicates whether to enable replicas not in the ISR set to be elected as leader as a last resort,
even though doing so may result in data loss.</p>
数据类型：boolean
默认的值：false
有效的值：
<tr><th>Server Default Property:</th><td>unclean.leader.election.enable
重要等级：medium
```

```
属性名称：message.downconversion.enable
属性描述：This configuration controls whether down-conversion of message formats is enabled to satisfy consume requests.
When set to <code>false</code>,
broker will not perform down-conversion for consumers expecting an older message format.
The broker responds with <code>UNSUPPORTED_VERSION</code> error for consume requests from such older clients.
This configurationdoes not apply to any message format conversion that might be required for replication to followers.</p>
数据类型：boolean
默认的值：true
有效的值：
<tr><th>Server Default Property:</th><td>log.message.downconversion.enable
重要等级：low
</ul>
```

## 3.3 生产者配置（Producer）

```
  Below is the configuration of the producer:
  <ul class="config-list">
```

```
属性名称：key.serializer
属性描述：Serializer class for key that implements the <code>org.apache.kafka.common.serialization.Serializer</code> interface.</p>
数据类型：class
默认的值：
有效的值：
重要等级：高
```

```
属性名称：value.serializer
属性描述：Serializer class for value that implements the <code>org.apache.kafka.common.serialization.Serializer</code> interface.</p>
数据类型：class
默认的值：
有效的值：
重要等级：高
```

```
属性名称：bootstrap.servers
属性描述：A list of host/port pairs to use for establishing the initial connection to the Kafka cluster.
The client will make use of all servers irrespective of which servers are specified here for bootstrapping—this list only impacts the initial hosts used to discover the full set of servers.
This list should be in the form <code>host1:port1,host2:port2,...</code>.
Since these servers are just used for the initial connection to discover the full cluster membership (which may change dynamically),
this list need not contain the full set of servers (you may want more than one,
though,
in case a server is down).</p>
数据类型：list
默认的值：""
有效的值：non-null string
重要等级：高
```

```
属性名称：buffer.memory
属性描述：The total bytes of memory the producer can use to buffer records waiting to be sent to the server.
If records are sent faster than they can be delivered to the server the producer will block for <code>max.block.ms</code> after which it will throw an exception.</p><p>This setting should correspond roughly to the total memory the producer will use,
but is not a hard bound since not all memory the producer uses is used for buffering.
Some additional memory will be used for compression (if compression is enabled) as well as for maintaining in-flight requests.</p>
数据类型：long
默认的值：33554432
有效的值：[0,...]
重要等级：高
```

```
属性名称：compression.type
属性描述：The compression type for all data generated by the producer.
The default is none (i.e.
no compression).
Valid  values are <code>none</code>,
<code>gzip</code>,
<code>snappy</code>,
<code>lz4</code>,
or <code>zstd</code>.
Compression is of full batches of data,
so the efficacy of batching will also impact the compression ratio (more batching means better compression).</p>
数据类型：string
默认的值：none
有效的值：
重要等级：高
```

```
属性名称：retries
属性描述：Setting a value greater than zero will cause the client to resend any record whose send fails with a potentially transient error.
Note that this retry is no different than if the client resent the record upon receiving the error.
Produce requests will be failed before the number of retries has been exhausted if the timeout configured by <code>delivery.timeout.ms</code> expires first before successful acknowledgement.
Users should generally prefer to leave this config unset and instead use <code>delivery.timeout.ms</code> to control retry behavior.</p><p>Enabling idempotence requires this config value to be greater than 0.
If conflicting configurations are set and idempotence is not explicitly enabled,
idempotence is disabled.</p><p>Allowing retries while setting <code>enable.idempotence</code> to <code>false</code> and <code>max.in.flight.requests.per.connection</code> to 1 will potentially change the ordering of records because if two batches are sent to a single partition,
and the first fails and is retried but the second succeeds,
then the records in the second batch may appear first.</p>
数据类型：int
默认的值：2147483647
有效的值：[0,...,2147483647]
重要等级：高
```

```
属性名称：ssl.key.password
属性描述：The password of the private key in the key store file or the PEM key specified in `ssl.keystore.key'.
This is required for clients only if two-way authentication is configured.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.certificate.chain
属性描述：Certificate chain in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with a list of X.509 certificates</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.key
属性描述：Private key in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with PKCS#8 keys.
If the key is encrypted,
key password must be specified using 'ssl.key.password'</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.location
属性描述：The location of the key store file.
This is optional for client and can be used for two-way authentication for client.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.password
属性描述：The store password for the key store file.
This is optional for client and only needed if 'ssl.keystore.location' is configured.
Key store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.certificates
属性描述：Trusted certificates in the format specified by 'ssl.truststore.type'.
Default SSL engine factory supports only PEM format with X.509 certificates.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.location
属性描述：The location of the trust store file.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.password
属性描述：The password for the trust store file.
If a password is not set,
trust store file configured will still be used,
but integrity checking is disabled.
Trust store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：batch.size
属性描述：The producer will attempt to batch records together into fewer requests whenever multiple records are being sent to the same partition.
This helps performance on both the client and the server.
This configuration controls the default batch size in bytes.
</p><p>No attempt will be made to batch records larger than this size.
</p><p>Requests sent to brokers will contain multiple batches,
one for each partition with data available to be sent.
</p><p>A small batch size will make batching less common and may reduce throughput (a batch size of zero will disable batching entirely).
A very large batch size may use memory a bit more wastefully as we will always allocate a buffer of the specified batch size in anticipation of additional records.</p><p>Note: This setting gives the upper bound of the batch size to be sent.
If we have fewer than this many bytes accumulated for this partition,
we will 'linger' for the <code>linger.ms</code> time waiting for more records to show up.
This <code>linger.ms</code> setting defaults to 0,
which means we'll immediately send out a record even the accumulated batch size is under this <code>batch.size</code> setting.</p>
数据类型：int
默认的值：16384
有效的值：[0,...]
重要等级：medium
```

```
属性名称：client.dns.lookup
属性描述：Controls how the client uses DNS lookups.
If set to <code>use_all_dns_ips</code>,
connect to each returned IP address in sequence until a successful connection is established.
After a disconnection,
the next IP is used.
Once all IPs have been used once,
the client resolves the IP(s) from the hostname again (both the JVM and the OS cache DNS name lookups,
however).
If set to <code>resolve_canonical_bootstrap_servers_only</code>,
resolve each bootstrap address into a list of canonical names.
After the bootstrap phase,
this behaves the same as <code>use_all_dns_ips</code>.</p>
数据类型：string
默认的值：use_all_dns_ips
有效的值：[use_all_dns_ips,
resolve_canonical_bootstrap_servers_only]
重要等级：medium
```

```
属性名称：client.id
属性描述：An id string to pass to the server when making requests.
The purpose of this is to be able to track the source of requests beyond just ip/port by allowing a logical application name to be included in server-side request logging.</p>
数据类型：string
默认的值：""
有效的值：
重要等级：medium
```

```
属性名称：connections.max.idle.ms
属性描述：Close idle connections after the number of milliseconds specified by this config.</p>
数据类型：long
默认的值：540000 (9 minutes)
有效的值：
重要等级：medium
```

```
属性名称：delivery.timeout.ms
属性描述：An upper bound on the time to report success or failure after a call to <code>send()</code> returns.
This limits the total time that a record will be delayed prior to sending,
the time to await acknowledgement from the broker (if expected),
and the time allowed for retriable send failures.
The producer may report failure to send a record earlier than this config if either an unrecoverable error is encountered,
the retries have been exhausted,
or the record is added to a batch which reached an earlier delivery expiration deadline.
The value of this config should be greater than or equal to the sum of <code>request.timeout.ms</code> and <code>linger.ms</code>.</p>
数据类型：int
默认的值：120000 (2 minutes)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：linger.ms
属性描述：The producer groups together any records that arrive in between request transmissions into a single batched request.
Normally this occurs only under load when records arrive faster than they can be sent out.
However in some circumstances the client may want to reduce the number of requests even under moderate load.
This setting accomplishes this by adding a small amount of artificial delay—that is,
rather than immediately sending out a record,
the producer will wait for up to the given delay to allow other records to be sent so that the sends can be batched together.
This can be thought of as analogous to Nagle's algorithm in TCP.
This setting gives the upper bound on the delay for batching: once we get <code>batch.size</code> worth of records for a partition it will be sent immediately regardless of this setting,
however if we have fewer than this many bytes accumulated for this partition we will 'linger' for the specified time waiting for more records to show up.
This setting defaults to 0 (i.e.
no delay).
Setting <code>linger.ms=5</code>,
for example,
would have the effect of reducing the number of requests sent but would add up to 5ms of latency to records sent in the absence of load.</p>
数据类型：long
默认的值：0
有效的值：[0,...]
重要等级：medium
```

```
属性名称：max.block.ms
属性描述：The configuration controls how long the <code>KafkaProducer</code>'s <code>send()</code>,
<code>partitionsFor()</code>,
<code>initTransactions()</code>,
<code>sendOffsetsToTransaction()</code>,
<code>commitTransaction()</code> and <code>abortTransaction()</code> methods will block.
For <code>send()</code> this timeout bounds the total time waiting for both metadata fetch and buffer allocation (blocking in the user-supplied serializers or partitioner is not counted against this timeout).
For <code>partitionsFor()</code> this timeout bounds the time spent waiting for metadata if it is unavailable.
The transaction-related methods always block,
but may timeout if the transaction coordinator could not be discovered or did not respond within the timeout.</p>
数据类型：long
默认的值：60000 (1 minute)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：max.request.size
属性描述：The maximum size of a request in bytes.
This setting will limit the number of record batches the producer will send in a single request to avoid sending huge requests.
This is also effectively a cap on the maximum uncompressed record batch size.
Note that the server has its own cap on the record batch size (after compression if compression is enabled) which may be different from this.</p>
数据类型：int
默认的值：1048576
有效的值：[0,...]
重要等级：medium
```

```
属性名称：partitioner.class
属性描述：A class to use to determine which partition to be send to when produce the records.
Available options are:</p><ul><li><code>org.apache.kafka.clients.producer.internals.DefaultPartitioner</code>: The default partitioner.
This strategy will try sticking to a partition until the batch is full,
or <code>linger.ms</code> is up.
It works with the strategy:<ul><li>If no partition is specified but a key is present,
choose a partition based on a hash of the key</li><li>If no partition or key is present,
choose the sticky partition that changes when the batch is full,
or <code>linger.ms</code> is up.</li></ul></li><li><code>org.apache.kafka.clients.producer.RoundRobinPartitioner</code>: This partitioning strategy is that each record in a series of consecutive records will be sent to a different partition(no matter if the 'key' is provided or not),
until we run out of partitions and start over again.
Note: There's a known issue that will cause uneven distribution when new batch is created.
Please check KAFKA-9965 for more detail.</li><li><code>org.apache.kafka.clients.producer.UniformStickyPartitioner</code>: This partitioning strategy will try sticking to a partition(no matter if the 'key' is provided or not) until the batch is full,
or <code>linger.ms</code> is up.</li></ul><p>Implementing the <code>org.apache.kafka.clients.producer.Partitioner</code> interface allows you to plug in a custom partitioner.</p>
数据类型：class
默认的值：org.apache.kafka.clients.producer.internals.DefaultPartitioner
有效的值：
重要等级：medium
```

```
属性名称：receive.buffer.bytes
属性描述：The size of the TCP receive buffer (SO_RCVBUF) to use when reading data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：32768 (32 kibibytes)
有效的值：[-1,...]
重要等级：medium
```

```
属性名称：request.timeout.ms
属性描述：The configuration controls the maximum amount of time the client will wait for the response of a request.
If the response is not received before the timeout elapses the client will resend the request if necessary or fail the request if retries are exhausted.
This should be larger than <code>replica.lag.time.max.ms</code> (a broker configuration) to reduce the possibility of message duplication due to unnecessary producer retries.</p>
数据类型：int
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：sasl.client.callback.handler.class
属性描述：The fully qualified name of a SASL client callback handler class that implements the AuthenticateCallbackHandler interface.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.jaas.config
属性描述：JAAS login context parameters for SASL connections in the format used by JAAS configuration files.
JAAS configuration file format is described <a href="http://docs.oracle.com/javase/8/docs/technotes/guides/security/jgss/tutorials/LoginConfigFile.html">here</a>.
The format for the value is: <code>loginModuleClass controlFlag (optionName=optionValue)*;</code>.
For brokers,
the config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.jaas.config=com.example.ScramLoginModule required;</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.kerberos.service.name
属性描述：The Kerberos principal name that Kafka runs as.
This can be defined either in Kafka's JAAS config or in Kafka's config.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.login.callback.handler.class
属性描述：The fully qualified name of a SASL login callback handler class that implements the AuthenticateCallbackHandler interface.
For brokers,
login callback handler config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.callback.handler.class=com.example.CustomScramLoginCallbackHandler</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.login.class
属性描述：The fully qualified name of a class that implements the Login interface.
For brokers,
login config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.class=com.example.CustomScramLogin</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.mechanism
属性描述：SASL mechanism used for client connections.
This may be any mechanism for which a security provider is available.
GSSAPI is the default mechanism.</p>
数据类型：string
默认的值：GSSAPI
有效的值：
重要等级：medium
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.url
属性描述：The OAuth/OIDC provider URL from which the provider's <a href="https://datatracker.ietf.org/doc/html/rfc7517#section-5">JWKS (JSON Web Key Set)</a> can be retrieved.
The URL can be HTTP(S)-based or file-based.
If the URL is HTTP(S)-based,
the JWKS data will be retrieved from the OAuth/OIDC provider via the configured URL on broker startup.
All then-current keys will be cached on the broker for incoming requests.
If an authentication request is received for a JWT that includes a "kid" header claim value that isn't yet in the cache,
the JWKS endpoint will be queried again on demand.
However,
the broker polls the URL every sasl.oauthbearer.jwks.endpoint.refresh.ms milliseconds to refresh the cache with any forthcoming keys before any JWT requests that include them are received.
If the URL is file-based,
the broker will load the JWKS file from a configured location on startup.
In the event that the JWT includes a "kid" header value that isn't in the JWKS file,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.oauthbearer.token.endpoint.url
属性描述：The URL for the OAuth/OIDC identity provider.
If the URL is HTTP(S)-based,
it is the issuer's token endpoint URL to which requests will be made to login based on the configuration in sasl.jaas.config.
If the URL is file-based,
it specifies a file containing an access token (in JWT serialized form) issued by the OAuth/OIDC identity provider to use for authorization.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：security.protocol
属性描述：Protocol used to communicate with brokers.
Valid values are: PLAINTEXT,
SSL,
SASL_PLAINTEXT,
SASL_SSL.</p>
数据类型：string
默认的值：PLAINTEXT
有效的值：
重要等级：medium
```

```
属性名称：send.buffer.bytes
属性描述：The size of the TCP send buffer (SO_SNDBUF) to use when sending data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：131072 (128 kibibytes)
有效的值：[-1,...]
重要等级：medium
```

```
属性名称：socket.connection.setup.timeout.max.ms
属性描述：The maximum amount of time the client will wait for the socket connection to be established.
The connection setup timeout will increase exponentially for each consecutive connection failure up to this maximum.
To avoid connection storms,
a randomization factor of 0.2 will be applied to the timeout resulting in a random range between 20% below and 20% above the computed value.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：
重要等级：medium
```

```
属性名称：socket.connection.setup.timeout.ms
属性描述：The amount of time the client will wait for the socket connection to be established.
If the connection is not built before the timeout elapses,
clients will close the socket channel.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：medium
```

```
属性名称：ssl.enabled.protocols
属性描述：The list of protocols enabled for SSL connections.
The default is 'TLSv1.2,TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
With the default value for Java 11,
clients and servers will prefer TLSv1.3 if both support it and fallback to TLSv1.2 otherwise (assuming both support at least TLSv1.2).
This default should be fine for most cases.
Also see the config documentation for `ssl.protocol`.</p>
数据类型：list
默认的值：TLSv1.2
有效的值：
重要等级：medium
```

```
属性名称：ssl.keystore.type
属性描述：The file format of the key store file.
This is optional for client.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
```

```
属性名称：ssl.protocol
属性描述：The SSL protocol used to generate the SSLContext.
The default is 'TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
This value should be fine for most use cases.
Allowed values in recent JVMs are 'TLSv1.2' and 'TLSv1.3'.
'TLS',
'TLSv1.1',
'SSL',
'SSLv2' and 'SSLv3' may be supported in older JVMs,
but their usage is discouraged due to known security vulnerabilities.
With the default value for this config and 'ssl.enabled.protocols',
clients will downgrade to 'TLSv1.2' if the server does not support 'TLSv1.3'.
If this config is set to 'TLSv1.2',
clients will not use 'TLSv1.3' even if it is one of the values in ssl.enabled.protocols and the server only supports 'TLSv1.3'.</p>
数据类型：string
默认的值：TLSv1.2
有效的值：
重要等级：medium
```

```
属性名称：ssl.provider
属性描述：The name of the security provider used for SSL connections.
Default value is the default security provider of the JVM.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：ssl.truststore.type
属性描述：The file format of the trust store file.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
```

```
属性名称：acks
属性描述：The number of acknowledgments the producer requires the leader to have received before considering a request complete.
This controls the  durability of records that are sent.
The following settings are allowed:  </p><ul> <li><code>acks=0</code> If set to zero then the producer will not wait for any acknowledgment from the server at all.
The record will be immediately added to the socket buffer and considered sent.
No guarantee can be made that the server has received the record in this case,
and the <code>retries</code> configuration will not take effect (as the client won't generally know of any failures).
The offset given back for each record will always be set to <code>-1</code>.
</li><li><code>acks=1</code> This will mean the leader will write the record to its local log but will respond without awaiting full acknowledgement from all followers.
In this case should the leader fail immediately after acknowledging the record but before the followers have replicated it then the record will be lost.
</li><li><code>acks=all</code> This means the leader will wait for the full set of in-sync replicas to acknowledge the record.
This guarantees that the record will not be lost as long as at least one in-sync replica remains alive.
This is the strongest available guarantee.
This is equivalent to the acks=-1 setting.</li></ul><p>Note that enabling idempotence requires this config value to be 'all'.
If conflicting configurations are set and idempotence is not explicitly enabled,
idempotence is disabled.</p>
数据类型：string
默认的值：all
有效的值：[all,
-1,
0,
1]
重要等级：low
```

```
属性名称：enable.idempotence
属性描述：When set to 'true',
the producer will ensure that exactly one copy of each message is written in the stream.
If 'false',
producer retries due to broker failures,
etc.,
may write duplicates of the retried message in the stream.
Note that enabling idempotence requires <code>max.in.flight.requests.per.connection</code> to be less than or equal to 5 (with message ordering preserved for any allowable value),
<code>retries</code> to be greater than 0,
and <code>acks</code> must be 'all'.
</p><p>Idempotence is enabled by default if no conflicting configurations are set.
If conflicting configurations are set and idempotence is not explicitly enabled,
idempotence is disabled.
If idempotence is explicitly enabled and conflicting configurations are set,
a <code>ConfigException</code> is thrown.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：low
```

```
属性名称：interceptor.classes
属性描述：A list of classes to use as interceptors.
Implementing the <code>org.apache.kafka.clients.producer.ProducerInterceptor</code> interface allows you to intercept (and possibly mutate) the records received by the producer before they are published to the Kafka cluster.
By default,
there are no interceptors.</p>
数据类型：list
默认的值：""
有效的值：non-null string
重要等级：low
```

```
属性名称：max.in.flight.requests.per.connection
属性描述：The maximum number of unacknowledged requests the client will send on a single connection before blocking.
Note that if this config is set to be greater than 1 and <code>enable.idempotence</code> is set to false,
there is a risk of message re-ordering after a failed send due to retries (i.e.,
if retries are enabled).
Additionally,
enabling idempotence requires this config value to be less than or equal to 5.
If conflicting configurations are set and idempotence is not explicitly enabled,
idempotence is disabled.</p>
数据类型：int
默认的值：5
有效的值：[1,...]
重要等级：low
```

```
属性名称：metadata.max.age.ms
属性描述：The period of time in milliseconds after which we force a refresh of metadata even if we haven't seen any partition leadership changes to proactively discover any new brokers or partitions.</p>
数据类型：long
默认的值：300000 (5 minutes)
有效的值：[0,...]
重要等级：low
```

```
属性名称：metadata.max.idle.ms
属性描述：Controls how long the producer will cache metadata for a topic that's idle.
If the elapsed time since a topic was last produced to exceeds the metadata idle duration,
then the topic's metadata is forgotten and the next access to it will force a metadata fetch request.</p>
数据类型：long
默认的值：300000 (5 minutes)
有效的值：[5000,...]
重要等级：low
```

```
属性名称：metric.reporters
属性描述：A list of classes to use as metrics reporters.
Implementing the <code>org.apache.kafka.common.metrics.MetricsReporter</code> interface allows plugging in classes that will be notified of new metric creation.
The JmxReporter is always included to register JMX statistics.</p>
数据类型：list
默认的值：""
有效的值：non-null string
重要等级：low
```

```
属性名称：metrics.num.samples
属性描述：The number of samples maintained to compute metrics.</p>
数据类型：int
默认的值：2
有效的值：[1,...]
重要等级：low
```

```
属性名称：metrics.recording.level
属性描述：The highest recording level for metrics.</p>
数据类型：string
默认的值：INFO
有效的值：[INFO,
DEBUG,
TRACE]
重要等级：low
```

```
属性名称：metrics.sample.window.ms
属性描述：The window of time a metrics sample is computed over.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：reconnect.backoff.max.ms
属性描述：The maximum amount of time in milliseconds to wait when reconnecting to a broker that has repeatedly failed to connect.
If provided,
the backoff per host will increase exponentially for each consecutive connection failure,
up to this maximum.
After calculating the backoff increase,
20% random jitter is added to avoid connection storms.</p>
数据类型：long
默认的值：1000 (1 second)
有效的值：[0,...]
重要等级：low
```

```
属性名称：reconnect.backoff.ms
属性描述：The base amount of time to wait before attempting to reconnect to a given host.
This avoids repeatedly connecting to a host in a tight loop.
This backoff applies to all connection attempts by the client to a broker.</p>
数据类型：long
默认的值：50
有效的值：[0,...]
重要等级：low
```

```
属性名称：retry.backoff.ms
属性描述：The amount of time to wait before attempting to retry a failed request to a given topic partition.
This avoids repeatedly sending requests in a tight loop under some failure scenarios.</p>
数据类型：long
默认的值：100
有效的值：[0,...]
重要等级：low
```

```
属性名称：sasl.kerberos.kinit.cmd
属性描述：Kerberos kinit command path.</p>
数据类型：string
默认的值：/usr/bin/kinit
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.min.time.before.relogin
属性描述：Login thread sleep time between refresh attempts.</p>
数据类型：long
默认的值：60000
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.ticket.renew.jitter
属性描述：Percentage of random jitter added to the renewal time.</p>
数据类型：double
默认的值：0.05
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.ticket.renew.window.factor
属性描述：Login thread will sleep until the specified window factor of time from last refresh to ticket's expiry has been reached,
at which time it will try to renew the ticket.</p>
数据类型：double
默认的值：0.8
有效的值：
重要等级：low
```

```
属性名称：sasl.login.connect.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider connection timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.login.read.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider read timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.login.refresh.buffer.seconds
属性描述：The amount of buffer time before credential expiration to maintain when refreshing a credential,
in seconds.
If a refresh would otherwise occur closer to expiration than the number of buffer seconds then the refresh will be moved up to maintain as much of the buffer time as possible.
Legal values are between 0 and 3600 (1 hour); a default value of  300 (5 minutes) is used if no value is specified.
This value and sasl.login.refresh.min.period.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：300
有效的值：[0,...,3600]
重要等级：low
```

```
属性名称：sasl.login.refresh.min.period.seconds
属性描述：The desired minimum time for the login refresh thread to wait before refreshing a credential,
in seconds.
Legal values are between 0 and 900 (15 minutes); a default value of 60 (1 minute) is used if no value is specified.
 This value and  sasl.login.refresh.buffer.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：60
有效的值：[0,...,900]
重要等级：low
```

```
属性名称：sasl.login.refresh.window.factor
属性描述：Login refresh thread will sleep until the specified window factor relative to the credential's lifetime has been reached,
at which time it will try to refresh the credential.
Legal values are between 0.5 (50%) and 1.0 (100%) inclusive; a default value of 0.8 (80%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.8
有效的值：[0.5,...,1.0]
重要等级：low
```

```
属性名称：sasl.login.refresh.window.jitter
属性描述：The maximum amount of random jitter relative to the credential's lifetime that is added to the login refresh thread's sleep time.
Legal values are between 0 and 0.25 (25%) inclusive; a default value of 0.05 (5%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.05
有效的值：[0.0,...,0.25]
重要等级：low
```

```
属性名称：sasl.login.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
```

```
属性名称：sasl.login.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.clock.skew.seconds
属性描述：The (optional) value in seconds to allow for differences between the time of the OAuth/OIDC identity provider and the broker.</p>
数据类型：int
默认的值：30
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.expected.audience
属性描述：The (optional) comma-delimited setting for the broker to use to verify that the JWT was issued for one of the expected audiences.
The JWT will be inspected for the standard OAuth "aud" claim and if this value is set,
the broker will match the value from JWT's "aud" claim  to see if there is an exact match.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.expected.issuer
属性描述：The (optional) setting for the broker to use to verify that the JWT was created by the expected issuer.
The JWT will be inspected for the standard OAuth "iss" claim and if this value is set,
the broker will match it exactly against what is in the JWT's "iss" claim.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.refresh.ms
属性描述：The (optional) value in milliseconds for the broker to wait between refreshing its JWKS (JSON Web Key Set) cache that contains the keys to verify the signature of the JWT.</p>
数据类型：long
默认的值：3600000 (1 hour)
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between attempts to retrieve the JWKS (JSON Web Key Set) from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between JWKS (JSON Web Key Set) retrieval attempts from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.scope.claim.name
属性描述：The OAuth claim for the scope is often named "scope",
but this (optional) setting can provide a different name to use for the scope included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：scope
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.sub.claim.name
属性描述：The OAuth claim for the subject is often named "sub",
but this (optional) setting can provide a different name to use for the subject included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：sub
有效的值：
重要等级：low
```

```
属性名称：security.providers
属性描述：A list of configurable creator classes each returning a provider implementing security algorithms.
These classes should implement the <code>org.apache.kafka.common.security.auth.SecurityProviderCreator</code> interface.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.cipher.suites
属性描述：A list of cipher suites.
This is a named combination of authentication,
encryption,
MAC and key exchange algorithm used to negotiate the security settings for a network connection using TLS or SSL network protocol.
By default all the available cipher suites are supported.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.endpoint.identification.algorithm
属性描述：The endpoint identification algorithm to validate server hostname using server certificate.
</p>
数据类型：string
默认的值：https
有效的值：
重要等级：low
```

```
属性名称：ssl.engine.factory.class
属性描述：The class of type org.apache.kafka.common.security.auth.SslEngineFactory to provide SSLEngine objects.
Default value is org.apache.kafka.common.security.ssl.DefaultSslEngineFactory</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.keymanager.algorithm
属性描述：The algorithm used by key manager factory for SSL connections.
Default value is the key manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：SunX509
有效的值：
重要等级：low
```

```
属性名称：ssl.secure.random.implementation
属性描述：The SecureRandom PRNG implementation to use for SSL cryptography operations.
</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.trustmanager.algorithm
属性描述：The algorithm used by trust manager factory for SSL connections.
Default value is the trust manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：PKIX
有效的值：
重要等级：low
```

```
属性名称：transaction.timeout.ms
属性描述：The maximum amount of time in ms that the transaction coordinator will wait for a transaction status update from the producer before proactively aborting the ongoing transaction.If this value is larger than the transaction.max.timeout.ms setting in the broker,
the request will fail with a <code>InvalidTxnTimeoutException</code> error.</p>
数据类型：int
默认的值：60000 (1 minute)
有效的值：
重要等级：low
```

```
属性名称：transactional.id
属性描述：The TransactionalId to use for transactional delivery.
This enables reliability semantics which span multiple producer sessions since it allows the client to guarantee that transactions using the same TransactionalId have been completed prior to starting any new transactions.
If no TransactionalId is provided,
then the producer is limited to idempotent delivery.
If a TransactionalId is configured,
<code>enable.idempotence</code> is implied.
By default the TransactionId is not configured,
which means transactions cannot be used.
Note that,
by default,
transactions require a cluster of at least three brokers which is the recommended setting for production; for development you can change this,
by adjusting broker setting <code>transaction.state.log.replication.factor</code>.</p>
数据类型：string
默认的值：null
有效的值：non-empty string
重要等级：low
</ul>
```

## 3.4 消费者配置（Consumer）

```
  Below is the configuration for the consumer:
  <ul class="config-list">
```

```
属性名称：key.deserializer
属性描述：Deserializer class for key that implements the <code>org.apache.kafka.common.serialization.Deserializer</code> interface.</p>
数据类型：class
默认的值：
有效的值：
重要等级：高
```

```
属性名称：value.deserializer
属性描述：Deserializer class for value that implements the <code>org.apache.kafka.common.serialization.Deserializer</code> interface.</p>
数据类型：class
默认的值：
有效的值：
重要等级：高
```

```
属性名称：bootstrap.servers
属性描述：A list of host/port pairs to use for establishing the initial connection to the Kafka cluster.
The client will make use of all servers irrespective of which servers are specified here for bootstrapping—this list only impacts the initial hosts used to discover the full set of servers.
This list should be in the form <code>host1:port1,host2:port2,...</code>.
Since these servers are just used for the initial connection to discover the full cluster membership (which may change dynamically),
this list need not contain the full set of servers (you may want more than one,
though,
in case a server is down).</p>
数据类型：list
默认的值：""
有效的值：non-null string
重要等级：高
```

```
属性名称：fetch.min.bytes
属性描述：The minimum amount of data the server should return for a fetch request.
If insufficient data is available the request will wait for that much data to accumulate before answering the request.
The default setting of 1 byte means that fetch requests are answered as soon as a single byte of data is available or the fetch request times out waiting for data to arrive.
Setting this to something greater than 1 will cause the server to wait for larger amounts of data to accumulate which can improve server throughput a bit at the cost of some additional latency.</p>
数据类型：int
默认的值：1
有效的值：[0,...]
重要等级：高
```

```
属性名称：group.id
属性描述：A unique string that identifies the consumer group this consumer belongs to.
This property is required if the consumer uses either the group management functionality by using <code>subscribe(topic)</code> or the Kafka-based offset management strategy.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：heartbeat.interval.ms
属性描述：The expected time between heartbeats to the consumer coordinator when using Kafka's group management facilities.
Heartbeats are used to ensure that the consumer's session stays active and to facilitate rebalancing when new consumers join or leave the group.
The value must be set lower than <code>session.timeout.ms</code>,
but typically should be set no higher than 1/3 of that value.
It can be adjusted even lower to control the expected time for normal rebalances.</p>
数据类型：int
默认的值：3000 (3 seconds)
有效的值：
重要等级：高
```

```
属性名称：max.partition.fetch.bytes
属性描述：The maximum amount of data per-partition the server will return.
Records are fetched in batches by the consumer.
If the first record batch in the first non-empty partition of the fetch is larger than this limit,
the batch will still be returned to ensure that the consumer can make progress.
The maximum record batch size accepted by the broker is defined via <code>message.max.bytes</code> (broker config) or <code>max.message.bytes</code> (topic config).
See fetch.max.bytes for limiting the consumer request size.</p>
数据类型：int
默认的值：1048576 (1 mebibyte)
有效的值：[0,...]
重要等级：高
```

```
属性名称：session.timeout.ms
属性描述：The timeout used to detect client failures when using Kafka's group management facility.
The client sends periodic heartbeats to indicate its liveness to the broker.
If no heartbeats are received by the broker before the expiration of this session timeout,
then the broker will remove this client from the group and initiate a rebalance.
Note that the value must be in the allowable range as configured in the broker configuration by <code>group.min.session.timeout.ms</code> and <code>group.max.session.timeout.ms</code>.</p>
数据类型：int
默认的值：45000 (45 seconds)
有效的值：
重要等级：高
```

```
属性名称：ssl.key.password
属性描述：The password of the private key in the key store file or the PEM key specified in `ssl.keystore.key'.
This is required for clients only if two-way authentication is configured.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.certificate.chain
属性描述：Certificate chain in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with a list of X.509 certificates</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.key
属性描述：Private key in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with PKCS#8 keys.
If the key is encrypted,
key password must be specified using 'ssl.key.password'</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.location
属性描述：The location of the key store file.
This is optional for client and can be used for two-way authentication for client.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.password
属性描述：The store password for the key store file.
This is optional for client and only needed if 'ssl.keystore.location' is configured.
Key store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.certificates
属性描述：Trusted certificates in the format specified by 'ssl.truststore.type'.
Default SSL engine factory supports only PEM format with X.509 certificates.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.location
属性描述：The location of the trust store file.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.password
属性描述：The password for the trust store file.
If a password is not set,
trust store file configured will still be used,
but integrity checking is disabled.
Trust store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：allow.auto.create.topics
属性描述：Allow automatic topic creation on the broker when subscribing to or assigning a topic.
A topic being subscribed to will be automatically created only if the broker allows for it using `auto.create.topics.enable` broker configuration.
This configuration must be set to `false` when using brokers older than 0.11.0</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：medium
```

```
属性名称：auto.offset.reset
属性描述：What to do when there is no initial offset in Kafka or if the current offset does not exist any more on the server (e.g.
because that data has been deleted): </p><ul><li>earliest: automatically reset the offset to the earliest offset</li><li>latest: automatically reset the offset to the latest offset</li><li>none: throw exception to the consumer if no previous offset is found for the consumer's group</li><li>anything else: throw exception to the consumer.</li></ul><p></p>
数据类型：string
默认的值：latest
有效的值：[latest,
earliest,
none]
重要等级：medium
```

```
属性名称：client.dns.lookup
属性描述：Controls how the client uses DNS lookups.
If set to <code>use_all_dns_ips</code>,
connect to each returned IP address in sequence until a successful connection is established.
After a disconnection,
the next IP is used.
Once all IPs have been used once,
the client resolves the IP(s) from the hostname again (both the JVM and the OS cache DNS name lookups,
however).
If set to <code>resolve_canonical_bootstrap_servers_only</code>,
resolve each bootstrap address into a list of canonical names.
After the bootstrap phase,
this behaves the same as <code>use_all_dns_ips</code>.</p>
数据类型：string
默认的值：use_all_dns_ips
有效的值：[use_all_dns_ips,
resolve_canonical_bootstrap_servers_only]
重要等级：medium
```

```
属性名称：connections.max.idle.ms
属性描述：Close idle connections after the number of milliseconds specified by this config.</p>
数据类型：long
默认的值：540000 (9 minutes)
有效的值：
重要等级：medium
```

```
属性名称：default.api.timeout.ms
属性描述：Specifies the timeout (in milliseconds) for client APIs.
This configuration is used as the default timeout for all client operations that do not specify a <code>timeout</code> parameter.</p>
数据类型：int
默认的值：60000 (1 minute)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：enable.auto.commit
属性描述：If true the consumer's offset will be periodically committed in the background.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：medium
```

```
属性名称：exclude.internal.topics
属性描述：Whether internal topics matching a subscribed pattern should be excluded from the subscription.
It is always possible to explicitly subscribe to an internal topic.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：medium
```

```
属性名称：fetch.max.bytes
属性描述：The maximum amount of data the server should return for a fetch request.
Records are fetched in batches by the consumer,
and if the first record batch in the first non-empty partition of the fetch is larger than this value,
the record batch will still be returned to ensure that the consumer can make progress.
As such,
this is not a absolute maximum.
The maximum record batch size accepted by the broker is defined via <code>message.max.bytes</code> (broker config) or <code>max.message.bytes</code> (topic config).
Note that the consumer performs multiple fetches in parallel.</p>
数据类型：int
默认的值：52428800 (50 mebibytes)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：group.instance.id
属性描述：A unique identifier of the consumer instance provided by the end user.
Only non-empty strings are permitted.
If set,
the consumer is treated as a static member,
which means that only one instance with this ID is allowed in the consumer group at any time.
This can be used in combination with a larger session timeout to avoid group rebalances caused by transient unavailability (e.g.
process restarts).
If not set,
the consumer will join the group as a dynamic member,
which is the traditional behavior.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：isolation.level
属性描述：Controls how to read messages written transactionally.
If set to <code>read_committed</code>,
consumer.poll() will only return transactional messages which have been committed.
If set to <code>read_uncommitted</code> (the default),
consumer.poll() will return all messages,
even transactional messages which have been aborted.
Non-transactional messages will be returned unconditionally in either mode.
</p><p>Messages will always be returned in offset order.
Hence,
in  <code>read_committed</code> mode,
consumer.poll() will only return messages up to the last stable offset (LSO),
which is the one less than the offset of the first open transaction.
In particular any messages appearing after messages belonging to ongoing transactions will be withheld until the relevant transaction has been completed.
As a result,
<code>read_committed</code> consumers will not be able to read up to the high watermark when there are in flight transactions.</p><p> Further,
when in <code>read_committed</code> the seekToEnd method will return the LSO</p><p></p>
数据类型：string
默认的值：read_uncommitted
有效的值：[read_committed,
read_uncommitted]
重要等级：medium
```

```
属性名称：max.poll.interval.ms
属性描述：The maximum delay between invocations of poll() when using consumer group management.
This places an upper bound on the amount of time that the consumer can be idle before fetching more records.
If poll() is not called before expiration of this timeout,
then the consumer is considered failed and the group will rebalance in order to reassign the partitions to another member.
For consumers using a non-null <code>group.instance.id</code> which reach this timeout,
partitions will not be immediately reassigned.
Instead,
the consumer will stop sending heartbeats and partitions will be reassigned after expiration of <code>session.timeout.ms</code>.
This mirrors the behavior of a static consumer which has shutdown.</p>
数据类型：int
默认的值：300000 (5 minutes)
有效的值：[1,...]
重要等级：medium
```

```
属性名称：max.poll.records
属性描述：The maximum number of records returned in a single call to poll().
Note,
that <code>max.poll.records</code> does not impact the underlying fetching behavior.
The consumer will cache the records from each fetch request and returns them incrementally from each poll.</p>
数据类型：int
默认的值：500
有效的值：[1,...]
重要等级：medium
```

```
属性名称：partition.assignment.strategy
属性描述：A list of class names or class types,
ordered by preference,
of supported partition assignment strategies that the client will use to distribute partition ownership amongst consumer instances when group management is used.
Available options are:</p><ul><li><code>org.apache.kafka.clients.consumer.RangeAssignor</code>: Assigns partitions on a per-topic basis.</li><li><code>org.apache.kafka.clients.consumer.RoundRobinAssignor</code>: Assigns partitions to consumers in a round-robin fashion.</li><li><code>org.apache.kafka.clients.consumer.StickyAssignor</code>: Guarantees an assignment that is maximally balanced while preserving as many existing partition assignments as possible.</li><li><code>org.apache.kafka.clients.consumer.CooperativeStickyAssignor</code>: Follows the same StickyAssignor logic,
but allows for cooperative rebalancing.</li></ul><p>The default assignor is [RangeAssignor,
CooperativeStickyAssignor],
which will use the RangeAssignor by default,
but allows upgrading to the CooperativeStickyAssignor with just a single rolling bounce that removes the RangeAssignor from the list.</p><p>Implementing the <code>org.apache.kafka.clients.consumer.ConsumerPartitionAssignor</code> interface allows you to plug in a custom assignment strategy.</p><p></p>
数据类型：list
默认的值：class org.apache.kafka.clients.consumer.RangeAssignor,class org.apache.kafka.clients.consumer.CooperativeStickyAssignor
有效的值：non-null string
重要等级：medium
```

```
属性名称：receive.buffer.bytes
属性描述：The size of the TCP receive buffer (SO_RCVBUF) to use when reading data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：65536 (64 kibibytes)
有效的值：[-1,...]
重要等级：medium
```

```
属性名称：request.timeout.ms
属性描述：The configuration controls the maximum amount of time the client will wait for the response of a request.
If the response is not received before the timeout elapses the client will resend the request if necessary or fail the request if retries are exhausted.</p>
数据类型：int
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：sasl.client.callback.handler.class
属性描述：The fully qualified name of a SASL client callback handler class that implements the AuthenticateCallbackHandler interface.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.jaas.config
属性描述：JAAS login context parameters for SASL connections in the format used by JAAS configuration files.
JAAS configuration file format is described <a href="http://docs.oracle.com/javase/8/docs/technotes/guides/security/jgss/tutorials/LoginConfigFile.html">here</a>.
The format for the value is: <code>loginModuleClass controlFlag (optionName=optionValue)*;</code>.
For brokers,
the config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.jaas.config=com.example.ScramLoginModule required;</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.kerberos.service.name
属性描述：The Kerberos principal name that Kafka runs as.
This can be defined either in Kafka's JAAS config or in Kafka's config.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.login.callback.handler.class
属性描述：The fully qualified name of a SASL login callback handler class that implements the AuthenticateCallbackHandler interface.
For brokers,
login callback handler config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.callback.handler.class=com.example.CustomScramLoginCallbackHandler</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.login.class
属性描述：The fully qualified name of a class that implements the Login interface.
For brokers,
login config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.class=com.example.CustomScramLogin</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.mechanism
属性描述：SASL mechanism used for client connections.
This may be any mechanism for which a security provider is available.
GSSAPI is the default mechanism.</p>
数据类型：string
默认的值：GSSAPI
有效的值：
重要等级：medium
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.url
属性描述：The OAuth/OIDC provider URL from which the provider's <a href="https://datatracker.ietf.org/doc/html/rfc7517#section-5">JWKS (JSON Web Key Set)</a> can be retrieved.
The URL can be HTTP(S)-based or file-based.
If the URL is HTTP(S)-based,
the JWKS data will be retrieved from the OAuth/OIDC provider via the configured URL on broker startup.
All then-current keys will be cached on the broker for incoming requests.
If an authentication request is received for a JWT that includes a "kid" header claim value that isn't yet in the cache,
the JWKS endpoint will be queried again on demand.
However,
the broker polls the URL every sasl.oauthbearer.jwks.endpoint.refresh.ms milliseconds to refresh the cache with any forthcoming keys before any JWT requests that include them are received.
If the URL is file-based,
the broker will load the JWKS file from a configured location on startup.
In the event that the JWT includes a "kid" header value that isn't in the JWKS file,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.oauthbearer.token.endpoint.url
属性描述：The URL for the OAuth/OIDC identity provider.
If the URL is HTTP(S)-based,
it is the issuer's token endpoint URL to which requests will be made to login based on the configuration in sasl.jaas.config.
If the URL is file-based,
it specifies a file containing an access token (in JWT serialized form) issued by the OAuth/OIDC identity provider to use for authorization.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：security.protocol
属性描述：Protocol used to communicate with brokers.
Valid values are: PLAINTEXT,
SSL,
SASL_PLAINTEXT,
SASL_SSL.</p>
数据类型：string
默认的值：PLAINTEXT
有效的值：
重要等级：medium
```

```
属性名称：send.buffer.bytes
属性描述：The size of the TCP send buffer (SO_SNDBUF) to use when sending data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：131072 (128 kibibytes)
有效的值：[-1,...]
重要等级：medium
```

```
属性名称：socket.connection.setup.timeout.max.ms
属性描述：The maximum amount of time the client will wait for the socket connection to be established.
The connection setup timeout will increase exponentially for each consecutive connection failure up to this maximum.
To avoid connection storms,
a randomization factor of 0.2 will be applied to the timeout resulting in a random range between 20% below and 20% above the computed value.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：
重要等级：medium
```

```
属性名称：socket.connection.setup.timeout.ms
属性描述：The amount of time the client will wait for the socket connection to be established.
If the connection is not built before the timeout elapses,
clients will close the socket channel.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：medium
```

```
属性名称：ssl.enabled.protocols
属性描述：The list of protocols enabled for SSL connections.
The default is 'TLSv1.2,TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
With the default value for Java 11,
clients and servers will prefer TLSv1.3 if both support it and fallback to TLSv1.2 otherwise (assuming both support at least TLSv1.2).
This default should be fine for most cases.
Also see the config documentation for `ssl.protocol`.</p>
数据类型：list
默认的值：TLSv1.2
有效的值：
重要等级：medium
```

```
属性名称：ssl.keystore.type
属性描述：The file format of the key store file.
This is optional for client.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
```

```
属性名称：ssl.protocol
属性描述：The SSL protocol used to generate the SSLContext.
The default is 'TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
This value should be fine for most use cases.
Allowed values in recent JVMs are 'TLSv1.2' and 'TLSv1.3'.
'TLS',
'TLSv1.1',
'SSL',
'SSLv2' and 'SSLv3' may be supported in older JVMs,
but their usage is discouraged due to known security vulnerabilities.
With the default value for this config and 'ssl.enabled.protocols',
clients will downgrade to 'TLSv1.2' if the server does not support 'TLSv1.3'.
If this config is set to 'TLSv1.2',
clients will not use 'TLSv1.3' even if it is one of the values in ssl.enabled.protocols and the server only supports 'TLSv1.3'.</p>
数据类型：string
默认的值：TLSv1.2
有效的值：
重要等级：medium
```

```
属性名称：ssl.provider
属性描述：The name of the security provider used for SSL connections.
Default value is the default security provider of the JVM.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：ssl.truststore.type
属性描述：The file format of the trust store file.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
```

```
属性名称：auto.commit.interval.ms
属性描述：The frequency in milliseconds that the consumer offsets are auto-committed to Kafka if <code>enable.auto.commit</code> is set to <code>true</code>.</p>
数据类型：int
默认的值：5000 (5 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：check.crcs
属性描述：Automatically check the CRC32 of the records consumed.
This ensures no on-the-wire or on-disk corruption to the messages occurred.
This check adds some overhead,
so it may be disabled in cases seeking extreme performance.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：low
```

```
属性名称：client.id
属性描述：An id string to pass to the server when making requests.
The purpose of this is to be able to track the source of requests beyond just ip/port by allowing a logical application name to be included in server-side request logging.</p>
数据类型：string
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：client.rack
属性描述：A rack identifier for this client.
This can be any string value which indicates where this client is physically located.
It corresponds with the broker config 'broker.rack'</p>
数据类型：string
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：fetch.max.wait.ms
属性描述：The maximum amount of time the server will block before answering the fetch request if there isn't sufficient data to immediately satisfy the requirement given by fetch.min.bytes.</p>
数据类型：int
默认的值：500
有效的值：[0,...]
重要等级：low
```

```
属性名称：interceptor.classes
属性描述：A list of classes to use as interceptors.
Implementing the <code>org.apache.kafka.clients.consumer.ConsumerInterceptor</code> interface allows you to intercept (and possibly mutate) records received by the consumer.
By default,
there are no interceptors.</p>
数据类型：list
默认的值：""
有效的值：non-null string
重要等级：low
```

```
属性名称：metadata.max.age.ms
属性描述：The period of time in milliseconds after which we force a refresh of metadata even if we haven't seen any partition leadership changes to proactively discover any new brokers or partitions.</p>
数据类型：long
默认的值：300000 (5 minutes)
有效的值：[0,...]
重要等级：low
```

```
属性名称：metric.reporters
属性描述：A list of classes to use as metrics reporters.
Implementing the <code>org.apache.kafka.common.metrics.MetricsReporter</code> interface allows plugging in classes that will be notified of new metric creation.
The JmxReporter is always included to register JMX statistics.</p>
数据类型：list
默认的值：""
有效的值：non-null string
重要等级：low
```

```
属性名称：metrics.num.samples
属性描述：The number of samples maintained to compute metrics.</p>
数据类型：int
默认的值：2
有效的值：[1,...]
重要等级：low
```

```
属性名称：metrics.recording.level
属性描述：The highest recording level for metrics.</p>
数据类型：string
默认的值：INFO
有效的值：[INFO,
DEBUG,
TRACE]
重要等级：low
```

```
属性名称：metrics.sample.window.ms
属性描述：The window of time a metrics sample is computed over.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：reconnect.backoff.max.ms
属性描述：The maximum amount of time in milliseconds to wait when reconnecting to a broker that has repeatedly failed to connect.
If provided,
the backoff per host will increase exponentially for each consecutive connection failure,
up to this maximum.
After calculating the backoff increase,
20% random jitter is added to avoid connection storms.</p>
数据类型：long
默认的值：1000 (1 second)
有效的值：[0,...]
重要等级：low
```

```
属性名称：reconnect.backoff.ms
属性描述：The base amount of time to wait before attempting to reconnect to a given host.
This avoids repeatedly connecting to a host in a tight loop.
This backoff applies to all connection attempts by the client to a broker.</p>
数据类型：long
默认的值：50
有效的值：[0,...]
重要等级：low
```

```
属性名称：retry.backoff.ms
属性描述：The amount of time to wait before attempting to retry a failed request to a given topic partition.
This avoids repeatedly sending requests in a tight loop under some failure scenarios.</p>
数据类型：long
默认的值：100
有效的值：[0,...]
重要等级：low
```

```
属性名称：sasl.kerberos.kinit.cmd
属性描述：Kerberos kinit command path.</p>
数据类型：string
默认的值：/usr/bin/kinit
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.min.time.before.relogin
属性描述：Login thread sleep time between refresh attempts.</p>
数据类型：long
默认的值：60000
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.ticket.renew.jitter
属性描述：Percentage of random jitter added to the renewal time.</p>
数据类型：double
默认的值：0.05
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.ticket.renew.window.factor
属性描述：Login thread will sleep until the specified window factor of time from last refresh to ticket's expiry has been reached,
at which time it will try to renew the ticket.</p>
数据类型：double
默认的值：0.8
有效的值：
重要等级：low
```

```
属性名称：sasl.login.connect.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider connection timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.login.read.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider read timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.login.refresh.buffer.seconds
属性描述：The amount of buffer time before credential expiration to maintain when refreshing a credential,
in seconds.
If a refresh would otherwise occur closer to expiration than the number of buffer seconds then the refresh will be moved up to maintain as much of the buffer time as possible.
Legal values are between 0 and 3600 (1 hour); a default value of  300 (5 minutes) is used if no value is specified.
This value and sasl.login.refresh.min.period.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：300
有效的值：[0,...,3600]
重要等级：low
```

```
属性名称：sasl.login.refresh.min.period.seconds
属性描述：The desired minimum time for the login refresh thread to wait before refreshing a credential,
in seconds.
Legal values are between 0 and 900 (15 minutes); a default value of 60 (1 minute) is used if no value is specified.
 This value and  sasl.login.refresh.buffer.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：60
有效的值：[0,...,900]
重要等级：low
```

```
属性名称：sasl.login.refresh.window.factor
属性描述：Login refresh thread will sleep until the specified window factor relative to the credential's lifetime has been reached,
at which time it will try to refresh the credential.
Legal values are between 0.5 (50%) and 1.0 (100%) inclusive; a default value of 0.8 (80%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.8
有效的值：[0.5,...,1.0]
重要等级：low
```

```
属性名称：sasl.login.refresh.window.jitter
属性描述：The maximum amount of random jitter relative to the credential's lifetime that is added to the login refresh thread's sleep time.
Legal values are between 0 and 0.25 (25%) inclusive; a default value of 0.05 (5%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.05
有效的值：[0.0,...,0.25]
重要等级：low
```

```
属性名称：sasl.login.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
```

```
属性名称：sasl.login.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.clock.skew.seconds
属性描述：The (optional) value in seconds to allow for differences between the time of the OAuth/OIDC identity provider and the broker.</p>
数据类型：int
默认的值：30
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.expected.audience
属性描述：The (optional) comma-delimited setting for the broker to use to verify that the JWT was issued for one of the expected audiences.
The JWT will be inspected for the standard OAuth "aud" claim and if this value is set,
the broker will match the value from JWT's "aud" claim  to see if there is an exact match.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.expected.issuer
属性描述：The (optional) setting for the broker to use to verify that the JWT was created by the expected issuer.
The JWT will be inspected for the standard OAuth "iss" claim and if this value is set,
the broker will match it exactly against what is in the JWT's "iss" claim.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.refresh.ms
属性描述：The (optional) value in milliseconds for the broker to wait between refreshing its JWKS (JSON Web Key Set) cache that contains the keys to verify the signature of the JWT.</p>
数据类型：long
默认的值：3600000 (1 hour)
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between attempts to retrieve the JWKS (JSON Web Key Set) from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between JWKS (JSON Web Key Set) retrieval attempts from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.scope.claim.name
属性描述：The OAuth claim for the scope is often named "scope",
but this (optional) setting can provide a different name to use for the scope included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：scope
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.sub.claim.name
属性描述：The OAuth claim for the subject is often named "sub",
but this (optional) setting can provide a different name to use for the subject included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：sub
有效的值：
重要等级：low
```

```
属性名称：security.providers
属性描述：A list of configurable creator classes each returning a provider implementing security algorithms.
These classes should implement the <code>org.apache.kafka.common.security.auth.SecurityProviderCreator</code> interface.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.cipher.suites
属性描述：A list of cipher suites.
This is a named combination of authentication,
encryption,
MAC and key exchange algorithm used to negotiate the security settings for a network connection using TLS or SSL network protocol.
By default all the available cipher suites are supported.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.endpoint.identification.algorithm
属性描述：The endpoint identification algorithm to validate server hostname using server certificate.
</p>
数据类型：string
默认的值：https
有效的值：
重要等级：low
```

```
属性名称：ssl.engine.factory.class
属性描述：The class of type org.apache.kafka.common.security.auth.SslEngineFactory to provide SSLEngine objects.
Default value is org.apache.kafka.common.security.ssl.DefaultSslEngineFactory</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.keymanager.algorithm
属性描述：The algorithm used by key manager factory for SSL connections.
Default value is the key manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：SunX509
有效的值：
重要等级：low
```

```
属性名称：ssl.secure.random.implementation
属性描述：The SecureRandom PRNG implementation to use for SSL cryptography operations.
</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.trustmanager.algorithm
属性描述：The algorithm used by trust manager factory for SSL connections.
Default value is the trust manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：PKIX
有效的值：
重要等级：low
</ul>
```

## 3.5 连接配置（Connect）

```
  Below is the configuration of the Kafka Connect framework.
  <ul class="config-list">
```

```
属性名称：config.storage.topic
属性描述：The name of the Kafka topic where connector configurations are stored</p>
数据类型：string
默认的值：
有效的值：
重要等级：高
```

```
属性名称：group.id
属性描述：A unique string that identifies the Connect cluster group this worker belongs to.</p>
数据类型：string
默认的值：
有效的值：
重要等级：高
```

```
属性名称：key.converter
属性描述：Converter class used to convert between Kafka Connect format and the serialized form that is written to Kafka.
This controls the format of the keys in messages written to or read from Kafka,
and since this is independent of connectors it allows any connector to work with any serialization format.
Examples of common formats include JSON and Avro.</p>
数据类型：class
默认的值：
有效的值：
重要等级：高
```

```
属性名称：offset.storage.topic
属性描述：The name of the Kafka topic where connector offsets are stored</p>
数据类型：string
默认的值：
有效的值：
重要等级：高
```

```
属性名称：status.storage.topic
属性描述：The name of the Kafka topic where connector and task status are stored</p>
数据类型：string
默认的值：
有效的值：
重要等级：高
```

```
属性名称：value.converter
属性描述：Converter class used to convert between Kafka Connect format and the serialized form that is written to Kafka.
This controls the format of the values in messages written to or read from Kafka,
and since this is independent of connectors it allows any connector to work with any serialization format.
Examples of common formats include JSON and Avro.</p>
数据类型：class
默认的值：
有效的值：
重要等级：高
```

```
属性名称：bootstrap.servers
属性描述：A list of host/port pairs to use for establishing the initial connection to the Kafka cluster.
The client will make use of all servers irrespective of which servers are specified here for bootstrapping—this list only impacts the initial hosts used to discover the full set of servers.
This list should be in the form <code>host1:port1,host2:port2,...</code>.
Since these servers are just used for the initial connection to discover the full cluster membership (which may change dynamically),
this list need not contain the full set of servers (you may want more than one,
though,
in case a server is down).</p>
数据类型：list
默认的值：localhost:9092
有效的值：
重要等级：高
```

```
属性名称：heartbeat.interval.ms
属性描述：The expected time between heartbeats to the group coordinator when using Kafka's group management facilities.
Heartbeats are used to ensure that the worker's session stays active and to facilitate rebalancing when new members join or leave the group.
The value must be set lower than <code>session.timeout.ms</code>,
but typically should be set no higher than 1/3 of that value.
It can be adjusted even lower to control the expected time for normal rebalances.</p>
数据类型：int
默认的值：3000 (3 seconds)
有效的值：
重要等级：高
```

```
属性名称：rebalance.timeout.ms
属性描述：The maximum allowed time for each worker to join the group once a rebalance has begun.
This is basically a limit on the amount of time needed for all tasks to flush any pending data and commit offsets.
If the timeout is exceeded,
then the worker will be removed from the group,
which will cause offset commit failures.</p>
数据类型：int
默认的值：60000 (1 minute)
有效的值：
重要等级：高
```

```
属性名称：session.timeout.ms
属性描述：The timeout used to detect worker failures.
The worker sends periodic heartbeats to indicate its liveness to the broker.
If no heartbeats are received by the broker before the expiration of this session timeout,
then the broker will remove the worker from the group and initiate a rebalance.
Note that the value must be in the allowable range as configured in the broker configuration by <code>group.min.session.timeout.ms</code> and <code>group.max.session.timeout.ms</code>.</p>
数据类型：int
默认的值：10000 (10 seconds)
有效的值：
重要等级：高
```

```
属性名称：ssl.key.password
属性描述：The password of the private key in the key store file or the PEM key specified in `ssl.keystore.key'.
This is required for clients only if two-way authentication is configured.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.certificate.chain
属性描述：Certificate chain in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with a list of X.509 certificates</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.key
属性描述：Private key in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with PKCS#8 keys.
If the key is encrypted,
key password must be specified using 'ssl.key.password'</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.location
属性描述：The location of the key store file.
This is optional for client and can be used for two-way authentication for client.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.password
属性描述：The store password for the key store file.
This is optional for client and only needed if 'ssl.keystore.location' is configured.
Key store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.certificates
属性描述：Trusted certificates in the format specified by 'ssl.truststore.type'.
Default SSL engine factory supports only PEM format with X.509 certificates.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.location
属性描述：The location of the trust store file.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.password
属性描述：The password for the trust store file.
If a password is not set,
trust store file configured will still be used,
but integrity checking is disabled.
Trust store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：client.dns.lookup
属性描述：Controls how the client uses DNS lookups.
If set to <code>use_all_dns_ips</code>,
connect to each returned IP address in sequence until a successful connection is established.
After a disconnection,
the next IP is used.
Once all IPs have been used once,
the client resolves the IP(s) from the hostname again (both the JVM and the OS cache DNS name lookups,
however).
If set to <code>resolve_canonical_bootstrap_servers_only</code>,
resolve each bootstrap address into a list of canonical names.
After the bootstrap phase,
this behaves the same as <code>use_all_dns_ips</code>.</p>
数据类型：string
默认的值：use_all_dns_ips
有效的值：[use_all_dns_ips,
resolve_canonical_bootstrap_servers_only]
重要等级：medium
```

```
属性名称：connections.max.idle.ms
属性描述：Close idle connections after the number of milliseconds specified by this config.</p>
数据类型：long
默认的值：540000 (9 minutes)
有效的值：
重要等级：medium
```

```
属性名称：connector.client.config.override.policy
属性描述：Class name or alias of implementation of <code>ConnectorClientConfigOverridePolicy</code>.
Defines what client configurations can be overriden by the connector.
The default implementation is `All`,
meaning connector configurations can override all client properties.
The other possible policies in the framework include `None` to disallow connectors from overriding client properties,
and `Principal` to allow connectors to override only client principals.</p>
数据类型：string
默认的值：All
有效的值：
重要等级：medium
```

```
属性名称：receive.buffer.bytes
属性描述：The size of the TCP receive buffer (SO_RCVBUF) to use when reading data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：32768 (32 kibibytes)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：request.timeout.ms
属性描述：The configuration controls the maximum amount of time the client will wait for the response of a request.
If the response is not received before the timeout elapses the client will resend the request if necessary or fail the request if retries are exhausted.</p>
数据类型：int
默认的值：40000 (40 seconds)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：sasl.client.callback.handler.class
属性描述：The fully qualified name of a SASL client callback handler class that implements the AuthenticateCallbackHandler interface.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.jaas.config
属性描述：JAAS login context parameters for SASL connections in the format used by JAAS configuration files.
JAAS configuration file format is described <a href="http://docs.oracle.com/javase/8/docs/technotes/guides/security/jgss/tutorials/LoginConfigFile.html">here</a>.
The format for the value is: <code>loginModuleClass controlFlag (optionName=optionValue)*;</code>.
For brokers,
the config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.jaas.config=com.example.ScramLoginModule required;</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.kerberos.service.name
属性描述：The Kerberos principal name that Kafka runs as.
This can be defined either in Kafka's JAAS config or in Kafka's config.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.login.callback.handler.class
属性描述：The fully qualified name of a SASL login callback handler class that implements the AuthenticateCallbackHandler interface.
For brokers,
login callback handler config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.callback.handler.class=com.example.CustomScramLoginCallbackHandler</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.login.class
属性描述：The fully qualified name of a class that implements the Login interface.
For brokers,
login config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.class=com.example.CustomScramLogin</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.mechanism
属性描述：SASL mechanism used for client connections.
This may be any mechanism for which a security provider is available.
GSSAPI is the default mechanism.</p>
数据类型：string
默认的值：GSSAPI
有效的值：
重要等级：medium
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.url
属性描述：The OAuth/OIDC provider URL from which the provider's <a href="https://datatracker.ietf.org/doc/html/rfc7517#section-5">JWKS (JSON Web Key Set)</a> can be retrieved.
The URL can be HTTP(S)-based or file-based.
If the URL is HTTP(S)-based,
the JWKS data will be retrieved from the OAuth/OIDC provider via the configured URL on broker startup.
All then-current keys will be cached on the broker for incoming requests.
If an authentication request is received for a JWT that includes a "kid" header claim value that isn't yet in the cache,
the JWKS endpoint will be queried again on demand.
However,
the broker polls the URL every sasl.oauthbearer.jwks.endpoint.refresh.ms milliseconds to refresh the cache with any forthcoming keys before any JWT requests that include them are received.
If the URL is file-based,
the broker will load the JWKS file from a configured location on startup.
In the event that the JWT includes a "kid" header value that isn't in the JWKS file,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.oauthbearer.token.endpoint.url
属性描述：The URL for the OAuth/OIDC identity provider.
If the URL is HTTP(S)-based,
it is the issuer's token endpoint URL to which requests will be made to login based on the configuration in sasl.jaas.config.
If the URL is file-based,
it specifies a file containing an access token (in JWT serialized form) issued by the OAuth/OIDC identity provider to use for authorization.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：security.protocol
属性描述：Protocol used to communicate with brokers.
Valid values are: PLAINTEXT,
SSL,
SASL_PLAINTEXT,
SASL_SSL.</p>
数据类型：string
默认的值：PLAINTEXT
有效的值：
重要等级：medium
```

```
属性名称：send.buffer.bytes
属性描述：The size of the TCP send buffer (SO_SNDBUF) to use when sending data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：131072 (128 kibibytes)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：ssl.enabled.protocols
属性描述：The list of protocols enabled for SSL connections.
The default is 'TLSv1.2,TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
With the default value for Java 11,
clients and servers will prefer TLSv1.3 if both support it and fallback to TLSv1.2 otherwise (assuming both support at least TLSv1.2).
This default should be fine for most cases.
Also see the config documentation for `ssl.protocol`.</p>
数据类型：list
默认的值：TLSv1.2
有效的值：
重要等级：medium
```

```
属性名称：ssl.keystore.type
属性描述：The file format of the key store file.
This is optional for client.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
```

```
属性名称：ssl.protocol
属性描述：The SSL protocol used to generate the SSLContext.
The default is 'TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
This value should be fine for most use cases.
Allowed values in recent JVMs are 'TLSv1.2' and 'TLSv1.3'.
'TLS',
'TLSv1.1',
'SSL',
'SSLv2' and 'SSLv3' may be supported in older JVMs,
but their usage is discouraged due to known security vulnerabilities.
With the default value for this config and 'ssl.enabled.protocols',
clients will downgrade to 'TLSv1.2' if the server does not support 'TLSv1.3'.
If this config is set to 'TLSv1.2',
clients will not use 'TLSv1.3' even if it is one of the values in ssl.enabled.protocols and the server only supports 'TLSv1.3'.</p>
数据类型：string
默认的值：TLSv1.2
有效的值：
重要等级：medium
```

```
属性名称：ssl.provider
属性描述：The name of the security provider used for SSL connections.
Default value is the default security provider of the JVM.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：ssl.truststore.type
属性描述：The file format of the trust store file.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
```

```
属性名称：worker.sync.timeout.ms
属性描述：When the worker is out of sync with other workers and needs to resynchronize configurations,
wait up to this amount of time before giving up,
leaving the group,
and waiting a backoff period before rejoining.</p>
数据类型：int
默认的值：3000 (3 seconds)
有效的值：
重要等级：medium
```

```
属性名称：worker.unsync.backoff.ms
属性描述：When the worker is out of sync with other workers and  fails to catch up within worker.sync.timeout.ms,
leave the Connect cluster for this long before rejoining.</p>
数据类型：int
默认的值：300000 (5 minutes)
有效的值：
重要等级：medium
```

```
属性名称：access.control.allow.methods
属性描述：Sets the methods supported for cross origin requests by setting the Access-Control-Allow-Methods header.
The default value of the Access-Control-Allow-Methods header allows cross origin requests for GET,
POST and HEAD.</p>
数据类型：string
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：access.control.allow.origin
属性描述：Value to set the Access-Control-Allow-Origin header to for REST API requests.To enable cross origin access,
set this to the domain of the application that should be permitted to access the API,
or '*' to allow access from any domain.
The default value only allows access from the domain of the REST API.</p>
数据类型：string
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：admin.listeners
属性描述：List of comma-separated URIs the Admin REST API will listen on.
The supported protocols are HTTP and HTTPS.
An empty or blank string will disable this feature.
The default behavior is to use the regular listener (specified by the 'listeners' property).</p>
数据类型：list
默认的值：null
有效的值：List of comma-separated URLs,
ex: http://localhost:8080,https://localhost:8443.
重要等级：low
```

```
属性名称：client.id
属性描述：An id string to pass to the server when making requests.
The purpose of this is to be able to track the source of requests beyond just ip/port by allowing a logical application name to be included in server-side request logging.</p>
数据类型：string
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：config.providers
属性描述：Comma-separated names of <code>ConfigProvider</code> classes,
loaded and used in the order specified.
Implementing the interface  <code>ConfigProvider</code> allows you to replace variable references in connector configurations,
such as for externalized secrets.
</p>
数据类型：list
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：config.storage.replication.factor
属性描述：Replication factor used when creating the configuration storage topic</p>
数据类型：short
默认的值：3
有效的值：Positive number not larger than the number of brokers in the Kafka cluster,
or -1 to use the broker's default
重要等级：low
```

```
属性名称：connect.protocol
属性描述：Compatibility mode for Kafka Connect Protocol</p>
数据类型：string
默认的值：sessioned
有效的值：[eager,
compatible,
sessioned]
重要等级：low
```

```
属性名称：header.converter
属性描述：HeaderConverter class used to convert between Kafka Connect format and the serialized form that is written to Kafka.
This controls the format of the header values in messages written to or read from Kafka,
and since this is independent of connectors it allows any connector to work with any serialization format.
Examples of common formats include JSON and Avro.
By default,
the SimpleHeaderConverter is used to serialize header values to strings and deserialize them by inferring the schemas.</p>
数据类型：class
默认的值：org.apache.kafka.connect.storage.SimpleHeaderConverter
有效的值：
重要等级：low
```

```
属性名称：inter.worker.key.generation.algorithm
属性描述：The algorithm to use for generating internal request keys</p>
数据类型：string
默认的值：HmacSHA256
有效的值：Any KeyGenerator algorithm supported by the worker JVM
重要等级：low
```

```
属性名称：inter.worker.key.size
属性描述：The size of the key to use for signing internal requests,
in bits.
If null,
the default key size for the key generation algorithm will be used.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：inter.worker.key.ttl.ms
属性描述：The TTL of generated session keys used for internal request validation (in milliseconds)</p>
数据类型：int
默认的值：3600000 (1 hour)
有效的值：[0,...,2147483647]
重要等级：low
```

```
属性名称：inter.worker.signature.algorithm
属性描述：The algorithm used to sign internal requests</p>
数据类型：string
默认的值：HmacSHA256
有效的值：Any MAC algorithm supported by the worker JVM
重要等级：low
```

```
属性名称：inter.worker.verification.algorithms
属性描述：A list of permitted algorithms for verifying internal requests</p>
数据类型：list
默认的值：HmacSHA256
有效的值：A list of one or more MAC algorithms,
each supported by the worker JVM
重要等级：low
```

```
属性名称：listeners
属性描述：List of comma-separated URIs the REST API will listen on.
The supported protocols are HTTP and HTTPS.<br> Specify hostname as 0.0.0.0 to bind to all interfaces.<br> Leave hostname empty to bind to default interface.<br> Examples of legal listener lists: HTTP://myhost:8083,HTTPS://myhost:8084</p>
数据类型：list
默认的值：http://:8083
有效的值：List of comma-separated URLs,
ex: http://localhost:8080,https://localhost:8443.
重要等级：low
```

```
属性名称：metadata.max.age.ms
属性描述：The period of time in milliseconds after which we force a refresh of metadata even if we haven't seen any partition leadership changes to proactively discover any new brokers or partitions.</p>
数据类型：long
默认的值：300000 (5 minutes)
有效的值：[0,...]
重要等级：low
```

```
属性名称：metric.reporters
属性描述：A list of classes to use as metrics reporters.
Implementing the <code>org.apache.kafka.common.metrics.MetricsReporter</code> interface allows plugging in classes that will be notified of new metric creation.
The JmxReporter is always included to register JMX statistics.</p>
数据类型：list
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：metrics.num.samples
属性描述：The number of samples maintained to compute metrics.</p>
数据类型：int
默认的值：2
有效的值：[1,...]
重要等级：low
```

```
属性名称：metrics.recording.level
属性描述：The highest recording level for metrics.</p>
数据类型：string
默认的值：INFO
有效的值：[INFO,
DEBUG]
重要等级：low
```

```
属性名称：metrics.sample.window.ms
属性描述：The window of time a metrics sample is computed over.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：offset.flush.interval.ms
属性描述：Interval at which to try committing offsets for tasks.</p>
数据类型：long
默认的值：60000 (1 minute)
有效的值：
重要等级：low
```

```
属性名称：offset.flush.timeout.ms
属性描述：Maximum number of milliseconds to wait for records to flush and partition offset data to be committed to offset storage before cancelling the process and restoring the offset data to be committed in a future attempt.</p>
数据类型：long
默认的值：5000 (5 seconds)
有效的值：
重要等级：low
```

```
属性名称：offset.storage.partitions
属性描述：The number of partitions used when creating the offset storage topic</p>
数据类型：int
默认的值：25
有效的值：Positive number,
or -1 to use the broker's default
重要等级：low
```

```
属性名称：offset.storage.replication.factor
属性描述：Replication factor used when creating the offset storage topic</p>
数据类型：short
默认的值：3
有效的值：Positive number not larger than the number of brokers in the Kafka cluster,
or -1 to use the broker's default
重要等级：low
```

```
属性名称：plugin.path
属性描述：List of paths separated by commas (,) that contain plugins (connectors,
converters,
transformations).
The list should consist of top level directories that include any combination of: <br>a) directories immediately containing jars with plugins and their dependencies<br>b) uber-jars with plugins and their dependencies<br>c) directories immediately containing the package directory structure of classes of plugins and their dependencies<br>Note: symlinks will be followed to discover dependencies or plugins.<br>Examples: plugin.path=/usr/local/share/java,/usr/local/share/kafka/plugins,/opt/connectors<br>Do not use config provider variables in this property,
since the raw path is used by the worker's scanner before config providers are initialized and used to replace variables.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：reconnect.backoff.max.ms
属性描述：The maximum amount of time in milliseconds to wait when reconnecting to a broker that has repeatedly failed to connect.
If provided,
the backoff per host will increase exponentially for each consecutive connection failure,
up to this maximum.
After calculating the backoff increase,
20% random jitter is added to avoid connection storms.</p>
数据类型：long
默认的值：1000 (1 second)
有效的值：[0,...]
重要等级：low
```

```
属性名称：reconnect.backoff.ms
属性描述：The base amount of time to wait before attempting to reconnect to a given host.
This avoids repeatedly connecting to a host in a tight loop.
This backoff applies to all connection attempts by the client to a broker.</p>
数据类型：long
默认的值：50
有效的值：[0,...]
重要等级：low
```

```
属性名称：response.http.headers.config
属性描述：Rules for REST API HTTP response headers</p>
数据类型：string
默认的值：""
有效的值：Comma-separated header rules,
where each header rule is of the form '[action] [header name]:[header value]' and optionally surrounded by double quotes if any part of a header rule contains a comma
重要等级：low
```

```
属性名称：rest.advertised.host.name
属性描述：If this is set,
this is the hostname that will be given out to other workers to connect to.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：rest.advertised.listener
属性描述：Sets the advertised listener (HTTP or HTTPS) which will be given to other workers to use.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：rest.advertised.port
属性描述：If this is set,
this is the port that will be given out to other workers to connect to.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：rest.extension.classes
属性描述：Comma-separated names of <code>ConnectRestExtension</code> classes,
loaded and called in the order specified.
Implementing the interface  <code>ConnectRestExtension</code> allows you to inject into Connect's REST API user defined resources like filters.
Typically used to add custom capability like logging,
security,
etc.
</p>
数据类型：list
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：retry.backoff.ms
属性描述：The amount of time to wait before attempting to retry a failed request to a given topic partition.
This avoids repeatedly sending requests in a tight loop under some failure scenarios.</p>
数据类型：long
默认的值：100
有效的值：[0,...]
重要等级：low
```

```
属性名称：sasl.kerberos.kinit.cmd
属性描述：Kerberos kinit command path.</p>
数据类型：string
默认的值：/usr/bin/kinit
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.min.time.before.relogin
属性描述：Login thread sleep time between refresh attempts.</p>
数据类型：long
默认的值：60000
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.ticket.renew.jitter
属性描述：Percentage of random jitter added to the renewal time.</p>
数据类型：double
默认的值：0.05
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.ticket.renew.window.factor
属性描述：Login thread will sleep until the specified window factor of time from last refresh to ticket's expiry has been reached,
at which time it will try to renew the ticket.</p>
数据类型：double
默认的值：0.8
有效的值：
重要等级：low
```

```
属性名称：sasl.login.connect.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider connection timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.login.read.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider read timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.login.refresh.buffer.seconds
属性描述：The amount of buffer time before credential expiration to maintain when refreshing a credential,
in seconds.
If a refresh would otherwise occur closer to expiration than the number of buffer seconds then the refresh will be moved up to maintain as much of the buffer time as possible.
Legal values are between 0 and 3600 (1 hour); a default value of  300 (5 minutes) is used if no value is specified.
This value and sasl.login.refresh.min.period.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：300
有效的值：[0,...,3600]
重要等级：low
```

```
属性名称：sasl.login.refresh.min.period.seconds
属性描述：The desired minimum time for the login refresh thread to wait before refreshing a credential,
in seconds.
Legal values are between 0 and 900 (15 minutes); a default value of 60 (1 minute) is used if no value is specified.
 This value and  sasl.login.refresh.buffer.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：60
有效的值：[0,...,900]
重要等级：low
```

```
属性名称：sasl.login.refresh.window.factor
属性描述：Login refresh thread will sleep until the specified window factor relative to the credential's lifetime has been reached,
at which time it will try to refresh the credential.
Legal values are between 0.5 (50%) and 1.0 (100%) inclusive; a default value of 0.8 (80%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.8
有效的值：[0.5,...,1.0]
重要等级：low
```

```
属性名称：sasl.login.refresh.window.jitter
属性描述：The maximum amount of random jitter relative to the credential's lifetime that is added to the login refresh thread's sleep time.
Legal values are between 0 and 0.25 (25%) inclusive; a default value of 0.05 (5%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.05
有效的值：[0.0,...,0.25]
重要等级：low
```

```
属性名称：sasl.login.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
```

```
属性名称：sasl.login.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.clock.skew.seconds
属性描述：The (optional) value in seconds to allow for differences between the time of the OAuth/OIDC identity provider and the broker.</p>
数据类型：int
默认的值：30
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.expected.audience
属性描述：The (optional) comma-delimited setting for the broker to use to verify that the JWT was issued for one of the expected audiences.
The JWT will be inspected for the standard OAuth "aud" claim and if this value is set,
the broker will match the value from JWT's "aud" claim  to see if there is an exact match.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.expected.issuer
属性描述：The (optional) setting for the broker to use to verify that the JWT was created by the expected issuer.
The JWT will be inspected for the standard OAuth "iss" claim and if this value is set,
the broker will match it exactly against what is in the JWT's "iss" claim.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.refresh.ms
属性描述：The (optional) value in milliseconds for the broker to wait between refreshing its JWKS (JSON Web Key Set) cache that contains the keys to verify the signature of the JWT.</p>
数据类型：long
默认的值：3600000 (1 hour)
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between attempts to retrieve the JWKS (JSON Web Key Set) from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between JWKS (JSON Web Key Set) retrieval attempts from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.scope.claim.name
属性描述：The OAuth claim for the scope is often named "scope",
but this (optional) setting can provide a different name to use for the scope included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：scope
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.sub.claim.name
属性描述：The OAuth claim for the subject is often named "sub",
but this (optional) setting can provide a different name to use for the subject included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：sub
有效的值：
重要等级：low
```

```
属性名称：scheduled.rebalance.max.delay.ms
属性描述：The maximum delay that is scheduled in order to wait for the return of one or more departed workers before rebalancing and reassigning their connectors and tasks to the group.
During this period the connectors and tasks of the departed workers remain unassigned</p>
数据类型：int
默认的值：300000 (5 minutes)
有效的值：[0,...,2147483647]
重要等级：low
```

```
属性名称：socket.connection.setup.timeout.max.ms
属性描述：The maximum amount of time the client will wait for the socket connection to be established.
The connection setup timeout will increase exponentially for each consecutive connection failure up to this maximum.
To avoid connection storms,
a randomization factor of 0.2 will be applied to the timeout resulting in a random range between 20% below and 20% above the computed value.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：socket.connection.setup.timeout.ms
属性描述：The amount of time the client will wait for the socket connection to be established.
If the connection is not built before the timeout elapses,
clients will close the socket channel.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：ssl.cipher.suites
属性描述：A list of cipher suites.
This is a named combination of authentication,
encryption,
MAC and key exchange algorithm used to negotiate the security settings for a network connection using TLS or SSL network protocol.
By default all the available cipher suites are supported.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.client.auth
属性描述：Configures kafka broker to request client authentication.
The following settings are common:  </p><ul> <li><code>ssl.client.auth=required</code> If set to required client authentication is required.
</li><li><code>ssl.client.auth=requested</code> This means client authentication is optional.
unlike required,
if this option is set client can choose not to provide authentication information about itself </li><li><code>ssl.client.auth=none</code> This means client authentication is not needed.</li></ul><p></p>
数据类型：string
默认的值：none
有效的值：
重要等级：low
```

```
属性名称：ssl.endpoint.identification.algorithm
属性描述：The endpoint identification algorithm to validate server hostname using server certificate.
</p>
数据类型：string
默认的值：https
有效的值：
重要等级：low
```

```
属性名称：ssl.engine.factory.class
属性描述：The class of type org.apache.kafka.common.security.auth.SslEngineFactory to provide SSLEngine objects.
Default value is org.apache.kafka.common.security.ssl.DefaultSslEngineFactory</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.keymanager.algorithm
属性描述：The algorithm used by key manager factory for SSL connections.
Default value is the key manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：SunX509
有效的值：
重要等级：low
```

```
属性名称：ssl.secure.random.implementation
属性描述：The SecureRandom PRNG implementation to use for SSL cryptography operations.
</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.trustmanager.algorithm
属性描述：The algorithm used by trust manager factory for SSL connections.
Default value is the trust manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：PKIX
有效的值：
重要等级：low
```

```
属性名称：status.storage.partitions
属性描述：The number of partitions used when creating the status storage topic</p>
数据类型：int
默认的值：5
有效的值：Positive number,
or -1 to use the broker's default
重要等级：low
```

```
属性名称：status.storage.replication.factor
属性描述：Replication factor used when creating the status storage topic</p>
数据类型：short
默认的值：3
有效的值：Positive number not larger than the number of brokers in the Kafka cluster,
or -1 to use the broker's default
重要等级：low
```

```
属性名称：task.shutdown.graceful.timeout.ms
属性描述：Amount of time to wait for tasks to shutdown gracefully.
This is the total amount of time,
not per task.
All task have shutdown triggered,
then they are waited on sequentially.</p>
数据类型：long
默认的值：5000 (5 seconds)
有效的值：
重要等级：low
```

```
属性名称：topic.creation.enable
属性描述：Whether to allow automatic creation of topics used by source connectors,
when source connectors are configured with `topic.creation.` properties.
Each task will use an admin client to create its topics and will not depend on the Kafka brokers to create topics automatically.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：low
```

```
属性名称：topic.tracking.allow.reset
属性描述：If set to true,
it allows user requests to reset the set of active topics per connector.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：low
```

```
属性名称：topic.tracking.enable
属性描述：Enable tracking the set of active topics per connector during runtime.</p>
数据类型：boolean
默认的值：true
有效的值：
重要等级：low
</ul>
```

### 3.5.1 发送端连接器配置（Source Connector）

```
  Below is the configuration of a source connector.
  <ul class="config-list">
```

```
属性名称：name
属性描述：Globally unique name to use for this connector.</p>
数据类型：string
默认的值：
有效的值：non-empty string without ISO control characters
重要等级：高
```

```
属性名称：connector.class
属性描述：Name or alias of the class for this connector.
Must be a subclass of org.apache.kafka.connect.connector.Connector.
If the connector is org.apache.kafka.connect.file.FileStreamSinkConnector,
you can either specify this full name,
 or use "FileStreamSink" or "FileStreamSinkConnector" to make the configuration a bit shorter</p>
数据类型：string
默认的值：
有效的值：
重要等级：高
```

```
属性名称：tasks.max
属性描述：Maximum number of tasks to use for this connector.</p>
数据类型：int
默认的值：1
有效的值：[1,...]
重要等级：高
```

```
属性名称：key.converter
属性描述：Converter class used to convert between Kafka Connect format and the serialized form that is written to Kafka.
This controls the format of the keys in messages written to or read from Kafka,
and since this is independent of connectors it allows any connector to work with any serialization format.
Examples of common formats include JSON and Avro.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：value.converter
属性描述：Converter class used to convert between Kafka Connect format and the serialized form that is written to Kafka.
This controls the format of the values in messages written to or read from Kafka,
and since this is independent of connectors it allows any connector to work with any serialization format.
Examples of common formats include JSON and Avro.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：header.converter
属性描述：HeaderConverter class used to convert between Kafka Connect format and the serialized form that is written to Kafka.
This controls the format of the header values in messages written to or read from Kafka,
and since this is independent of connectors it allows any connector to work with any serialization format.
Examples of common formats include JSON and Avro.
By default,
the SimpleHeaderConverter is used to serialize header values to strings and deserialize them by inferring the schemas.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：config.action.reload
属性描述：The action that Connect should take on the connector when changes in external configuration providers result in a change in the connector's configuration properties.
A value of 'none' indicates that Connect will do nothing.
A value of 'restart' indicates that Connect should restart/reload the connector with the updated configuration properties.The restart may actually be scheduled in the future if the external configuration provider indicates that a configuration value will expire in the future.</p>
数据类型：string
默认的值：restart
有效的值：[none,
restart]
重要等级：low
```

```
属性名称：transforms
属性描述：Aliases for the transformations to be applied to records.</p>
数据类型：list
默认的值：""
有效的值：non-null string,
unique transformation aliases
重要等级：low
```

```
属性名称：predicates
属性描述：Aliases for the predicates used by transformations.</p>
数据类型：list
默认的值：""
有效的值：non-null string,
unique predicate aliases
重要等级：low
```

```
属性名称：errors.retry.timeout
属性描述：The maximum duration in milliseconds that a failed operation will be reattempted.
The default is 0,
which means no retries will be attempted.
Use -1 for infinite retries.</p>
数据类型：long
默认的值：0
有效的值：
重要等级：medium
```

```
属性名称：errors.retry.delay.max.ms
属性描述：The maximum duration in milliseconds between consecutive retry attempts.
Jitter will be added to the delay once this limit is reached to prevent thundering herd issues.</p>
数据类型：long
默认的值：60000 (1 minute)
有效的值：
重要等级：medium
```

```
属性名称：errors.tolerance
属性描述：Behavior for tolerating errors during connector operation.
'none' is the default value and signals that any error will result in an immediate connector task failure; 'all' changes the behavior to skip over problematic records.</p>
数据类型：string
默认的值：none
有效的值：[none,
all]
重要等级：medium
```

```
属性名称：errors.log.enable
属性描述：If true,
write each error and the details of the failed operation and problematic record to the Connect application log.
This is 'false' by default,
so that only errors that are not tolerated are reported.</p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：medium
```

```
属性名称：errors.log.include.messages
属性描述：Whether to include in the log the Connect record that resulted in a failure.For sink records,
the topic,
partition,
offset,
and timestamp will be logged.
For source records,
the key and value (and their schemas),
all headers,
and the timestamp,
Kafka topic,
Kafka partition,
source partition,
and source offset will be logged.
This is 'false' by default,
which will prevent record keys,
values,
and headers from being written to log files.</p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：medium
```

```
属性名称：topic.creation.groups
属性描述：Groups of configurations for topics created by source connectors</p>
数据类型：list
默认的值：""
有效的值：non-null string,
unique topic creation groups
重要等级：low
</ul>
```

## 3.5.2 接收端连接器配置（Sink Connector）

```
  Below is the configuration of a sink connector.
  <ul class="config-list">
```

```
属性名称：name
属性描述：Globally unique name to use for this connector.</p>
数据类型：string
默认的值：
有效的值：non-empty string without ISO control characters
重要等级：高
```

```
属性名称：connector.class
属性描述：Name or alias of the class for this connector.
Must be a subclass of org.apache.kafka.connect.connector.Connector.
If the connector is org.apache.kafka.connect.file.FileStreamSinkConnector,
you can either specify this full name,
 or use "FileStreamSink" or "FileStreamSinkConnector" to make the configuration a bit shorter</p>
数据类型：string
默认的值：
有效的值：
重要等级：高
```

```
属性名称：tasks.max
属性描述：Maximum number of tasks to use for this connector.</p>
数据类型：int
默认的值：1
有效的值：[1,...]
重要等级：高
```

```
属性名称：topics
属性描述：List of topics to consume,
separated by commas</p>
数据类型：list
默认的值：""
有效的值：
重要等级：高
```

```
属性名称：topics.regex
属性描述：Regular expression giving topics to consume.
Under the hood,
the regex is compiled to a <code>java.util.regex.Pattern</code>.
Only one of topics or topics.regex should be specified.</p>
数据类型：string
默认的值：""
有效的值：valid regex
重要等级：高
```

```
属性名称：key.converter
属性描述：Converter class used to convert between Kafka Connect format and the serialized form that is written to Kafka.
This controls the format of the keys in messages written to or read from Kafka,
and since this is independent of connectors it allows any connector to work with any serialization format.
Examples of common formats include JSON and Avro.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：value.converter
属性描述：Converter class used to convert between Kafka Connect format and the serialized form that is written to Kafka.
This controls the format of the values in messages written to or read from Kafka,
and since this is independent of connectors it allows any connector to work with any serialization format.
Examples of common formats include JSON and Avro.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：header.converter
属性描述：HeaderConverter class used to convert between Kafka Connect format and the serialized form that is written to Kafka.
This controls the format of the header values in messages written to or read from Kafka,
and since this is independent of connectors it allows any connector to work with any serialization format.
Examples of common formats include JSON and Avro.
By default,
the SimpleHeaderConverter is used to serialize header values to strings and deserialize them by inferring the schemas.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：config.action.reload
属性描述：The action that Connect should take on the connector when changes in external configuration providers result in a change in the connector's configuration properties.
A value of 'none' indicates that Connect will do nothing.
A value of 'restart' indicates that Connect should restart/reload the connector with the updated configuration properties.The restart may actually be scheduled in the future if the external configuration provider indicates that a configuration value will expire in the future.</p>
数据类型：string
默认的值：restart
有效的值：[none,
restart]
重要等级：low
```

```
属性名称：transforms
属性描述：Aliases for the transformations to be applied to records.</p>
数据类型：list
默认的值：""
有效的值：non-null string,
unique transformation aliases
重要等级：low
```

```
属性名称：predicates
属性描述：Aliases for the predicates used by transformations.</p>
数据类型：list
默认的值：""
有效的值：non-null string,
unique predicate aliases
重要等级：low
```

```
属性名称：errors.retry.timeout
属性描述：The maximum duration in milliseconds that a failed operation will be reattempted.
The default is 0,
which means no retries will be attempted.
Use -1 for infinite retries.</p>
数据类型：long
默认的值：0
有效的值：
重要等级：medium
```

```
属性名称：errors.retry.delay.max.ms
属性描述：The maximum duration in milliseconds between consecutive retry attempts.
Jitter will be added to the delay once this limit is reached to prevent thundering herd issues.</p>
数据类型：long
默认的值：60000 (1 minute)
有效的值：
重要等级：medium
```

```
属性名称：errors.tolerance
属性描述：Behavior for tolerating errors during connector operation.
'none' is the default value and signals that any error will result in an immediate connector task failure; 'all' changes the behavior to skip over problematic records.</p>
数据类型：string
默认的值：none
有效的值：[none,
all]
重要等级：medium
```

```
属性名称：errors.log.enable
属性描述：If true,
write each error and the details of the failed operation and problematic record to the Connect application log.
This is 'false' by default,
so that only errors that are not tolerated are reported.</p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：medium
```

```
属性名称：errors.log.include.messages
属性描述：Whether to include in the log the Connect record that resulted in a failure.For sink records,
the topic,
partition,
offset,
and timestamp will be logged.
For source records,
the key and value (and their schemas),
all headers,
and the timestamp,
Kafka topic,
Kafka partition,
source partition,
and source offset will be logged.
This is 'false' by default,
which will prevent record keys,
values,
and headers from being written to log files.</p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：medium
```

```
属性名称：errors.deadletterqueue.topic.name
属性描述：The name of the topic to be used as the dead letter queue (DLQ) for messages that result in an error when processed by this sink connector,
or its transformations or converters.
The topic name is blank by default,
which means that no messages are to be recorded in the DLQ.</p>
数据类型：string
默认的值：""
有效的值：
重要等级：medium
```

```
属性名称：errors.deadletterqueue.topic.replication.factor
属性描述：Replication factor used to create the dead letter queue topic when it doesn't already exist.</p>
数据类型：short
默认的值：3
有效的值：
重要等级：medium
```

```
属性名称：errors.deadletterqueue.context.headers.enable
属性描述：If true,
add headers containing error context to the messages written to the dead letter queue.
To avoid clashing with headers from the original record,
all error context header keys,
all error context header keys will start with <code>__connect.errors.</code></p>
数据类型：boolean
默认的值：false
有效的值：
重要等级：medium
</ul>
```

## 3.6 流配置（Streams）

```
  Below is the configuration of the Kafka Streams client library.
  <ul class="config-list">
```

```
属性名称：application.id
属性描述：An identifier for the stream processing application.
Must be unique within the Kafka cluster.
It is used as 1) the default client-id prefix,
2) the group-id for membership management,
3) the changelog topic prefix.</p>
数据类型：string
默认的值：
有效的值：
重要等级：高
```

```
属性名称：bootstrap.servers
属性描述：A list of host/port pairs to use for establishing the initial connection to the Kafka cluster.
The client will make use of all servers irrespective of which servers are specified here for bootstrapping—this list only impacts the initial hosts used to discover the full set of servers.
This list should be in the form <code>host1:port1,host2:port2,...</code>.
Since these servers are just used for the initial connection to discover the full cluster membership (which may change dynamically),
this list need not contain the full set of servers (you may want more than one,
though,
in case a server is down).</p>
数据类型：list
默认的值：
有效的值：
重要等级：高
```

```
属性名称：num.standby.replicas
属性描述：The number of standby replicas for each task.</p>
数据类型：int
默认的值：0
有效的值：
重要等级：高
```

```
属性名称：state.dir
属性描述：Directory location for state store.
This path must be unique for each streams instance sharing the same underlying filesystem.</p>
数据类型：string
默认的值：/var/folders/ds/dq10m26j2kjcypywn_lt0b0m0000gn/T//kafka-streams
有效的值：
重要等级：高
```

```
属性名称：acceptable.recovery.lag
属性描述：The maximum acceptable lag (number of offsets to catch up) for a client to be considered caught-up enough to receive an active task assignment.
Upon assignment,
it will still restore the rest of the changelog before processing.
To avoid a pause in processing during rebalances,
this config should correspond to a recovery time of well under a minute for a given workload.
Must be at least 0.</p>
数据类型：long
默认的值：10000
有效的值：[0,...]
重要等级：medium
```

```
属性名称：cache.max.bytes.buffering
属性描述：Maximum number of memory bytes to be used for buffering across all threads</p>
数据类型：long
默认的值：10485760
有效的值：[0,...]
重要等级：medium
```

```
属性名称：client.id
属性描述：An ID prefix string used for the client IDs of internal consumer,
producer and restore-consumer,
with pattern '<client.id>-StreamThread-<threadsequencenumber>-<consumer|producer|restore-consumer>'.</consumer|producer|restore-consumer></threadsequencenumber></client.id></p>
数据类型：string
默认的值：""
有效的值：
重要等级：medium
```

```
属性名称：default.deserialization.exception.handler
属性描述：Exception handling class that implements the <code>org.apache.kafka.streams.errors.DeserializationExceptionHandler</code> interface.</p>
数据类型：class
默认的值：org.apache.kafka.streams.errors.LogAndFailExceptionHandler
有效的值：
重要等级：medium
```

```
属性名称：default.key.serde
属性描述： Default serializer / deserializer class for key that implements the <code>org.apache.kafka.common.serialization.Serde</code> interface.
Note when windowed serde class is used,
one needs to set the inner serde class that implements the <code>org.apache.kafka.common.serialization.Serde</code> interface via 'default.windowed.key.serde.inner' or 'default.windowed.value.serde.inner' as well</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：default.list.key.serde.inner
属性描述：Default inner class of list serde for key that implements the <code>org.apache.kafka.common.serialization.Serde</code> interface.
This configuration will be read if and only if <code>default.key.serde</code> configuration is set to <code>org.apache.kafka.common.serialization.Serdes.ListSerde</code></p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：default.list.key.serde.type
属性描述：Default class for key that implements the <code>java.util.List</code> interface.
This configuration will be read if and only if <code>default.key.serde</code> configuration is set to <code>org.apache.kafka.common.serialization.Serdes.ListSerde</code> Note when list serde class is used,
one needs to set the inner serde class that implements the <code>org.apache.kafka.common.serialization.Serde</code> interface via 'default.list.key.serde.inner'</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：default.list.value.serde.inner
属性描述：Default inner class of list serde for value that implements the <code>org.apache.kafka.common.serialization.Serde</code> interface.
This configuration will be read if and only if <code>default.value.serde</code> configuration is set to <code>org.apache.kafka.common.serialization.Serdes.ListSerde</code></p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：default.list.value.serde.type
属性描述：Default class for value that implements the <code>java.util.List</code> interface.
This configuration will be read if and only if <code>default.value.serde</code> configuration is set to <code>org.apache.kafka.common.serialization.Serdes.ListSerde</code> Note when list serde class is used,
one needs to set the inner serde class that implements the <code>org.apache.kafka.common.serialization.Serde</code> interface via 'default.list.value.serde.inner'</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：default.production.exception.handler
属性描述：Exception handling class that implements the <code>org.apache.kafka.streams.errors.ProductionExceptionHandler</code> interface.</p>
数据类型：class
默认的值：org.apache.kafka.streams.errors.DefaultProductionExceptionHandler
有效的值：
重要等级：medium
```

```
属性名称：default.timestamp.extractor
属性描述：Default timestamp extractor class that implements the <code>org.apache.kafka.streams.processor.TimestampExtractor</code> interface.</p>
数据类型：class
默认的值：org.apache.kafka.streams.processor.FailOnInvalidTimestamp
有效的值：
重要等级：medium
```

```
属性名称：default.value.serde
属性描述：Default serializer / deserializer class for value that implements the <code>org.apache.kafka.common.serialization.Serde</code> interface.
Note when windowed serde class is used,
one needs to set the inner serde class that implements the <code>org.apache.kafka.common.serialization.Serde</code> interface via 'default.windowed.key.serde.inner' or 'default.windowed.value.serde.inner' as well</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：max.task.idle.ms
属性描述：This config controls whether joins and merges may produce out-of-order results.
The config value is the maximum amount of time in milliseconds a stream task will stay idle when it is fully caught up on some (but not all) input partitions to wait for producers to send additional records and avoid potential out-of-order record processing across multiple input streams.
The default (zero) does not wait for producers to send more records,
but it does wait to fetch data that is already present on the brokers.
This default means that for records that are already present on the brokers,
Streams will process them in timestamp order.
Set to -1 to disable idling entirely and process any locally available data,
even though doing so may produce out-of-order processing.</p>
数据类型：long
默认的值：0
有效的值：
重要等级：medium
```

```
属性名称：max.warmup.replicas
属性描述：The maximum number of warmup replicas (extra standbys beyond the configured num.standbys) that can be assigned at once for the purpose of keeping  the task available on one instance while it is warming up on another instance it has been reassigned to.
Used to throttle how much extra broker  traffic and cluster state can be used for high availability.
Must be at least 1.</p>
数据类型：int
默认的值：2
有效的值：[1,...]
重要等级：medium
```

```
属性名称：num.stream.threads
属性描述：The number of threads to execute stream processing.</p>
数据类型：int
默认的值：1
有效的值：
重要等级：medium
```

```
属性名称：processing.guarantee
属性描述：The processing guarantee that should be used.
Possible values are <code>at_least_once</code> (default) and <code>exactly_once_v2</code> (requires brokers version 2.5 or higher).
Deprecated options are <code>exactly_once</code> (requires brokers version 0.11.0 or higher) and <code>exactly_once_beta</code> (requires brokers version 2.5 or higher).
Note that exactly-once processing requires a cluster of at least three brokers by default what is the recommended setting for production; for development you can change this,
by adjusting broker setting <code>transaction.state.log.replication.factor</code> and <code>transaction.state.log.min.isr</code>.</p>
数据类型：string
默认的值：at_least_once
有效的值：[at_least_once,
exactly_once,
exactly_once_beta,
exactly_once_v2]
重要等级：medium
```

```
属性名称：rack.aware.assignment.tags
属性描述：List of client tag keys used to distribute standby replicas across Kafka Streams instances.
When configured,
Kafka Streams will make a best-effort to distribute the standby tasks over each client tag dimension.</p>
数据类型：list
默认的值：""
有效的值：List containing maximum of 5 elements
重要等级：medium
```

```
属性名称：replication.factor
属性描述：The replication factor for change log topics and repartition topics created by the stream processing application.
The default of <code>-1</code> (meaning: use broker default replication factor) requires broker version 2.4 or newer</p>
数据类型：int
默认的值：-1
有效的值：
重要等级：medium
```

```
属性名称：security.protocol
属性描述：Protocol used to communicate with brokers.
Valid values are: PLAINTEXT,
SSL,
SASL_PLAINTEXT,
SASL_SSL.</p>
数据类型：string
默认的值：PLAINTEXT
有效的值：
重要等级：medium
```

```
属性名称：task.timeout.ms
属性描述：The maximum amount of time in milliseconds a task might stall due to internal errors and retries until an error is raised.
For a timeout of 0ms,
a task would raise an error for the first internal error.
For any timeout larger than 0ms,
a task will retry at least once before an error is raised.</p>
数据类型：long
默认的值：300000 (5 minutes)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：topology.optimization
属性描述：A configuration telling Kafka Streams if it should optimize the topology,
disabled by default</p>
数据类型：string
默认的值：none
有效的值：[none,
all]
重要等级：medium
```

```
属性名称：application.server
属性描述：A host:port pair pointing to a user-defined endpoint that can be used for state store discovery and interactive queries on this KafkaStreams instance.</p>
数据类型：string
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：buffered.records.per.partition
属性描述：Maximum number of records to buffer per partition.</p>
数据类型：int
默认的值：1000
有效的值：
重要等级：low
```

```
属性名称：built.in.metrics.version
属性描述：Version of the built-in metrics to use.</p>
数据类型：string
默认的值：latest
有效的值：[latest]
重要等级：low
```

```
属性名称：commit.interval.ms
属性描述：The frequency in milliseconds with which to save the position of the processor.
(Note,
if <code>processing.guarantee</code> is set to <code>exactly_once_v2</code>,
<code>exactly_once</code>,the default value is <code>100</code>,
otherwise the default value is <code>30000</code>.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：connections.max.idle.ms
属性描述：Close idle connections after the number of milliseconds specified by this config.</p>
数据类型：long
默认的值：540000 (9 minutes)
有效的值：
重要等级：low
```

```
属性名称：default.dsl.store
属性描述：The default state store type used by DSL operators.</p>
数据类型：string
默认的值：rocksDB
有效的值：[rocksDB,
in_memory]
重要等级：low
```

```
属性名称：metadata.max.age.ms
属性描述：The period of time in milliseconds after which we force a refresh of metadata even if we haven't seen any partition leadership changes to proactively discover any new brokers or partitions.</p>
数据类型：long
默认的值：300000 (5 minutes)
有效的值：[0,...]
重要等级：low
```

```
属性名称：metric.reporters
属性描述：A list of classes to use as metrics reporters.
Implementing the <code>org.apache.kafka.common.metrics.MetricsReporter</code> interface allows plugging in classes that will be notified of new metric creation.
The JmxReporter is always included to register JMX statistics.</p>
数据类型：list
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：metrics.num.samples
属性描述：The number of samples maintained to compute metrics.</p>
数据类型：int
默认的值：2
有效的值：[1,...]
重要等级：low
```

```
属性名称：metrics.recording.level
属性描述：The highest recording level for metrics.</p>
数据类型：string
默认的值：INFO
有效的值：[INFO,
DEBUG,
TRACE]
重要等级：low
```

```
属性名称：metrics.sample.window.ms
属性描述：The window of time a metrics sample is computed over.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：poll.ms
属性描述：The amount of time in milliseconds to block waiting for input.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
```

```
属性名称：probing.rebalance.interval.ms
属性描述：The maximum time in milliseconds to wait before triggering a rebalance to probe for warmup replicas that have finished warming up and are ready to become active.
Probing rebalances will continue to be triggered until the assignment is balanced.
Must be at least 1 minute.</p>
数据类型：long
默认的值：600000 (10 minutes)
有效的值：[60000,...]
重要等级：low
```

```
属性名称：receive.buffer.bytes
属性描述：The size of the TCP receive buffer (SO_RCVBUF) to use when reading data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：32768 (32 kibibytes)
有效的值：[-1,...]
重要等级：low
```

```
属性名称：reconnect.backoff.max.ms
属性描述：The maximum amount of time in milliseconds to wait when reconnecting to a broker that has repeatedly failed to connect.
If provided,
the backoff per host will increase exponentially for each consecutive connection failure,
up to this maximum.
After calculating the backoff increase,
20% random jitter is added to avoid connection storms.</p>
数据类型：long
默认的值：1000 (1 second)
有效的值：[0,...]
重要等级：low
```

```
属性名称：reconnect.backoff.ms
属性描述：The base amount of time to wait before attempting to reconnect to a given host.
This avoids repeatedly connecting to a host in a tight loop.
This backoff applies to all connection attempts by the client to a broker.</p>
数据类型：long
默认的值：50
有效的值：[0,...]
重要等级：low
```

```
属性名称：repartition.purge.interval.ms
属性描述：The frequency in milliseconds with which to delete fully consumed records from repartition topics.
Purging will occur after at least this value since the last purge,
but may be delayed until later.
(Note,
unlike <code>commit.interval.ms</code>,
the default for this value remains unchanged when <code>processing.guarantee</code> is set to <code>exactly_once_v2</code>).</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：request.timeout.ms
属性描述：The configuration controls the maximum amount of time the client will wait for the response of a request.
If the response is not received before the timeout elapses the client will resend the request if necessary or fail the request if retries are exhausted.</p>
数据类型：int
默认的值：40000 (40 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：retries
属性描述：Setting a value greater than zero will cause the client to resend any request that fails with a potentially transient error.
It is recommended to set the value to either zero or `MAX_VALUE` and use corresponding timeout parameters to control how long a client should retry a request.</p>
数据类型：int
默认的值：0
有效的值：[0,...,2147483647]
重要等级：low
```

```
属性名称：retry.backoff.ms
属性描述：The amount of time to wait before attempting to retry a failed request to a given topic partition.
This avoids repeatedly sending requests in a tight loop under some failure scenarios.</p>
数据类型：long
默认的值：100
有效的值：[0,...]
重要等级：low
```

```
属性名称：rocksdb.config.setter
属性描述：A Rocks DB config setter class or class name that implements the <code>org.apache.kafka.streams.state.RocksDBConfigSetter</code> interface</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：send.buffer.bytes
属性描述：The size of the TCP send buffer (SO_SNDBUF) to use when sending data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：131072 (128 kibibytes)
有效的值：[-1,...]
重要等级：low
```

```
属性名称：state.cleanup.delay.ms
属性描述：The amount of time in milliseconds to wait before deleting state when a partition has migrated.
Only state directories that have not been modified for at least <code>state.cleanup.delay.ms</code> will be removed</p>
数据类型：long
默认的值：600000 (10 minutes)
有效的值：
重要等级：low
```

```
属性名称：upgrade.from
属性描述：Allows upgrading in a backward compatible way.
This is needed when upgrading from [0.10.0,
1.1] to 2.0+,
or when upgrading from [2.0,
2.3] to 2.4+.
When upgrading from 2.4 to a newer version it is not required to specify this config.
Default is `null`.
Accepted values are "0.10.0",
0.10.1,
0.10.2,
0.11.0,
1.0,
1.1,
2.0,
2.1,
2.2,
2.3 (for upgrading from the corresponding old version).</p>
数据类型：string
默认的值：null
有效的值：[null,
0.10.0,
0.10.1,
0.10.2,
0.11.0,
1.0,
1.1,
2.0,
2.1,
2.2,
2.3]
重要等级：low
```

```
属性名称：window.size.ms
属性描述：Sets window size for the deserializer in order to calculate window end times.</p>
数据类型：long
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：windowed.inner.class.serde
属性描述： Default serializer / deserializer for the inner class of a windowed record.
Must implement the " +<br>        "<code>org.apache.kafka.common.serialization.Serde</code> interface..
Note that setting this config in KafkaStreams application would result in an error as it is meant to be used only from Plain consumer client.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：windowstore.changelog.additional.retention.ms
属性描述：Added to a windows maintainMs to ensure data is not deleted from the log prematurely.
Allows for clock drift.
Default is 1 day</p>
数据类型：long
默认的值：86400000 (1 day)
有效的值：
重要等级：low
</ul>
```

## 3.7 管理员配置（Admin）

```
  Below is the configuration of the Kafka Admin client library.
  <ul class="config-list">
```

```
属性名称：bootstrap.servers
属性描述：A list of host/port pairs to use for establishing the initial connection to the Kafka cluster.
The client will make use of all servers irrespective of which servers are specified here for bootstrapping—this list only impacts the initial hosts used to discover the full set of servers.
This list should be in the form <code>host1:port1,host2:port2,...</code>.
Since these servers are just used for the initial connection to discover the full cluster membership (which may change dynamically),
this list need not contain the full set of servers (you may want more than one,
though,
in case a server is down).</p>
数据类型：list
默认的值：
有效的值：
重要等级：高
```

```
属性名称：ssl.key.password
属性描述：The password of the private key in the key store file or the PEM key specified in `ssl.keystore.key'.
This is required for clients only if two-way authentication is configured.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.certificate.chain
属性描述：Certificate chain in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with a list of X.509 certificates</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.key
属性描述：Private key in the format specified by 'ssl.keystore.type'.
Default SSL engine factory supports only PEM format with PKCS#8 keys.
If the key is encrypted,
key password must be specified using 'ssl.key.password'</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.location
属性描述：The location of the key store file.
This is optional for client and can be used for two-way authentication for client.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.keystore.password
属性描述：The store password for the key store file.
This is optional for client and only needed if 'ssl.keystore.location' is configured.
Key store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.certificates
属性描述：Trusted certificates in the format specified by 'ssl.truststore.type'.
Default SSL engine factory supports only PEM format with X.509 certificates.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.location
属性描述：The location of the trust store file.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：ssl.truststore.password
属性描述：The password for the trust store file.
If a password is not set,
trust store file configured will still be used,
but integrity checking is disabled.
Trust store password is not supported for PEM format.</p>
数据类型：password
默认的值：null
有效的值：
重要等级：高
```

```
属性名称：client.dns.lookup
属性描述：Controls how the client uses DNS lookups.
If set to <code>use_all_dns_ips</code>,
connect to each returned IP address in sequence until a successful connection is established.
After a disconnection,
the next IP is used.
Once all IPs have been used once,
the client resolves the IP(s) from the hostname again (both the JVM and the OS cache DNS name lookups,
however).
If set to <code>resolve_canonical_bootstrap_servers_only</code>,
resolve each bootstrap address into a list of canonical names.
After the bootstrap phase,
this behaves the same as <code>use_all_dns_ips</code>.</p>
数据类型：string
默认的值：use_all_dns_ips
有效的值：[use_all_dns_ips,
resolve_canonical_bootstrap_servers_only]
重要等级：medium
```

```
属性名称：client.id
属性描述：An id string to pass to the server when making requests.
The purpose of this is to be able to track the source of requests beyond just ip/port by allowing a logical application name to be included in server-side request logging.</p>
数据类型：string
默认的值：""
有效的值：
重要等级：medium
```

```
属性名称：connections.max.idle.ms
属性描述：Close idle connections after the number of milliseconds specified by this config.</p>
数据类型：long
默认的值：300000 (5 minutes)
有效的值：
重要等级：medium
```

```
属性名称：default.api.timeout.ms
属性描述：Specifies the timeout (in milliseconds) for client APIs.
This configuration is used as the default timeout for all client operations that do not specify a <code>timeout</code> parameter.</p>
数据类型：int
默认的值：60000 (1 minute)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：receive.buffer.bytes
属性描述：The size of the TCP receive buffer (SO_RCVBUF) to use when reading data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：65536 (64 kibibytes)
有效的值：[-1,...]
重要等级：medium
```

```
属性名称：request.timeout.ms
属性描述：The configuration controls the maximum amount of time the client will wait for the response of a request.
If the response is not received before the timeout elapses the client will resend the request if necessary or fail the request if retries are exhausted.</p>
数据类型：int
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：medium
```

```
属性名称：sasl.client.callback.handler.class
属性描述：The fully qualified name of a SASL client callback handler class that implements the AuthenticateCallbackHandler interface.</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.jaas.config
属性描述：JAAS login context parameters for SASL connections in the format used by JAAS configuration files.
JAAS configuration file format is described <a href="http://docs.oracle.com/javase/8/docs/technotes/guides/security/jgss/tutorials/LoginConfigFile.html">here</a>.
The format for the value is: <code>loginModuleClass controlFlag (optionName=optionValue)*;</code>.
For brokers,
the config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.jaas.config=com.example.ScramLoginModule required;</p>
数据类型：password
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.kerberos.service.name
属性描述：The Kerberos principal name that Kafka runs as.
This can be defined either in Kafka's JAAS config or in Kafka's config.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.login.callback.handler.class
属性描述：The fully qualified name of a SASL login callback handler class that implements the AuthenticateCallbackHandler interface.
For brokers,
login callback handler config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.callback.handler.class=com.example.CustomScramLoginCallbackHandler</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.login.class
属性描述：The fully qualified name of a class that implements the Login interface.
For brokers,
login config must be prefixed with listener prefix and SASL mechanism name in lower-case.
For example,
listener.name.sasl_ssl.scram-sha-256.sasl.login.class=com.example.CustomScramLogin</p>
数据类型：class
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.mechanism
属性描述：SASL mechanism used for client connections.
This may be any mechanism for which a security provider is available.
GSSAPI is the default mechanism.</p>
数据类型：string
默认的值：GSSAPI
有效的值：
重要等级：medium
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.url
属性描述：The OAuth/OIDC provider URL from which the provider's <a href="https://datatracker.ietf.org/doc/html/rfc7517#section-5">JWKS (JSON Web Key Set)</a> can be retrieved.
The URL can be HTTP(S)-based or file-based.
If the URL is HTTP(S)-based,
the JWKS data will be retrieved from the OAuth/OIDC provider via the configured URL on broker startup.
All then-current keys will be cached on the broker for incoming requests.
If an authentication request is received for a JWT that includes a "kid" header claim value that isn't yet in the cache,
the JWKS endpoint will be queried again on demand.
However,
the broker polls the URL every sasl.oauthbearer.jwks.endpoint.refresh.ms milliseconds to refresh the cache with any forthcoming keys before any JWT requests that include them are received.
If the URL is file-based,
the broker will load the JWKS file from a configured location on startup.
In the event that the JWT includes a "kid" header value that isn't in the JWKS file,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：sasl.oauthbearer.token.endpoint.url
属性描述：The URL for the OAuth/OIDC identity provider.
If the URL is HTTP(S)-based,
it is the issuer's token endpoint URL to which requests will be made to login based on the configuration in sasl.jaas.config.
If the URL is file-based,
it specifies a file containing an access token (in JWT serialized form) issued by the OAuth/OIDC identity provider to use for authorization.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：security.protocol
属性描述：Protocol used to communicate with brokers.
Valid values are: PLAINTEXT,
SSL,
SASL_PLAINTEXT,
SASL_SSL.</p>
数据类型：string
默认的值：PLAINTEXT
有效的值：
重要等级：medium
```

```
属性名称：send.buffer.bytes
属性描述：The size of the TCP send buffer (SO_SNDBUF) to use when sending data.
If the value is -1,
the OS default will be used.</p>
数据类型：int
默认的值：131072 (128 kibibytes)
有效的值：[-1,...]
重要等级：medium
```

```
属性名称：socket.connection.setup.timeout.max.ms
属性描述：The maximum amount of time the client will wait for the socket connection to be established.
The connection setup timeout will increase exponentially for each consecutive connection failure up to this maximum.
To avoid connection storms,
a randomization factor of 0.2 will be applied to the timeout resulting in a random range between 20% below and 20% above the computed value.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：
重要等级：medium
```

```
属性名称：socket.connection.setup.timeout.ms
属性描述：The amount of time the client will wait for the socket connection to be established.
If the connection is not built before the timeout elapses,
clients will close the socket channel.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：medium
```

```
属性名称：ssl.enabled.protocols
属性描述：The list of protocols enabled for SSL connections.
The default is 'TLSv1.2,TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
With the default value for Java 11,
clients and servers will prefer TLSv1.3 if both support it and fallback to TLSv1.2 otherwise (assuming both support at least TLSv1.2).
This default should be fine for most cases.
Also see the config documentation for `ssl.protocol`.</p>
数据类型：list
默认的值：TLSv1.2
有效的值：
重要等级：medium
```

```
属性名称：ssl.keystore.type
属性描述：The file format of the key store file.
This is optional for client.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
```

```
属性名称：ssl.protocol
属性描述：The SSL protocol used to generate the SSLContext.
The default is 'TLSv1.3' when running with Java 11 or newer,
'TLSv1.2' otherwise.
This value should be fine for most use cases.
Allowed values in recent JVMs are 'TLSv1.2' and 'TLSv1.3'.
'TLS',
'TLSv1.1',
'SSL',
'SSLv2' and 'SSLv3' may be supported in older JVMs,
but their usage is discouraged due to known security vulnerabilities.
With the default value for this config and 'ssl.enabled.protocols',
clients will downgrade to 'TLSv1.2' if the server does not support 'TLSv1.3'.
If this config is set to 'TLSv1.2',
clients will not use 'TLSv1.3' even if it is one of the values in ssl.enabled.protocols and the server only supports 'TLSv1.3'.</p>
数据类型：string
默认的值：TLSv1.2
有效的值：
重要等级：medium
```

```
属性名称：ssl.provider
属性描述：The name of the security provider used for SSL connections.
Default value is the default security provider of the JVM.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：medium
```

```
属性名称：ssl.truststore.type
属性描述：The file format of the trust store file.</p>
数据类型：string
默认的值：JKS
有效的值：
重要等级：medium
```

```
属性名称：metadata.max.age.ms
属性描述：The period of time in milliseconds after which we force a refresh of metadata even if we haven't seen any partition leadership changes to proactively discover any new brokers or partitions.</p>
数据类型：long
默认的值：300000 (5 minutes)
有效的值：[0,...]
重要等级：low
```

```
属性名称：metric.reporters
属性描述：A list of classes to use as metrics reporters.
Implementing the <code>org.apache.kafka.common.metrics.MetricsReporter</code> interface allows plugging in classes that will be notified of new metric creation.
The JmxReporter is always included to register JMX statistics.</p>
数据类型：list
默认的值：""
有效的值：
重要等级：low
```

```
属性名称：metrics.num.samples
属性描述：The number of samples maintained to compute metrics.</p>
数据类型：int
默认的值：2
有效的值：[1,...]
重要等级：low
```

```
属性名称：metrics.recording.level
属性描述：The highest recording level for metrics.</p>
数据类型：string
默认的值：INFO
有效的值：[INFO,
DEBUG,
TRACE]
重要等级：low
```

```
属性名称：metrics.sample.window.ms
属性描述：The window of time a metrics sample is computed over.</p>
数据类型：long
默认的值：30000 (30 seconds)
有效的值：[0,...]
重要等级：low
```

```
属性名称：reconnect.backoff.max.ms
属性描述：The maximum amount of time in milliseconds to wait when reconnecting to a broker that has repeatedly failed to connect.
If provided,
the backoff per host will increase exponentially for each consecutive connection failure,
up to this maximum.
After calculating the backoff increase,
20% random jitter is added to avoid connection storms.</p>
数据类型：long
默认的值：1000 (1 second)
有效的值：[0,...]
重要等级：low
```

```
属性名称：reconnect.backoff.ms
属性描述：The base amount of time to wait before attempting to reconnect to a given host.
This avoids repeatedly connecting to a host in a tight loop.
This backoff applies to all connection attempts by the client to a broker.</p>
数据类型：long
默认的值：50
有效的值：[0,...]
重要等级：low
```

```
属性名称：retries
属性描述：Setting a value greater than zero will cause the client to resend any request that fails with a potentially transient error.
It is recommended to set the value to either zero or `MAX_VALUE` and use corresponding timeout parameters to control how long a client should retry a request.</p>
数据类型：int
默认的值：2147483647
有效的值：[0,...,2147483647]
重要等级：low
```

```
属性名称：retry.backoff.ms
属性描述：The amount of time to wait before attempting to retry a failed request.
This avoids repeatedly sending requests in a tight loop under some failure scenarios.</p>
数据类型：long
默认的值：100
有效的值：[0,...]
重要等级：low
```

```
属性名称：sasl.kerberos.kinit.cmd
属性描述：Kerberos kinit command path.</p>
数据类型：string
默认的值：/usr/bin/kinit
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.min.time.before.relogin
属性描述：Login thread sleep time between refresh attempts.</p>
数据类型：long
默认的值：60000
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.ticket.renew.jitter
属性描述：Percentage of random jitter added to the renewal time.</p>
数据类型：double
默认的值：0.05
有效的值：
重要等级：low
```

```
属性名称：sasl.kerberos.ticket.renew.window.factor
属性描述：Login thread will sleep until the specified window factor of time from last refresh to ticket's expiry has been reached,
at which time it will try to renew the ticket.</p>
数据类型：double
默认的值：0.8
有效的值：
重要等级：low
```

```
属性名称：sasl.login.connect.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider connection timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.login.read.timeout.ms
属性描述：The (optional) value in milliseconds for the external authentication provider read timeout.
Currently applies only to OAUTHBEARER.</p>
数据类型：int
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.login.refresh.buffer.seconds
属性描述：The amount of buffer time before credential expiration to maintain when refreshing a credential,
in seconds.
If a refresh would otherwise occur closer to expiration than the number of buffer seconds then the refresh will be moved up to maintain as much of the buffer time as possible.
Legal values are between 0 and 3600 (1 hour); a default value of  300 (5 minutes) is used if no value is specified.
This value and sasl.login.refresh.min.period.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：300
有效的值：[0,...,3600]
重要等级：low
```

```
属性名称：sasl.login.refresh.min.period.seconds
属性描述：The desired minimum time for the login refresh thread to wait before refreshing a credential,
in seconds.
Legal values are between 0 and 900 (15 minutes); a default value of 60 (1 minute) is used if no value is specified.
 This value and  sasl.login.refresh.buffer.seconds are both ignored if their sum exceeds the remaining lifetime of a credential.
Currently applies only to OAUTHBEARER.</p>
数据类型：short
默认的值：60
有效的值：[0,...,900]
重要等级：low
```

```
属性名称：sasl.login.refresh.window.factor
属性描述：Login refresh thread will sleep until the specified window factor relative to the credential's lifetime has been reached,
at which time it will try to refresh the credential.
Legal values are between 0.5 (50%) and 1.0 (100%) inclusive; a default value of 0.8 (80%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.8
有效的值：[0.5,...,1.0]
重要等级：low
```

```
属性名称：sasl.login.refresh.window.jitter
属性描述：The maximum amount of random jitter relative to the credential's lifetime that is added to the login refresh thread's sleep time.
Legal values are between 0 and 0.25 (25%) inclusive; a default value of 0.05 (5%) is used if no value is specified.
Currently applies only to OAUTHBEARER.</p>
数据类型：double
默认的值：0.05
有效的值：[0.0,...,0.25]
重要等级：low
```

```
属性名称：sasl.login.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
```

```
属性名称：sasl.login.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between login attempts to the external authentication provider.
Login uses an exponential backoff algorithm with an initial wait based on the sasl.login.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.login.retry.backoff.max.ms setting.
Currently applies only to OAUTHBEARER.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.clock.skew.seconds
属性描述：The (optional) value in seconds to allow for differences between the time of the OAuth/OIDC identity provider and the broker.</p>
数据类型：int
默认的值：30
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.expected.audience
属性描述：The (optional) comma-delimited setting for the broker to use to verify that the JWT was issued for one of the expected audiences.
The JWT will be inspected for the standard OAuth "aud" claim and if this value is set,
the broker will match the value from JWT's "aud" claim  to see if there is an exact match.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.expected.issuer
属性描述：The (optional) setting for the broker to use to verify that the JWT was created by the expected issuer.
The JWT will be inspected for the standard OAuth "iss" claim and if this value is set,
the broker will match it exactly against what is in the JWT's "iss" claim.
If there is no match,
the broker will reject the JWT and authentication will fail.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.refresh.ms
属性描述：The (optional) value in milliseconds for the broker to wait between refreshing its JWKS (JSON Web Key Set) cache that contains the keys to verify the signature of the JWT.</p>
数据类型：long
默认的值：3600000 (1 hour)
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms
属性描述：The (optional) value in milliseconds for the maximum wait between attempts to retrieve the JWKS (JSON Web Key Set) from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：10000 (10 seconds)
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.jwks.endpoint.retry.backoff.ms
属性描述：The (optional) value in milliseconds for the initial wait between JWKS (JSON Web Key Set) retrieval attempts from the external authentication provider.
JWKS retrieval uses an exponential backoff algorithm with an initial wait based on the sasl.oauthbearer.jwks.endpoint.retry.backoff.ms setting and will double in wait length between attempts up to a maximum wait length specified by the sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms setting.</p>
数据类型：long
默认的值：100
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.scope.claim.name
属性描述：The OAuth claim for the scope is often named "scope",
but this (optional) setting can provide a different name to use for the scope included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：scope
有效的值：
重要等级：low
```

```
属性名称：sasl.oauthbearer.sub.claim.name
属性描述：The OAuth claim for the subject is often named "sub",
but this (optional) setting can provide a different name to use for the subject included in the JWT payload's claims if the OAuth/OIDC provider uses a different name for that claim.</p>
数据类型：string
默认的值：sub
有效的值：
重要等级：low
```

```
属性名称：security.providers
属性描述：A list of configurable creator classes each returning a provider implementing security algorithms.
These classes should implement the <code>org.apache.kafka.common.security.auth.SecurityProviderCreator</code> interface.</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.cipher.suites
属性描述：A list of cipher suites.
This is a named combination of authentication,
encryption,
MAC and key exchange algorithm used to negotiate the security settings for a network connection using TLS or SSL network protocol.
By default all the available cipher suites are supported.</p>
数据类型：list
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.endpoint.identification.algorithm
属性描述：The endpoint identification algorithm to validate server hostname using server certificate.
</p>
数据类型：string
默认的值：https
有效的值：
重要等级：low
```

```
属性名称：ssl.engine.factory.class
属性描述：The class of type org.apache.kafka.common.security.auth.SslEngineFactory to provide SSLEngine objects.
Default value is org.apache.kafka.common.security.ssl.DefaultSslEngineFactory</p>
数据类型：class
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.keymanager.algorithm
属性描述：The algorithm used by key manager factory for SSL connections.
Default value is the key manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：SunX509
有效的值：
重要等级：low
```

```
属性名称：ssl.secure.random.implementation
属性描述：The SecureRandom PRNG implementation to use for SSL cryptography operations.
</p>
数据类型：string
默认的值：null
有效的值：
重要等级：low
```

```
属性名称：ssl.trustmanager.algorithm
属性描述：The algorithm used by trust manager factory for SSL connections.
Default value is the trust manager factory algorithm configured for the Java Virtual Machine.</p>
数据类型：string
默认的值：PKIX
有效的值：
重要等级：low
</ul>
</div>
```




